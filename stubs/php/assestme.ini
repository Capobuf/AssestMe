; Install for both CLI and FPM when the distribution keeps separate ini trees.
memory_limit = 512M
upload_max_filesize = 25M
post_max_size = 30M
max_file_uploads = 20
max_execution_time = 120
max_input_time = 120
session.cookie_httponly = 1
session.cookie_samesite = Lax
expose_php = Off
