# AssestMe — Deterministic Executable Development Plan

Specification version: **2.4**
Status: **approved for implementation**  
Application name: **AssestMe**  
Reference runtime: **PHP 8.3 capability contract; no normative host distribution**
Primary development URL: **http://127.0.0.1:8000/admin**  
Application language for the first release: **Italian**  
Code, identifiers, commit messages, tests, and code comments: **English**

The spelling **AssestMe** is intentional and final.

This document is the authoritative implementation plan. It is an executable plan in the sense used by OpenAI ExecPlans: it is self-contained, must be updated while work progresses, and must record discoveries, deviations, and final evidence. The implementation agent must not silently reinterpret requirements or replace a selected component.

---

## 1. Product definition

AssestMe is a single-user web application for conducting structured IT assessments for small and medium-sized organizations.

The application must allow the administrator to:

1. manage clients and their sites;
2. optionally register generic or detailed IT assets;
3. maintain a reusable library of finding templates;
4. import and export finding templates in a versioned JSON format;
5. create an assessment for a client;
6. add, locate, edit, duplicate, order, and remove findings through a structured list and single-record lateral inspector;
7. associate a finding with the whole organization, one or more selected sites, a network scope, selected assets, or a custom scope;
8. define multiple remediation solutions for the same finding;
9. select one recommended solution and, when the finding is resolved, one implemented solution;
10. evaluate the priority of intervention manually or through a configurable consequence × likelihood matrix;
11. express the implementation effort and an indicative economic estimate without creating a quotation;
12. attach images, PDFs, documents, logs, links, and other evidence;
13. generate a professional configurable PDF report;
14. generate an XLSX export containing the complete textual summary of the assessment;
15. archive or permanently delete data according to the configured deletion policy.

The application is not:

- a quotation or invoicing system;
- a project management system;
- a vulnerability scanner;
- a CMDB requiring complete asset registration;
- a multi-tenant SaaS;
- a client portal;
- an AI service;
- an audit-trail system;
- a document version-control system.

AI is external to the application. It may generate JSON template libraries, but AssestMe does not record or infer whether a template was generated by AI.

---

## 2. Signed decision register

The following decisions are normative. `Status: APPROVED` means an implementation agent may not reinterpret the decision. A decision can become `SUPERSEDED` only after explicit user approval is recorded in this document.

| ID | Status | Decision |
|---|---|---|
| D-001 | APPROVED | Laravel 13 and Filament 5 Panel Builder |
| D-002 | APPROVED | Exactly one administrator account; no registration, roles, or client access |
| D-003 (v1) | SUPERSEDED | Ubuntu Server 24.04 LTS was the normative reference environment; superseded by D-003 on 2026-07-17 |
| D-003 | APPROVED | No host operating-system distribution is normative; runtime and bootstrap are capability-based |
| D-004 (v1) | SUPERSEDED | PHP 8.3 was required from the official Ubuntu 24.04 repositories; superseded by D-004 on 2026-07-17 |
| D-004 | APPROVED | PHP 8.3 is required without prescribing an operating-system package source |
| D-005 | APPROVED | SQLite with foreign keys, WAL, and 5-second busy timeout |
| D-006 | APPROVED | File cache/session and synchronous queue; no Redis or worker |
| D-007 (v1) | SUPERSEDED | Docker was prohibited together with frontend build dependencies; superseded by D-007 on 2026-07-17 |
| D-007 (v2) | SUPERSEDED | No Node.js/npm/pnpm/Vite build or external runtime service; Docker was permitted for future packaging but was not required; superseded by D-007 on 2026-07-18 |
| D-007 | APPROVED | The application requires no Node.js frontend build; Docker Compose is the maintained and authoritative local development environment |
| D-008 (v1) | SUPERSEDED | Advance Table Repeater was the selected assessment-grid component; superseded by D-008 on 2026-07-13 |
| D-008 (v2) | SUPERSEDED | The assessment workspace used only the native Filament 5 table Repeater; superseded by D-008 on 2026-07-18 |
| D-008 | APPROVED | The Finding workspace uses a Filament Table structured list with a single-record lateral inspector; native Repeaters remain permitted for bounded child collections such as Finding solutions |
| D-009 (v1) | SUPERSEDED | Spatie Laravel PDF with DOMPDF as the only renderer and no renderer validation path; superseded by D-009 on 2026-07-24 |
| D-009 | APPROVED | Use WeasyPrint through Spatie Laravel PDF as the sole production renderer; the completed Chrome-first D-059 validation accepted WeasyPrint, with no automatic fallback or parallel renderer |
| D-010 | APPROVED | One configurable professional report layout |
| D-011 | APPROVED | Italian v1 UI/report, translation keys from the first commit |
| D-012 | APPROVED | JSON Schema v1 is the canonical template interchange contract |
| D-013 | APPROVED | Priority = consequence × likelihood, with reasoned manual override |
| D-014 | APPROVED | Economic values are indicative, never totaled as a quotation, and always understood as excluding VAT |
| D-015 | APPROVED | Asset association is optional and scope can be organization-wide |
| D-016 | APPROVED | Autosave plus explicit draft save, both using the same persistence layer |
| D-017 | APPROVED | Archive/permanent-delete behavior follows one global setting |
| D-018 | APPROVED | Fail loudly; no silent fallback, omission, or placeholder success |
| D-019 (v1) | SUPERSEDED | Nginx + PHP-FPM was the production profile and direct Artisan startup was the local development profile; superseded by D-019 on 2026-07-18 |
| D-019 | APPROVED | The only supported installation profiles are Docker Compose for development and CloudPanel for production |
| D-020 | APPROVED | Optimistic locking and idempotent workspace-save requests |
| D-021 | APPROVED | Completed assessments are read-only until explicitly reopened |
| D-022 | APPROVED | Every generated report stores a complete normalized immutable payload snapshot |
| D-023 | APPROVED | File deletion uses recoverable staging, not a false claim of SQL/filesystem atomicity |
| D-024 | APPROVED | Referenced finding solutions cannot be deleted until references are reassigned |
| D-025 | APPROVED | Template import is full replace by stable template/solution external IDs |
| D-026 | APPROVED | PhpSpreadsheet is the direct XLSX implementation dependency |
| D-027 | APPROVED | Laravel Dusk is used for browser behavior tests without Node.js |
| D-028 | APPROVED | TOTP MFA is optional, with recovery codes and CLI reset |
| D-029 | APPROVED | Local backups are implemented and tested; off-host replication is operationally external |
| D-030 | APPROVED | Composer lock is the authoritative exact dependency set |
| D-031 | APPROVED | Vendor code is never edited; a fork requires explicit approval |
| D-032 | APPROVED | UTC storage and Europe/Rome presentation |
| D-033 | APPROVED | Integer database primary keys; no UUID/ULID migration complexity |
| D-034 | APPROVED | No audit log or historical finding revisions in v1 |
| D-035 | APPROVED | API, MCP server, Jira, runZero, and scanner imports are outside v1 |
| D-036 | APPROVED | Univer Sheet is not installed and never stores assessment findings |
| D-037 | APPROVED | Right Click and Advanced Table Export enhance standard resource tables only |
| D-038 | APPROVED | Risk profiles own consequence, likelihood, priority, and matrix records; effort levels are global |
| D-039 | APPROVED | VAT treatment is not modeled or configurable; reports and XLSX contain one mandatory VAT-excluded estimate note |
| D-040 | APPROVED | Optional consultant identity/contact/logo and text-only signature fields are explicitly defined for reports |
| D-041 | APPROVED | Development verification is autonomous and isolated; manual cross-browser/device QA is deferred until a reference installation and devices exist |
| D-042 (v1) | SUPERSEDED | The Docker development profile published the development HTTP port only on host loopback; superseded by D-042 on 2026-07-18 |
| D-042 | APPROVED | The Docker development profile is defined by `docker/compose.dev.yml`, a project-owned PHP 8.3 development image, bind-mounted source code, persistent SQLite state, optional isolated Selenium browser testing, and HTTP publication on all host IPv4 interfaces |
| D-043 | APPROVED | Italian user-facing terminology uses Azienda/Aziende and Intera azienda; internal `Client`, `clients`, `client_id`, relations, and persistence contracts remain unchanged |
| D-044 | APPROVED | Assessment creation proposes a reusable automatically generated but user-editable title, exposes only organization/site/custom assessment scopes, filters one or more sites by company, and makes the Workspace the primary post-create and list-row destination |
| D-045 | APPROVED | Finding asset association remains optional for every scope except `selected_assets`, which requires at least one same-company asset; conditional site/asset UI and completion/report validation use the same domain rule |
| D-046 | APPROVED | The dashboard separates four compact clickable operational KPIs, the latest five assessments, urgent findings, and a compact application-status section for backup, database integrity, and unresolved cleanup operations |
| D-047 | APPROVED | Company, site, asset, template, and generated-file Filament surfaces use the approved native responsive layouts, explicit create/cancel destinations, conditional solution fields, and compact tabular generated-file history |
| D-048 | APPROVED | Manually created template and solution external IDs are generated deterministically from titles on first authoritative save, collision-suffixed, immutable thereafter, and preserved unchanged by JSON import/export |
| D-049 | APPROVED | Risk profile and level technical codes are generated on creation and immutable after first save; existing level identity is preserved and the sixteen entries are edited through a deterministic 4×4 consequence-by-likelihood matrix |
| D-050 | APPROVED | PDF generation creates one immutable authoritative report, applies any configured freeze, immediately downloads that same authenticated hash-verified file, and retains it in generated-file history |
| D-051 | APPROVED | Report settings retain consultant/company/both branding and add typed cover-title mode plus optional priority-legend descriptions without changing immutable settings snapshots |
| D-052 | SUPERSEDED | The A4 portrait DOMPDF composition is retained as implementation history; its future report composition is superseded by D-059 on 2026-07-24 |
| D-053 | APPROVED | Risk-matrix editing uses an application-owned custom Filament Field with one independent select in each semantic 4×4 table cell; nested Livewire state is keyed by persisted level IDs, `risk_matrix_entries` remains authoritative, and no plugin, Node.js pipeline, or runtime dependency is added |
| D-054 | APPROVED | The Finding workspace has exactly two intentional container-responsive modes: lateral desktop split at an effective workspace-container width of at least 64rem, and sequential list/detail below that threshold; the intermediate absolute-positioned inspector is removed |
| D-055 | APPROVED | The Finding workspace presents a three-area application workbench with a compact Filament Table navigator, a dominant selected-Finding editor, and contextual properties; narrow containers retain deliberate sequential navigator/editor behavior |
| D-056 | APPROVED | The Finding workbench uses the approved lime/green/white/black palette, one solid primary action per context, aggregated export and Finding-creation actions, and a reduced navigator row containing only selection-critical information |
| D-057 | APPROVED | Filament 5 native clusters and parent items define the hierarchical sidebar; Tag is removed from v1; Finding assets remain required only for `selected_assets`; and implementation verification is proportional while `scripts/verify.sh` remains the complete authoritative gate |
| D-058 | APPROVED | The compact Finding navigator retains expandable icon search but removes its unusable filter control, places icon-only native reorder beside search, and keeps Finding titles visible during reorder; the non-functional summary preview tab is removed, contextual properties use compact collapsible sections with reliable internal scrolling, and generated-file history receives an aligned responsive tabular presentation |
| D-059 (v1) | SUPERSEDED | The portrait-only DOMPDF editorial layout and manually duplicated indicative HTML preview are retained as implementation history; superseded by D-059 on 2026-07-24 |
| D-059 | APPROVED | Accepted pending technical validation: one Blade/CSS editorial report must support mixed A4 orientation and a same-view preview without changing immutable report persistence |
| D-060 | APPROVED | The report-settings preview renders a transient PDF through the same application-owned WeasyPrint renderer service and `reports.assessment` Blade/CSS used for production, exposes only operative settings with explicit scope descriptions, and supports a temporary dependency-free fullscreen view without creating immutable report state |

### D-001 — Framework

Use Laravel 13 and Filament 5 Panel Builder with Livewire 4.

### D-002 — Product boundary and account model

AssestMe is an internal, single-instance, multi-client application used by one administrator.

Requirements:

- exactly one row may exist in `users`;
- no public registration;
- no client authentication;
- no role/permission package;
- no audit log;
- no change-history/version browser for findings;
- no public API or MCP server in v1;
- no portal, quotation, ticketing, scanner, Jira, or runZero integration in v1.

Enforce the single-user rule with a unique `singleton_key` column fixed to `1`, application validation, and tests.

### D-003 — Runtime portability

No host operating-system distribution or CPU architecture is normative for the application.

The project-owned development image and the future CloudPanel production site provide the required runtime capabilities. Application scripts:

- verify capabilities they actually use;
- do not install operating-system packages;
- do not reject a host by distribution name, version, or architecture;
- fail with the exact missing command, PHP extension, permission, or runtime constraint.

The current GitHub Actions workflow may use a hosted Ubuntu runner as CI infrastructure. That runner does not define or certify the production operating system.

### D-004 — PHP

Use PHP 8.3. The project-owned development image supplies it for Docker Compose. The future CloudPanel implementation must supply it for production without making a host operating-system distribution normative.

Required extensions:

- `bcmath`
- `ctype`
- `curl`
- `dom`
- `fileinfo`
- `filter`
- `gd`
- `iconv`
- `intl`
- `libxml`
- `mbstring`
- `openssl`
- `pdo`
- `pdo_sqlite`
- `session`
- `simplexml`
- `tokenizer`
- `xml`
- `xmlreader`
- `xmlwriter`
- `zip`
- `zlib`

Laravel 13 requires PHP 8.3 or newer. AssestMe deliberately targets PHP 8.3 for one reproducible runtime contract across development, CI, production, and future container packaging.

### D-005 — Database

Use one SQLite file:

```text
database/database.sqlite
```

Connection settings:

```text
foreign_keys = ON
journal_mode = WAL
busy_timeout = 5000 ms
synchronous = NORMAL
transaction_mode = IMMEDIATE
```

Rules:

- do not use SQLite-only schema types or raw vendor-specific migration syntax;
- use portable Laravel migrations;
- verify the effective PRAGMAs at application startup in local/testing and through `assestme:diagnose`;
- use `IMMEDIATE` transaction mode for write transactions so lock acquisition fails before partial workspace processing;
- treat `database is locked` as a visible failure;
- do not retry indefinitely.

### D-006 — Queue, cache, and sessions

Initial runtime:

```dotenv
QUEUE_CONNECTION=sync
CACHE_STORE=file
SESSION_DRIVER=file
```

No Redis, Horizon, queue worker, or asynchronous report generation.

### D-007 — Frontend build and authoritative development environment

The application does not require Node.js and has no frontend build. Docker Compose is the official and authoritative development environment. A development host requires only Docker, Docker Compose, and the AssestMe repository. PHP, Composer, PHP extensions, and WeasyPrint are supplied by the application container and must not be installed directly on the development host as part of the supported installation profile.

Allowed:

- Filament's published compiled assets;
- Blade;
- Livewire;
- Alpine bundled by Filament;
- plain application-owned JavaScript registered through Filament asset management;
- Composer packages.

Prohibited in development and runtime:

- Node.js;
- npm;
- pnpm;
- Yarn;
- Vite;
- Redis;
- external databases;
- queue workers;
- frontend development servers.

Laravel Dusk may use the optional profiled Selenium service. Native Chrome/ChromeDriver may remain in CI where the technically reusable isolated runner is invoked outside the supported workstation installation profile. Selenium is not a runtime or production dependency and is not a build step.

### D-008 (v2) — Central assessment grid

Use only native Filament 5 form and schema components for the first implementation of the assessment workspace.

The central findings grid uses a native Filament `Repeater` configured with its table layout through `Repeater::table()`. Each finding remains a related Eloquent record managed through the Repeater relationship.

Use native Filament components inside the table:

- `Textarea` for the multiline problem, recommended-solution summary projection, and entrepreneur-facing notes;
- `TextInput` for short text and monetary values;
- `Select` for category, priority, effort, estimate type, and status;
- `Toggle` or `Checkbox` for report inclusion;
- native Repeater item actions for creation, cloning, deletion, and reordering.

Do not install a third-party grid, table-repeater, or spreadsheet package for the assessment workspace in this phase. Do not create a custom JavaScript spreadsheet component in this phase. The native implementation is the primary implementation, not a fallback.

The first release does not require:

- multi-cell copy and paste;
- range selection;
- Excel-style fill handles;
- column resizing;
- arbitrary column reordering;
- table pagination inside the assessment form.

These capabilities may be reconsidered only after the native workspace has been implemented and tested with real assessments.

Milestone 0 must prove that the native Repeater table can:

- render multiline `Textarea` fields and preserve line breaks;
- persist a `HasMany` relationship;
- add, clone, delete, and reorder findings;
- manage at least 10, 25, and 50 findings;
- use controlled autosave and explicit save through the same application service;
- display validation and persistence errors without losing current form state.

If a native capability does not behave as required, record the exact limitation in Discoveries. Do not install an alternative plugin without explicit approval.

### D-008 — Finding list/detail workspace

The previous requirement to represent the complete Finding collection through one native table Repeater is **SUPERSEDED by D-008**. The Finding workspace uses Filament Table Builder as the authoritative list engine and an application-style structured row rather than a conventional multi-column CRUD grid. Selecting one row opens a lateral inspector that loads and edits only that complete Finding aggregate. Closing the inspector restores the list to the full available width. Container-responsive behavior presents a side-by-side layout on wide containers, an overlaid inspector at intermediate widths, and consecutive list/detail views on mobile.

The list owns Eloquent query, search, filters, deterministic reorder, empty state, collection actions, accessible row selection, inline status, and inline report-inclusion controls. Only status and report inclusion are editable inline. The inspector owns the complete single-Finding editor, collapsible description, scope/risk, solutions, and evidence sections, previous/next navigation, explicit save, save-and-next, dirty/offline/error/conflict states, and read-only rendering for completed or archived assessments.

The native Filament Repeater remains permitted for bounded child collections such as the solutions belonging to the selected Finding. It is not the primary Finding-list workspace. No Node.js pipeline, external data grid, JavaScript persistence layer, or frontend runtime dependency is introduced.

This decision supersedes only the incompatible table-Repeater presentation and whole-Finding-collection submission rules in §§9.4–9.5, 10.1, 16.1, Milestones 0/3, and their corresponding acceptance wording. Assessment metadata may retain its separate signed persistence path. All other approved domain, persistence, optimistic-locking, idempotency, lifecycle, report, evidence, deployment, and test decisions remain authoritative. Every Finding mutation continues to serialize on the assessment save lock, compare and increment `lock_version`, and record an idempotent request UUID; no unsigned or last-write-wins path is permitted.

### D-009 (v1) — PDF renderer

Use:

```text
spatie/laravel-pdf
dompdf/dompdf
```

Environment key:

```dotenv
LARAVEL_PDF_DRIVER=dompdf
```

DOMPDF is the only renderer. CSS must stay within tested DOMPDF capabilities.

This decision records the renderer contract implemented and accepted on 2026-07-13. Its permanent DOMPDF exclusivity and absence of a validation path are **SUPERSEDED by D-009**; the implementation history and evidence remain valid.

### D-009 — PDF renderer

**Status: APPROVED — Validation completed and production migration accepted.**

WeasyPrint is the sole production renderer through `spatie/laravel-pdf`, with `LARAVEL_PDF_DRIVER=weasyprint` and a configurable local binary path. The completed D-059 spike evaluated the official Chrome driver first: it failed the blocking mixed-A4 MediaBox requirement, while WeasyPrint passed mixed orientation, named pages, page counters, selectable text, images, indivisible blocks, and continuation requirements. DOMPDF, `DomPdfCanvasDriver`, Chrome, and their application runtime configuration are removed. No automatic fallback, custom replacement driver, parallel renderer, SaaS renderer, or external rendering service is permitted. CSS Paged Media owns page size, orientation, margins, header, and footer.

### D-010 — Report model

Provide one Blade report layout with configurable sections and branding.

Users cannot upload or edit Blade templates.

### D-011 — Language

The v1 UI and report are Italian.

Code, identifiers, tests, comments, and commits are English.

All user-visible application strings use Laravel translation keys. Template records do not contain a locale field; their language is their content language.

### D-012 — Template contract

JSON Schema Draft 2020-12, `schema_version = 1`, is canonical.

Use `opis/json-schema` directly. Application validation adds cross-record checks that standard JSON Schema cannot express.

CSV/XLSX template import is outside v1. JSON export is required.

### D-013 — Priority

Default mode:

```text
matrix_with_override
```

Manual override is available only on editable assessments and always requires a reason.

### D-014 — Estimates

Supported estimate types:

- `exact`;
- `range`;
- `bundled`;
- `requires_quote`;
- `requires_analysis`;
- `variable`;
- `not_applicable`.

No total is calculated or displayed.

### D-015 — Scope and assets

Finding scope values:

- `organization`;
- `selected_sites`;
- `network`;
- `selected_assets`;
- `custom`.

Asset records are optional.

### D-016 — Saving

Provide:

- autosave for committed field changes;
- explicit **Salva assessment** for full draft validation and persistence;
- separate **Completa assessment** for completion validation and state transition.

Autosave never marks an assessment completed.

### D-017 — Deletion

Global setting:

- `archive`;
- `permanent`.

Permanent deletion uses staged file moves and recovery records as defined later.

### D-018 — Failure behavior

No silent degradation.

A failure must:

1. leave persistent data in a known state;
2. log structured context;
3. show an actionable Italian error;
4. avoid success notifications;
5. be covered by a failure-path test where practical.

### D-019 — Supported installation profiles

Development has one supported profile: `docker/compose.dev.yml`. The application is served by Laravel Artisan inside the `app` container and is available at `http://127.0.0.1:8000/admin` or, subject to the development host firewall, at `http://<development-host-ip>:8000/admin` from an authorized local network.

Production has one approved destination: a dedicated PHP site managed by CloudPanel. The CloudPanel procedure is outside this activity and is not yet implemented. A future approved decision and implementation must define at least PHP-FPM, document root, `.env`, SQLite, permissions, scheduler, backup, deployment, update, and rollback behavior.

The following are not supported installation profiles:

- development with PHP and Composer installed directly on the workstation;
- generic Nginx/PHP-FPM servers outside CloudPanel;
- Laravel Sail;
- Kubernetes;
- Docker Swarm;
- shared hosting without shell access;
- external managed databases;
- the previous generic `releases/shared/current` layout.

Scripts that remain technically executable on a host may be retained for CI, diagnostics, or internal reuse, but they do not define a third installation method.

### D-020 — Concurrency

Every assessment has `lock_version`.

Every save request has a UUID idempotency key and expected version. A stale version causes a conflict; the server never performs last-write-wins.

### D-021 — Lifecycle

Assessment status:

- `draft`;
- `completed`;
- `archived`.

Completed and archived assessments are read-only. Reopening is explicit.

### D-022 — Report immutability

Each generated PDF/XLSX stores:

- complete normalized data snapshot;
- settings snapshot;
- payload SHA-256;
- file SHA-256;
- sequential version number.

The stored file is authoritative. Re-rendering a snapshot may be data-equivalent but is not promised to be byte-identical.

### D-023 — File deletion

Files are atomically renamed within the same filesystem into a private trash operation directory before database deletion. Database failure restores them. Post-commit physical cleanup is tracked and retryable.

### D-024 — Finding solutions

All finding solutions use soft deletes.

A solution selected as recommended or implemented cannot be deleted. The administrator must reassign or clear the reference first.

### D-025 — Import update semantics

Template and solution `external_id` fields are stable lowercase identifiers.

Updating an existing template is a complete replacement of all import-managed fields and solutions inside one transaction.

### D-026 — XLSX

Use `phpoffice/phpspreadsheet` directly. Do not rely on a transitive dependency of a Filament export plugin.

### D-027 — Browser testing

Use Laravel Dusk with Chrome/ChromeDriver in development/CI. No Node.js or Playwright.

The maintained Dusk suite is the automated browser gate while the application is under active development. It covers the browser behaviors already represented by executable tests and must run against an isolated disposable database and storage root.

Manual Edge, Firefox, iOS Safari, Android Chrome, camera, and device-specific checks are deferred until a reference installation and suitable devices exist. They are compatibility evidence for a later production-release decision and do not block development milestones.

### D-041 — Autonomous isolated verification

No development or CI verification command may read, migrate, truncate, seed, back up, restore, benchmark, or otherwise mutate the configured application database or private storage.

Required behavior:

- the maintained entry points `php artisan test`, `composer quality` (including Canary), `composer browser` (Dusk), the benchmark, and `scripts/verify.sh` use disposable file-backed SQLite databases created specifically for that run;
- `composer quality` and a complete `composer browser` run write non-versioned receipts bound to the exact executable source, authoritative plan through section 21, dependency, PHP runtime, and extension fingerprint only after success; factual Progress, Discoveries, and Final outcome updates are excluded because they record evidence rather than change executable behavior, and focused browser runs do not write a complete-browser receipt;
- `scripts/verify.sh` is the authoritative aggregate entry point and executes each expensive gate at most once, reusing a quality or complete-browser result only while its exact fingerprint receipt still matches; source, dependency, or runtime changes invalidate reuse;
- test cache, sessions, generated reports, evidence, backups, temporary uploads, and deletion trash use disposable test roots;
- verification creates its own domain fixtures and administrator when required;
- commands clean their disposable state on success and failure;
- a missing seed, template, administrator, or reference assessment in the application database cannot make a test or benchmark fail;
- the application may have no initialized reference database while development is in progress;
- `scripts/bootstrap-local.sh` is the environment-neutral application bootstrap reused by the Docker entrypoint and technically reusable by CI; it is not a direct-host installation profile;
- tests proving backup/restore or deployment behavior operate only on temporary paths.

Until a reference installation and test devices exist, the complete blocking automated acceptance gate remains Composer validation/audit, Pint, PHPStan, Pest, Canary strict zero-skip, the maintained Dusk suite where ChromeDriver is available, diagnostics against an isolated initialized database, the isolated 50-finding benchmark, storage audit, and `scripts/verify.sh`. D-057 governs proportional verification during implementation. Manual cross-browser/device QA is recorded as deferred, never falsely reported as passed.

### D-042 — Docker development profile

The authoritative development command is:

```bash
docker compose -f docker/compose.dev.yml up --build
```

No Compose file exists at the repository root. The topology contains only the required `app` service and the optional `selenium` service under the `browser` profile. Normal `up` starts only `app`; `app` has no mandatory dependency on Selenium. No custom network, reverse proxy, database service, cache, mail service, administration service, worker, or other infrastructure service is permitted.

The project-owned development image uses the official Debian-based PHP `8.3.32-cli-bookworm` image, Composer `2.10.2` copied from the official Composer image, Xdebug `3.5.3`, and WeasyPrint `57.2`. It supplies Bash, Git, Curl, Unzip, Zip, the utilities used by maintained scripts, the native/font libraries required by WeasyPrint, development-only Poppler utilities, and every D-004 extension. PHP extensions are built and enabled with the official PHP image helpers; Xdebug is installed through PECL without a third-party extension installer. The application image contains no Nginx, PHP-FPM, Node.js, Redis, external database, Supervisor, systemd, Horizon, queue worker, Chrome, or ChromeDriver. The optional Selenium service remains isolated browser-test infrastructure and is not a report renderer. The repository is not copied into the image.

The `app` service:

- works at `/workspace` and bind-mounts the complete repository there;
- runs `php artisan serve --host=0.0.0.0 --port=8000 --no-reload`; Laravel's `--no-reload` mode is required so the child PHP server retains the Docker process overrides instead of replacing them with host-specific values from `.env`;
- publishes `0.0.0.0:8000:8000`, making the development HTTP service reachable through every host IPv4 interface, including an authorized local network;
- uses `init: true` and an HTTP login-page healthcheck;
- has no `container_name`, privileged mode, Docker socket, added capability, or named source-code volume;
- maps `host.docker.internal` to `host-gateway` for Linux development;
- detects the bind-mounted repository UID/GID, accepts explicit `UID`/`GID` overrides, and drops entrypoint privileges before bootstrap so normal repository, Composer, Laravel, and SQLite writes use the host repository owner rather than root.

The entire repository bind mount is the persistence boundary. PHP, Blade, Livewire, configuration, translation, application CSS/JavaScript, and test edits are visible immediately without rebuild or Compose Watch. `.env`, `vendor/`, the complete `database/` directory including SQLite WAL/SHM files, `storage/`, generated reports, evidence, and `backups/` remain on the host. No named SQLite volume and no single-file SQLite mount is allowed; stop, recreation, and image rebuild must not delete this state.

`docker/dev/php.ini` is development-only and defines the approved 512 MB memory limit, 25 MB upload limit, 30 MB post limit, 20 upload-file limit, 120-second execution limit, full visible/logged errors, and timestamp-validating OPcache. Xdebug is loaded but its default mode is `off`; its client is `host.docker.internal:9003`, and users enable it only by explicitly setting `XDEBUG_MODE`. Tests, quality, benchmark, Dusk, and verification do not enable it automatically. Production PHP stubs contain no Docker or Xdebug configuration.

`.env.example` remains environment-neutral. Compose supplies process overrides for `DB_DATABASE=/workspace/database/database.sqlite`, `LARAVEL_PDF_DRIVER=weasyprint`, `LARAVEL_PDF_WEASYPRINT_BINARY=/usr/bin/weasyprint`, `ASSESTME_BACKUP_ROOT=/workspace/backups`, and `APP_URL=http://127.0.0.1:8000`. No Docker-specific Laravel environment file or versioned secret exists.

`docker/dev/entrypoint.sh` uses `set -Eeuo pipefail`, validates the bind mount, prepares writable per-UID temporary Composer state, invokes the single environment-neutral `scripts/bootstrap-local.sh`, and executes the received command. The shared idempotent bootstrap creates only missing `.env`, directories, and SQLite state; installs Composer dependencies only when `composer.lock` differs or `vendor` is absent; generates only a missing application key; applies pending migrations and domain seeds; publishes Filament assets; preserves an existing singleton administrator and creates one only when absent; clears obsolete Laravel caches; runs preflight and diagnostics; and fails on incomplete initialization. It never runs `migrate:fresh`, deletes normal database/storage/backups, replaces an application key, changes existing administrator credentials, creates a second user, or hides an error.

The optional browser service uses `selenium/standalone-chromium:4.46.0-20260707`, 2 GB shared memory, no host WebDriver publication, and a readiness healthcheck. Dusk retains D-041 isolation: a marked temporary root, temporary file SQLite database, storage, cache, sessions, backups, and cleanup on success or failure. Native defaults bind and address the isolated server through `127.0.0.1`. Docker execution binds it to `0.0.0.0`, gives the `app` service the default-network alias `assestme-app`, gives the remote browser `http://assestme-app:<port>`, checks readiness locally through `127.0.0.1`, and uses `http://selenium:4444` without starting local ChromeDriver or silently falling back. The alias avoids Chromium treating the bare service name `app` as the HSTS-preloaded public `.app` name; it is not a custom network or `container_name`.

Script classification:

- environment-neutral: `scripts/preflight.sh`, `scripts/bootstrap-local.sh`, `scripts/isolated-environment.sh`, `scripts/quality-isolated.sh`, `scripts/dusk-isolated.sh`, and `scripts/verify.sh`;
- Docker-specific: `docker/dev/entrypoint.sh` and `docker/compose.dev.yml`;
- future CloudPanel-specific: none yet; CloudPanel production configuration is not implemented by this decision;
- legacy/deprecated generic-host production artifacts: `scripts/deploy-production.sh`, `scripts/rollback-production.sh`, `scripts/rehearse-production.sh`, `stubs/nginx/assestme.conf`, `stubs/php/assestme.ini`, and `stubs/cron/assestme`;
- deprecated direct-host installation: none is supported; technically reusable host execution of environment-neutral scripts is limited to CI, diagnostics, and internal verification.

### D-043 — Italian company terminology

Every Italian UI, validation message, PDF, and XLSX label uses `Azienda`, `Aziende`, and `Intera azienda` instead of `Cliente`, `Clienti`, and `Intera organizzazione`. This is a presentation contract only. English application identifiers and the existing `Client`, `ClientResource`, `clients`, `client_id`, and relationship/database contracts are unchanged.

### D-044 — Assessment title, scope, and primary navigation

Assessment creation proposes `<Azienda> — <Ambito> — <gg/mm/aaaa>` through one testable application implementation. Company, assessment scope, selected sites, custom scope description, and date update the proposal only until the user edits the visible title manually. The saved `title` remains an ordinary value and is never rewritten after creation because a company or site is renamed.

Assessment-level scope choices are exactly `organization`, `selected_sites`, and `custom`; the broader `ScopeType` enum remains valid for individual findings. Site scope accepts one or more sites belonging to the selected company and removes invalid site selections after a company change. Custom scope requires `scope_description`; the same field is optional and labelled as notes for other scopes. Create/save opens the Workspace, the list row opens the Workspace, general-data edit remains an explicit secondary action, and create cancellation returns to the assessment list.

### D-045 — Finding scope relations

The blanket sentence “Asset association is optional” in D-015 is **SUPERSEDED only for `selected_assets` findings by D-045**. D-015 otherwise remains APPROVED. An included finding requires at least one asset only when its explicit scope is `selected_assets`. No asset is required for `organization`, `selected_sites`, `network`, or `custom`; selected sites remain required only for `selected_sites`. Assets and sites must belong to the assessment company. The Workspace conditionally shows the relevant native Filament relation control, while save, completion, and report generation delegate to the same server-side rule.

### D-046 — Dashboard information architecture

The previous presentation of backup, integrity, and cleanup alongside operational counts as equal large statistics is **SUPERSEDED by D-046**. The dashboard contains four compact, content-height, clickable operational KPIs: draft assessments, completed assessments, open findings, and included open high/critical findings. It separately shows the latest five assessments and compact urgent findings, with primary links to the applicable filtered assessment list or Workspace. `Stato applicazione` shows one compact row per backup, database-integrity, and unresolved-cleanup check with status, last event, concise message, and an action when applicable. Severity uses text or an icon in addition to color, and messages do not claim an application block that is not enforced.

### D-047 — Native Filament form and generated-file layouts

Company creation save/cancel returns to the company list while `Salva e nuovo` resets to a blank create form and duplicate warnings remain active. Site and asset forms use native responsive grids: identity/assignment stays compact, detail sections span full width, and narrow viewports use one column. Template finding forms use full-width Content, Classification/defaults, Solutions, and Advanced options sections. Every solution is at most two columns, with full-width long text and native conditional monetary/range/custom-frequency fields. Generated files use a compact tabular history containing format/version, filename, configured-timezone date/time, size, and authenticated Download/Delete actions; destructive deletion remains confirmed and recoverable.

### D-048 — Stable external IDs

Manual template saves generate a lowercase contract-compatible `external_id` from the title when creating the record. Global collisions use the first available deterministic numeric suffix `-2`, `-3`, and so on. Manual nested solutions use the same rule with uniqueness scoped to the template. Existing template and solution IDs are immutable in the authoritative save action even for manipulated payloads; title changes never regenerate them. JSON imports preserve supplied IDs and retain the schema, replace/skip, and round-trip contracts. Advanced options hide input on new records and show persisted IDs read-only on existing records.

### D-049 — Stable risk identities and 4×4 matrix

Risk profile, consequence, likelihood, and priority codes are generated from their initial labels during creation with deterministic collision suffixes in their existing uniqueness scopes. After persistence, the authoritative aggregate action rejects any code mutation. Existing level integer IDs are carried in the form and matched explicitly so label edits update the same row rather than creating a replacement. Level forms normally show label, optional description, applicable score, color, order, and enabled status; codes are advanced read-only values. The previous vertical matrix repeater with manually repeated consequence/likelihood codes is **SUPERSEDED by D-049**. A simple Filament/Blade 4×4 grid renders consequence rows, likelihood columns, and one native priority select per cell with priority label and color. The action still validates and atomically persists exactly sixteen same-profile combinations.

### D-050 — Authoritative PDF download and history

The previous Workspace behavior that generated a PDF and only redirected to history is **SUPERSEDED by D-050**. One `GenerateAssessmentPdf` invocation creates the immutable record, snapshot, hashes, and authoritative private file and applies `freeze_after_generation` when configured. Its successful Livewire/Filament response immediately starts an authenticated download through the existing hash-verifying route for that same record; it never rerenders or copies to a different temporary output. The record remains in history. Validation or generation failure remains visible and never triggers a success or download.

### D-051 — Cover and priority-legend settings

Branding values remain exactly consultant, company, and both. A typed cover-title mode is added with `separate` and `combined` values. Separate mode prints the effective report title and company name on distinct lines; combined mode prints `<effective title> — <azienda>` once and omits the separate company line. `report_title_override` and the title pattern remain supported without automatic company-name repetition. A typed boolean controls whether optional priority descriptions appear in the legend; color and label always appear and an empty description produces no placeholder.

### D-052 — Entrepreneur-oriented PDF composition

**Status: SUPERSEDED by D-059 on 2026-07-24.** The following text records the accepted DOMPDF implementation and its historical evidence; D-059 now governs future report composition, orientation, pagination, and preview. Its immutable-data preservation and no-omission principles are carried forward explicitly by D-059.

The “Exactly” nine-column summary table in §13.4 is **SUPERSEDED by D-052**; all nine logical values remain mandatory but each finding uses a readable dynamic-height two-row block on A4 portrait. The previous technical ordering in §13.5 is **SUPERSEDED only as to presentation order by D-052**; its data-inclusion requirements remain APPROVED. Detail order is number/title/priority/status, prominent entrepreneur notes when present, problem, recommended solution, effort/estimate, alternatives, scope/sites/assets/risk, evidence, optional technical notes, and resolution. Empty entrepreneur notes render no empty block. Company information omits duplicate display/legal names, country-only `IT` addresses, and empty rows. Evidence title, image, and caption form one DOMPDF-supported page-safe logical block; images preserve proportions, respect A4-safe maximum height, and never overflow or crop.

### D-053 — Application-owned risk-matrix field

The schema-generated `ViewField` and its flat code-keyed matrix state described by D-049 are **SUPERSEDED only as to presentation and form-state shape by D-053**. Risk-profile editing uses `App\Filament\Components\RiskMatrixField`, an application-owned Filament `Field` with its own Blade view. Its primary content is an accessible semantic 4×4 table: consequence rows, likelihood columns, and one independent priority select in each of the sixteen cells. The field is intentionally risk-specific and contains presentation/state-adapter behavior only.

Livewire form state is nested as `matrix.<consequence_id>.<likelihood_id> = <priority_id>`. Persisted integer IDs are the stable identities; a new repeater item may use its stable form UUID only until the existing aggregate transaction persists the level and maps that UUID to the new integer ID. Temporary UUIDs are never stored. Labels, colors, scores, codes, and visual order are not matrix keys. Label, color, enablement, and ordering changes therefore do not silently detach existing associations.

`SaveRiskProfileConfiguration` remains the sole transactional aggregate save path and performs all same-profile, exact-combination, duplicate, unknown-ID, enabled-level, and completeness validation before synchronizing the relational entries. `risk_matrix_entries` remains the only persistent and authoritative matrix contract. The field does not introduce JSON/settings/cache/session persistence, a second save path, automatic recalculation of existing Findings, an external plugin, a JavaScript library, Node.js, Vite, or any new runtime dependency.

### D-054 — Finding workspace responsive modes

The Finding workspace has exactly two intentional responsive modes, determined by the effective inline size of the workspace container:

1. lateral desktop split when the container is at least `64rem` wide;
2. sequential narrow list/detail below `64rem`.

The intermediate mode from D-008 that positioned the inspector absolutely is **SUPERSEDED only as to that responsive behavior by D-054**. Filament Table remains the authoritative Finding list and the complete single-Finding inspector remains the editor. D-054 does not change the domain, persistence, optimistic locking, idempotency, reporting, attachments, or assessment lifecycle decisions in D-008 or elsewhere.

In desktop split mode:

- list and inspector are visible simultaneously;
- the inspector is lateral to the list and can never appear below it;
- the page has no horizontal overflow;
- list and inspector manage their own content without overlap;
- the list has an approximately `32rem` useful minimum and the inspector an approximately `28rem` useful minimum, using zero-minimum grid tracks so intrinsic content cannot force page overflow;
- inspector header and footer remain visible while its body scrolls, and the list scrolls when necessary.

In narrow sequential mode:

- list and detail are deliberate consecutive views, not a fallback;
- with no selected Finding the list is shown;
- selecting a Finding shows its full-width detail without an overlay or list content behind it;
- closing the inspector returns to the list without losing search, filters, pagination, ordering, or scroll position.

No alternative layout, feature flag, duplicated view, absolute overlay, compatibility path, or silent degradation is permitted. If the desktop split cannot be verified in the supported browsers, the implementation is blocked.

### D-055 — Finding workbench

The Finding workspace adopts an application-style presentation composed of:

1. a compact Finding navigator;
2. the main editor for the selected Finding;
3. a contextual properties panel.

The new presentation supersedes only the previous visual list/inspector layout. It does not change the domain, data model, Filament Table as the authoritative list engine, the single Livewire `findingData` state for the selected Finding, signed persistence, optimistic locking, idempotency, reporting, evidence, lifecycle, or read-only behavior.

The first implementation uses Blade, Livewire, Filament components, application CSS, minimal application JavaScript, and the existing Filament asset publication mechanism. It introduces no Node.js, Vite, or compiled Tailwind theme. The frontend-build decision may be reconsidered only after the workbench has been validated in the real application.

On sufficiently wide desktop workspace containers, the interface presents three areas: an approximately `17–19rem` navigator, a flexible and visually dominant central editor, and an approximately `17–19rem` properties panel. Each area may scroll vertically without creating page-level horizontal overflow, and all use the one JavaScript-maintained available-height custom property.

When the workspace container is not sufficiently wide, the interface uses deliberate sequential navigator/editor views. Contextual properties remain accessible from the editor without permanently compressing its main content. This is a UX-validation implementation and is not considered graphically final before user review.

### D-056 — Finding workbench visual hierarchy

The approved workbench palette is Primary Lime `#E1FB15`, Secondary Green `#32D583`, Neutral White `#FFFFFF`, and Neutral Black `#131313`. Dark mode targets an overall visual weight of 65% Neutral Black, 25% Neutral White, 7% Primary Lime, and 3% Secondary Green. Light mode targets 70% Neutral White, 22% Neutral Black, 5% Primary Lime, and 3% Secondary Green. Primary Lime is a rare active/primary accent; Secondary Green is reserved for successful semantic states. Warning, danger, and textual priority colors remain distinct only where they communicate their existing functional meaning.

Each action context has at most one solid Primary Lime button. The selected-Finding editor uses it only for `Salva`; `Salva e passa al successivo`, assessment completion, and export use neutral hierarchy. PDF and XLSX generation are grouped under one visible `Esporta` action without changing either report operation. Blank and template-based Finding creation are grouped under one compact `Nuovo finding` action without changing either creation operation.

The Filament Table remains the authoritative navigator engine. Each Finding row always shows only its progressive number, title clamped to two lines, textual priority, and native secondary-action menu. A non-open status and incomplete state appear only when applicable. Report exclusion uses an accessible compact icon. Category, scope, problem excerpts, solution/evidence counts, repeated `Aperto`, repeated `Completo`, and percentages remain available through search/filter/editor/properties behavior but are not rendered as permanent row metadata. The implementation uses the existing Blade, Livewire, application CSS/JavaScript, and Filament asset publication path; it adds no frontend build, dependency, domain change, or persistence path.

### D-057 — Native hierarchical navigation, Tag removal, scope assets, and proportional verification

The sidebar uses only Filament 5 native navigation and Cluster mechanisms verified against the exact version in `composer.lock`. No custom navigation view, replacement Blade sidebar, navigation plugin, or manual third-level menu is introduced.

The visible hierarchy is:

1. Dashboard;
2. Assessment, containing Assessment;
3. Aziende, a clickable `ClientResource` navigation item whose native child items are Sedi and Asset;
4. Impostazioni, a `SettingsCluster` navigation item.

`SiteResource` is a normal child of Aziende. `AssetCluster` is also a child of Aziende, uses Asset as its navigation label and first real destination, and contains `AssetResource` then `AssetTypeResource`. Its native page sub-navigation is Asset and Tipologie asset. `SettingsCluster` uses Impostazioni as its only main-sidebar entry, redirects to the first real page Generale, and contains, in order, `GeneralSettingsPage`, `ReportSettingsPage`, `FindingTemplateResource`, `CategoryResource`, `RiskProfileResource`, and `EffortLevelResource`. Its native page sub-navigation is Generale, Report, Template, Categorie, Matrice priorità, and Livelli di impegno. Navigation labels and internal page titles use distinct translation keys where their text differs. Clustered resources/pages do not also appear independently in the main sidebar.

The panel keeps resource and page discovery and adds native `discoverClusters()` with the application cluster namespace/directory. Cluster membership may be declared on existing resource/page classes; physical relocation is not required. Cluster URL and route-name prefixes are approved parts of the new structure. Every application link, redirect, test destination, dashboard link, table link, Workspace link, notification link, and Canary target uses a Filament class URL generator or named route rather than a hardcoded admin URL. No legacy URL redirect is added.

Filament `v5.6.8` source proves that `Cluster` inherits navigation parent/group properties and `getNavigationParentItem()`. Because its navigation manager resolves a parent label only among items in the same group, Aziende, Sedi, and `AssetCluster` use the same group. If a future locked version cannot keep `AssetCluster` as a child of the clickable Aziende item through that native mechanism, the navigation part is BLOCKED; no custom-navigation fallback is authorized.

Tag is removed from the v1 runtime and interchange contract. There is no Tag resource, model, action, factory, seeder behavior, Finding/template relation, form state, validation, pivot synchronization, eager loading, template copy, property, filter, badge, translation, snapshot field, DTO field, PDF value, XLSX column, import/export/preview comparison field, JSON fixture field, or JSON Schema property. A forward migration drops `finding_tag`, then `finding_template_tag`, then `tags`; its rollback recreates the schema in dependency-safe order. Existing migrations are not rewritten. JSON Schema remains version 1 with `additionalProperties: false`, so a document containing `tags` is rejected rather than treated as a legacy format.

D-045 remains the single Finding scope rule. `selected_assets` requires at least one same-company Asset and is the only scope that shows and requires the Asset selector. `selected_sites` requires at least one same-company Site and no Asset. `organization` requires no Asset. `network` and `custom` require no Asset and retain their existing description requirement. Workspace save/autosave, completeness, assessment completion, reports, template copy, and the incomplete-Finding filter all delegate to or reproduce exactly this rule.

`scripts/verify.sh` remains the complete authoritative gate without reducing any command, criterion, or receipt behavior. During a single implementation, checks are selected proportionally from the modified PHP files/directories and affected Feature, Canary, schema/migration, and focused Dusk behavior. The complete gate runs at the conclusion of a coherent set of changes, before merge to `main`, at milestone completion, after dependency/runtime/test-infrastructure changes, or on explicit request. An ordinary prompt or intermediate graphical/navigation edit does not implicitly require the benchmark, backup/restore, complete PDF/XLSX suites, full storage audit, or complete Dusk suite. After a failure, the failing command or test is rerun first. Focused success never certifies or replaces the complete gate.

### D-058 — Workspace compact controls and secondary surfaces

The Finding navigator keeps Filament Table search as its only query-refinement control. The previous filter dropdown and saved filter state are removed because the approximately 18rem navigator cannot present that interaction usefully. This supersedes only D-008 and D-056 wording that requires navigator filters; Filament Table remains authoritative, search remains available through an icon that expands the native field on click/focus and stays expanded while a query exists, and completeness, status, priority, report inclusion, category, and scope remain available in rows where exceptional, in the editor/properties, and through domain behavior. Native reorder remains available only for the complete unsearched collection, uses an accessible icon-only trigger in the same toolbar row as search, and must preserve readable Finding titles and deterministic order while active.

`Nuovo finding` remains the aggregated blank/template action and occupies a centered dedicated navigator row. Navigator chrome remains fixed while only the record area scrolls, preventing text from passing beneath a transparent sticky heading or overlapping toolbar controls.

The selected-Finding contextual panel uses the existing single `findingData` state and properties schema. Its status/report, category, scope, risk, and resolution groups become compact collapsible sections so their headings remain discoverable without permanently consuming the narrow panel height. Expanding a lower group brings its newly revealed content into the panel viewport. The properties body remains an independently scrollable zero-minimum grid track as a reliable overflow boundary; no page-level horizontal scroll, duplicated form, or second persistence path is introduced.

The former `Anteprima riepilogo` tab is removed. Its implementation displayed only the company name, effective assessment title, and count of included Findings and was not a report preview, immutable snapshot, validation surface, or domain operation. This supersedes only the four-tab list in §9.4; the Workspace tabs are now Finding, Dettagli assessment, and File Generati. PDF/XLSX generation and immutable history remain unchanged. File Generati retains the D-047 semantic compact table and authenticated actions, with application-owned responsive spacing, explicit context, stable column alignment, and a horizontally contained narrow-viewport fallback.

### D-059 (v1) — Single editorial assessment-report layout

**Status: SUPERSEDED by D-059 on 2026-07-24.** The following text is retained as the historical DOMPDF implementation decision. Its accepted implementation and verification results are not rewritten; its portrait-only, DOMPDF-layout, Canvas-finality, and manually duplicated preview clauses are no longer the target architecture.

The assessment report uses one formally brutalist A4 portrait Blade layout. Structural customization means editing the report Blade files in the repository. Ordinary customization continues to use only the existing report settings, including primary color, logos, branding, title, header/footer text, enabled sections, costs, alternative solutions, and evidence. No theme selector, theme or layout identifier/version, Blade trace field, uploadable template, visual editor, JSON layout configuration, alternate renderer, dependency, model, or application table is introduced.

The layout uses asymmetric composition, prominent section and Finding numbers, strong DejaVu Sans hierarchy, thin horizontal and vertical rules, generous white space, square rectangular blocks, and the configured primary color only for short lines, markers, thin borders, cover details, and structural indicators. Priority colors remain independent functional colors. The report does not use gradients, shadows, rounded cards, crosshairs, decorative concentric circles, asterisks or decorative symbols, non-data icons, extensive colored backgrounds, or graphics without functional meaning.

`resources/views/reports/assessment.blade.php` remains the readable document orchestrator and delegates to a small set of responsibility-focused partials with centralized DOMPDF-compatible CSS. Tables, normal block flow, explicit millimeter/point dimensions, and page-break controls implement the layout; CSS Grid, layout-critical Flexbox, remote fonts/images, JavaScript, and unsupported renderer behavior are not used. D-059 (v1) superseded D-052 only where the then-approved visual hierarchy and section presentation differed. The D-052 data-preservation, dynamic summary height, page-safe evidence, A4 portrait, and no-cropping requirements remained authoritative for that implementation.

The Blade receives and uses only `AssessmentReportData` and its report DTO graph. It performs no database query and automatically generates no assessment content. A section with no data is omitted without invented placeholder copy. The cover resolves consultant and client logos separately by `ReportLogoData::owner`; branding controls their approved placement without changing the logo contract. The internal overview, dynamic priority KPI counts, per-Finding two-row summaries, full untruncated Finding titles, recommended and ordered alternative solutions, classification, separately presented assets, mixed evidence in snapshot order, resolution, methodology, disclaimer, text signature, consultant information, index, priority legend, global attachment list, and the single mandatory VAT-excluded estimate note retain their approved data and setting gates.

The report-settings page contains a small indicative HTML style preview built with the installed native Filament schema `View` component. It reads the same page's unsaved form state, uses local Alpine state only to switch between cover and internal-page examples, and does not generate a PDF, create a `GeneratedReport`, call `BuildAssessmentSnapshot`, create temporary files, use an iframe or polling, or mount a nested Livewire component. On wide screens the form occupies approximately two thirds and the sticky preview one third; on narrow screens the preview follows the form. The preview is explicitly illustrative of style rather than final PDF pagination.

Report confidentiality is removed from the product. The `ReportSettings` property, form field, settings snapshot value, translations, PDF generator input, Canvas rendering, cover/footer rendering, and related tests are removed. A forward Spatie settings migration deletes the persisted `report.confidentiality_label` key. This supersedes the confidentiality entry/default in §8.2, its validation in §8.3, the confidentiality wording in §13.2, the Canvas input in §13.3, and the default label in §13.8. Existing generated files and their immutable stored snapshots are not modified.

Repeated page chrome remains implemented only by `DomPdfCanvasDriver`. After an optional cover, the header uses configured text or `ASSESTME / <azienda>` from the immutable snapshot. The footer uses configured text or, in order, business name, consultant name, and snapshot application name. Thin neutral rules separate header and footer from content; the right footer contains only `Pagina X di Y`, and no center block is rendered. Snapshot hashing, physical files, immutable generated-report history, authoritative authenticated download, versioning, and failure behavior remain unchanged.

### D-059 — Editorial report architecture and renderer spike

**Status: APPROVED — Validation completed and production layout implemented.**

#### Implementation result

The completed Chrome-first proof rejected the official Chrome driver on the blocking A4 MediaBox criterion and accepted WeasyPrint `57.2` through `pontedilana/php-weasyprint` `2.7.0` and Spatie Laravel PDF `2.12.0`. Production now renders the immutable `AssessmentReportData` graph through one `reports.assessment` Blade/CSS implementation shared with the authenticated settings preview. Named CSS pages provide a portrait cover and internal pages, a landscape Finding summary, margin-box header/footer, cover-excluded page numbering, and deterministic one/two-page Finding plans. DOMPDF, Canvas chrome, Chrome, the temporary proof pipeline, and the duplicated preview are removed without changing generated-report persistence, versioning, hashes, history, storage, or download.

#### Verified problem

The report is now an editorial document rather than simple HTML converted to PDF. The current DOMPDF path needs tables and workarounds for ordinary composition, provides fragile page-break control, can leave an indivisible solution on an unbalanced page, cannot reliably mix the required A4 landscape summary with A4 portrait content, and depends on `DomPdfCanvasDriver` for repeated chrome. The Filament preview manually reproduces the visual language instead of rendering the report view. The replacement must keep server-side generation and real selectable/searchable text. Immutable snapshots, versioning, payload/file hashing, generated history, private physical files, and authenticated hash-verified download do not change.

#### Binding constraints

- all software in the report path is free and open source; no SaaS, external cloud renderer, or assessment-data transfer to third parties is permitted;
- generation is server-side and retains `spatie/laravel-pdf` as its abstraction if technically possible;
- PDF text remains real, selectable, and searchable; only genuine supplied images may be raster content;
- one document supports A4 portrait and landscape pages, images, and evidence;
- layout remains application-owned Blade and CSS, with a preview coherent with the real PDF;
- development remains compatible with `docker/compose.dev.yml`, and the selected runtime must have a manageable, documentable CloudPanel installation path;
- additional dependencies are minimized; users receive no free-form HTML, Blade, or CSS editor;
- the immutable assessment/settings snapshot model, generated-report persistence, versioning, hashes, history, and download authorization remain unchanged.

#### Approved report composition

The document order is:

1. simplified A4 portrait cover;
2. optional `Quadro generale`, enabled by default;
3. A4 landscape Finding summary;
4. A4 portrait Finding sheets;
5. evidence;
6. any enabled final sections.

`Quadro generale` reuses the existing `executive_summary`; its interface label becomes `Riepilogo Assessment` without a duplicate database field. The page may be generated when that text is empty. It presents total, open, and resolved Finding KPIs, a compact priority distribution, and a separate priority legend under the editorial title `Quadro generale`.

The landscape Finding summary contains number and title, a deterministic editorial excerpt of the problem, the primary solution, priority, effort, and estimate. Scope and category are removed; status is removed unless a later measured need is approved. The primary solution is the implemented solution when present, otherwise the recommended solution. Full problem and solution content remains in the Finding sheet; excerpts use a deterministic application rule, not renderer-dependent visual clipping.

Each portrait Finding sheet places the number to the left of a title whose sizing follows deterministic length classes. Priority, status, and category use discrete text-labelled badges so color is never the sole signal. Status symbols are: open empty circle, planned calendar, in progress hourglass, resolved check mark, accepted green circle, and not applicable grey circle. `Nota per la direzione` is renamed `Spiegazione del problema`; it adopts the current open editorial treatment of the recommended solution, while the primary solution adopts the current neutral management-note container.

Finding content order is:

1. `Spiegazione del problema`;
2. problem;
3. implemented or recommended primary solution;
4. other solutions;
5. risk assessment;
6. affected systems and assets;
7. optional technical notes;
8. optional resolution.

A Finding has at most three solutions and always has a recommended solution; it may also have an implemented solution, and both references may identify the same solution. If they differ, both labels are explicit, and the implemented solution receives visual priority for a resolved Finding. Every solution always shows title, description, effort, and estimate. A solution block is indivisible and never starts on one page and continues on the next. A continued Finding page repeats a compact Finding heading. One Finding occupies at most two pages excluding evidence.

Those pagination rules require hard text limits. D-059 does not invent character counts before the renderer and final layout are proven. The spike must identify realistic budgets proportional to the number of solutions; the later implementation must show remaining-character counters and enforce the same limits server-side.

Risk uses a real dynamic mini-matrix derived from the configured risk profile and shows consequence, likelihood, and resulting priority with textual labels. A compact `Sistemi interessati` block follows where useful. Each asset preferably occupies one compact row containing name, site, manufacturer, and IP. The free-form scope description remains data but is not mandatory PDF content.

The typed `Mostra risoluzione` report setting defaults to enabled and is captured through the existing settings snapshot shape; it creates no empty `Risoluzione` block. Evidence remains on dedicated pages. Automatic placement of small images in residual page space is deferred and is not promised by this decision.

The cover removes section-number-like elements and repeated application naming. Internal headers are minimal; the footer contains only the page number, and the cover is excluded from numbering.

#### Alternatives

- **DOMPDF — rejected former production renderer.** It is pure PHP, integrated, and operationally simple. Its documented CSS 2.1/partial CSS 3 limits, non-repeating native HTML header/footer behavior, Canvas dependency, fragile pagination, duplicated preview, and unproven mixed orientation made it unsuitable as the permanent target ([Spatie DOMPDF driver](https://spatie.be/docs/laravel-pdf/v2/drivers/using-the-dompdf-driver)).
- **Spatie Chrome driver — rejected primary spike candidate.** It drives local Chrome/Chromium directly from PHP through `chrome-php/chrome`, without Node.js, npm, Puppeteer, or a permanent service ([Spatie Chrome driver](https://spatie.be/docs/laravel-pdf/v2/drivers/using-the-chrome-driver), [chrome-php/chrome](https://github.com/chrome-php/chrome)). The official integration failed the blocking mixed-A4 MediaBox criterion because it did not request CSS-preferred page sizes ([Chrome DevTools `printToPDF`](https://chromedevtools.github.io/devtools-protocol/tot/Page/#method-printToPDF)).
- **WeasyPrint — selected production renderer.** It is a free BSD Python pagination engine with CSS Paged Media, named pages, page size/orientation, margin boxes, running elements, and page-break control ([WeasyPrint](https://doc.courtbouillon.org/weasyprint/stable/), [Spatie WeasyPrint driver](https://spatie.be/docs/laravel-pdf/v2/drivers/using-the-weasyprint-driver)). The installed binary and native/font libraries passed the bounded and definitive report acceptance.
- **Gotenberg — operational fallback only.** It is an open-source Docker API backed by headless Chromium, considered only if local Chromium passes document requirements but is unstable or unmanageable. It adds a permanent service to development and deployment ([Gotenberg](https://gotenberg.dev/docs/getting-started/introduction), [Spatie Gotenberg driver](https://spatie.be/docs/laravel-pdf/v2/drivers/using-the-gotenberg-driver)).
- **Html2Media — not selected.** Upstream code runs in the browser, renders DOM with `html2canvas`, slices canvases, and inserts PNG images into jsPDF under one global orientation; the resulting report is principally rasterized and is incompatible with searchable professional text and immutable server-side generation ([Html2Media source](https://github.com/torgodly/Html2Media/blob/main/resources/js/html2media-main.js)). Its preview/print/download interaction may inform UX only.

Browsershot is excluded because it adds Node.js, Puppeteer, and Chromium when direct PHP-to-Chromium may suffice ([Browsershot requirements](https://github.com/spatie/browsershot/blob/main/docs/requirements.md)). Paged.js and Vivliostyle add a more complex JavaScript/Node editorial pipeline than required ([Paged.js](https://pagedjs.org/en/documentation/), [Vivliostyle CLI](https://docs.vivliostyle.org/en/cli/getting-started/)). Typst would rewrite the template outside Blade ([Typst](https://typst.app/docs/)). wkhtmltopdf is not suitable as a new dependency because its upstream repository is archived ([wkhtmltopdf](https://github.com/wkhtmltopdf/wkhtmltopdf)). SaaS renderers are excluded by privacy and external-dependency constraints.

#### Completed bounded spike

The spike is small, reversible, and does not redesign the complete production report. It uses one deterministic snapshot inspired by the VIP Estintori report and the existing immutable DTO graph. It contains a portrait cover, portrait `Quadro generale`, landscape summary, four portrait Findings, one Finding with three solutions, an implemented solution different from the recommended solution, an indivisible solution, a continued-page Finding heading, a dynamic risk mini-matrix, assets, one image evidence item, a page-number-only footer, and a preview of the same view.

Chrome passes only if one PDF proves all of the following:

- A4 portrait and landscape in the same file;
- selectable and searchable text, with no rasterized content except real images;
- no overlap, truncation, or split solution;
- correct continued-Finding heading and evidence;
- unchanged snapshot, hashing, versioning, history, private storage, and authenticated-download behavior;
- operation in the Docker development profile and a documentable CloudPanel installation path.

Failure of any item is blocking for Chrome. Only then may the same bounded proof be attempted with WeasyPrint. Gotenberg is considered only for a proven operational problem with local Chromium. The spike selects one production renderer; it does not build parallel production implementations.

#### Implemented preview

The duplicated illustrative thumbnail is removed. An authenticated preview route renders the same Blade view and CSS as the PDF in an iframe; `@media screen` represents paged sheets and `@media print` controls PDF output. Unsaved report settings refresh the preview with debounce through a short-lived token bound to the authenticated user and session. The preview creates no `GeneratedReport`, immutable file, version, history entry, report-directory write, or persisted setting. No user-editable HTML, Blade, or CSS is introduced.

#### Supersession scope

D-059 supersedes D-052 and D-059 (v1) for current composition and supersedes the portrait-only, fixed-summary, unlimited-Finding-pagination, Canvas-finality, and duplicated-preview clauses in §§8, 13, 20, and review items B-02/B-12. D-009 supersedes permanent DOMPDF exclusivity. D-007 and D-042 describe the current WeasyPrint runtime without application Chromium. Historical implementation and test evidence remains factual.

### D-060 — Authoritative WeasyPrint settings preview

The authenticated report-settings preview uses the same application-owned rendering service, `reports.assessment` Blade view, CSS, immutable DTO shape, and explicitly selected Spatie WeasyPrint driver as production PDF generation. This supersedes only D-059's screen-media HTML rendering for the settings iframe. The representative preview data remains side-effect-free, but the iframe response is a real transient PDF with `no-store` response controls. Preview rendering creates no `GeneratedReport`, immutable snapshot, version, history entry, report-directory file, persisted setting, or alternate renderer.

Every visible PDF setting must either affect both the transient preview PDF and the production PDF through their shared DTO/view or be removed from the Report page. A visible operational or XLSX-only control may remain only when its Italian helper text states that exact non-preview scope. Persisted legacy General setting keys are not deleted or reinterpreted merely because obsolete, duplicated, or DOMPDF-era controls are removed from this page.

The preview may expand temporarily over the application viewport through one application-owned control with an explicit visible exit and Escape-key support. The expansion adds no browser-rendering library, frontend build, dependency, second report view, or persistence path.

### D-028 — Authentication and MFA

TOTP MFA is optional and disabled until the administrator enables it.

When enabled:

- generate eight single-use recovery codes;
- require the user to confirm storage of the codes;
- provide `php artisan assestme:reset-mfa`;
- do not use email recovery.

### D-029 — Backups

Implement application-aware local backups with a restore test. Off-host copying and encrypted storage are operational responsibilities outside v1.

### D-030 — Dependency locking

`composer.lock` must be committed.

The plan specifies package constraints; the exact versions resolved in Milestone 0 become immutable until a reviewed dependency-update change.

### D-031 — Community packages

Inspect licenses, Composer metadata, source, tests, and advisories before acceptance.

Do not edit `vendor/`. Do not fork without explicit approval.

### D-032 — Time

Store timestamps in UTC. Render them in `Europe/Rome`.

`assessment_date` is a date-only value and is not converted across time zones.

### D-033 — Identifiers

Use auto-incrementing integer primary keys.

Use UUID strings only for:

- idempotency request keys;
- deletion operation IDs;
- generated correlation IDs.

### D-034 — Historical data

No audit log or editable history is required.

Generated report snapshots provide immutable delivery history.

### D-035 — Future integrations

Keep business logic in application actions/services so future integrations can call it, but do not create speculative API contracts or adapters in v1.

### D-036 — Univer Sheet

Do not install `yuichi-okada/filament-univer`. Its single JSON spreadsheet document is incompatible with the relational finding/solution/asset/evidence model and would create a second source of truth.

### D-037 — Auxiliary table plugins

Install Right Click and Advanced Table Export only after standard Filament resources exist.

- Right Click duplicates visible accessible actions on standard resource tables; it is never the only interaction path and is not integrated into the assessment workspace.
- Advanced Table Export serves generic client/template/asset/assessment lists. It does not generate the assessment workbook, which uses the dedicated PhpSpreadsheet exporter.

## 3. Authoritative external references

Use primary documentation and the installed-version Laravel Boost documentation search. URLs are implementation references, not permission to change signed decisions.

### Platform

- Laravel 13: https://laravel.com/docs/13.x
- Laravel deployment requirements: https://laravel.com/docs/13.x/deployment
- Laravel database: https://laravel.com/docs/13.x/database
- Laravel cache locks: https://laravel.com/docs/13.x/cache
- Laravel sessions: https://laravel.com/docs/13.x/session
- Laravel localization: https://laravel.com/docs/13.x/localization
- Laravel filesystem: https://laravel.com/docs/13.x/filesystem
- Laravel validation: https://laravel.com/docs/13.x/validation
- Laravel testing: https://laravel.com/docs/13.x/testing
- Laravel Dusk: https://laravel.com/docs/13.x/dusk
- Laravel Boost: https://laravel.com/docs/13.x/boost
- Laravel Pint: https://laravel.com/docs/13.x/pint
- SQLite WAL: https://sqlite.org/wal.html
- SQLite PRAGMAs: https://sqlite.org/pragma.html
- Nginx documentation: https://nginx.org/en/docs/
- Certbot Nginx instructions: https://certbot.eff.org/instructions

### Filament

- Filament 5: https://filamentphp.com/docs/5.x
- Installation: https://filamentphp.com/docs/5.x/introduction/installation
- Resources: https://filamentphp.com/docs/5.x/resources/overview
- Custom pages: https://filamentphp.com/docs/5.x/navigation/custom-pages
- Schemas/forms: https://filamentphp.com/docs/5.x/schemas/overview
- Repeater: https://filamentphp.com/docs/5.x/forms/repeater
- Textarea: https://filamentphp.com/docs/5.x/forms/textarea
- File upload: https://filamentphp.com/docs/5.x/forms/file-upload
- Actions: https://filamentphp.com/docs/5.x/actions/overview
- Tables: https://filamentphp.com/docs/5.x/tables/overview
- Security: https://filamentphp.com/docs/5.x/advanced/security
- MFA: https://filamentphp.com/docs/5.x/users/multi-factor-authentication
- Testing: https://filamentphp.com/docs/5.x/testing/overview

### Selected packages

- Canary: https://filamentphp.com/plugins/bas-van-dinther-canary
- Right Click: https://filamentphp.com/plugins/leek-right-click
- Advanced Table Export: https://filamentphp.com/plugins/igor-clauss-advanced-table-export
- Spatie settings: https://filamentphp.com/plugins/filament-spatie-settings
- Spatie Laravel PDF: https://spatie.be/docs/laravel-pdf
- Chrome driver: https://spatie.be/docs/laravel-pdf/v2/drivers/using-the-chrome-driver
- Chrome PHP: https://github.com/chrome-php/chrome
- Chrome DevTools `Page.printToPDF`: https://chromedevtools.github.io/devtools-protocol/tot/Page/#method-printToPDF
- WeasyPrint driver: https://spatie.be/docs/laravel-pdf/v2/drivers/using-the-weasyprint-driver
- WeasyPrint: https://doc.courtbouillon.org/weasyprint/stable/
- Gotenberg driver: https://spatie.be/docs/laravel-pdf/v2/drivers/using-the-gotenberg-driver
- Gotenberg: https://gotenberg.dev/docs/getting-started/introduction
- DOMPDF driver: https://spatie.be/docs/laravel-pdf/v2/drivers/using-the-dompdf-driver
- DOMPDF: https://github.com/dompdf/dompdf
- Html2Media: https://github.com/torgodly/Html2Media
- PhpSpreadsheet: https://github.com/PHPOffice/PhpSpreadsheet
- PDF text test parser: https://github.com/smalot/pdfparser
- Opis JSON Schema: https://opis.io/json-schema/
- Larastan: https://github.com/larastan/larastan
- Pest: https://pestphp.com/docs

### Agentic development and standards

- Codex `AGENTS.md`: https://developers.openai.com/codex/guides/agents-md
- OpenAI ExecPlans: https://developers.openai.com/codex/guides/exec-plans
- PSR-12: https://www.php-fig.org/psr/psr-12/
- PER Coding Style: https://www.php-fig.org/per/coding-style/

## 4. Required project files

The implementation must create and commit:

```text
AGENTS.md
plan.md
.gitignore
.env.example
composer.json
composer.lock
phpstan.neon
pint.json
schemas/finding-template.schema.json
templates/base-findings.it.json
resources/lang/it/
resources/views/reports/assessment.blade.php
resources/views/reports/partials/
tests/
tests/Browser/
.github/workflows/quality.yml
scripts/preflight.sh
scripts/bootstrap-local.sh
scripts/verify.sh
docker/compose.dev.yml
docker/dev/Dockerfile
docker/dev/php.ini
docker/dev/entrypoint.sh
scripts/deploy-production.sh
stubs/nginx/assestme.conf
stubs/php/assestme.ini
stubs/cron/assestme
fixtures/imports/
fixtures/reports/
fixtures/evidence/
```

Rules:

- do not create additional product, architecture, or feature Markdown files;
- shell scripts and configuration stubs are allowed and must be executable or directly usable;
- `plan.md` is the only product/architecture document;
- implementation comments document non-obvious code behavior;
- screenshots from manual QA go under `storage/app/qa-artifacts` and are not committed.

## 5. Installation, development, and production environment

### 5.1 Runtime provisioning

The supported development host provides Docker, Docker Compose, and the repository only. The project-owned image provides PHP 8.3 CLI, every D-004 extension, Composer 2, and the standard shell/file utilities used by maintained development scripts. PHP, Composer, PHP extensions, Chrome, and ChromeDriver are not installed directly on the workstation as part of the supported profile.

The application runtime does not require the standalone SQLite CLI. SQLite access is provided by PHP's `pdo_sqlite` extension. Production runtime provisioning is reserved for the future CloudPanel decision and implementation.

### 5.2 Preflight

The Docker entrypoint runs `scripts/preflight.sh` inside `app`. It must verify:

```bash
php -v
php -m
php --ini
composer --version
```

Required:

- PHP `8.3.x`;
- every required extension from D-004;
- Composer 2;
- Laravel project markers `artisan`, `composer.json`, and `composer.lock`;
- writable project directory.

Preflight does not inspect or reject the operating-system distribution, version, or CPU architecture. Any failure exits non-zero and states the missing capability.

### 5.3 Project creation

The repository is already the application source. Supported development installation is:

```bash
docker compose -f docker/compose.dev.yml up --build
```

The entrypoint performs the environment-neutral bootstrap against the bind-mounted repository. The following Composer constraints record the approved dependency inventory and are not a direct-host installation procedure.

Runtime dependencies:

```bash
composer require filament/filament:"^5.0" -W
composer require filament/spatie-laravel-settings-plugin:"^5.0" -W
composer require spatie/laravel-pdf:"^2.0"
composer require dompdf/dompdf:"^3.0"
composer require phpoffice/phpspreadsheet:"^5.0"
composer require opis/json-schema:"^2.6"
composer require leek/filament-right-click
composer require occtherapist/advanced-table-export-for-filament
```

Development dependencies:

```bash
composer require laravel/boost --dev
composer require baspa/filament-canary --dev
composer require laravel/dusk --dev
composer require pestphp/pest pestphp/pest-plugin-laravel --dev -W
composer require larastan/larastan --dev
composer require smalot/pdfparser:"^2.12" --dev
```

Initial framework scaffolding provenance (already completed in the repository, not an installation procedure):

```bash
php artisan filament:install --panels
php artisan boost:install
php artisan canary:install
php artisan dusk:install
php artisan pest:install
php artisan filament:assets
```

After Filament worked without application-built assets, the unused Laravel starter frontend files were removed:

```text
package.json
package-lock.json
vite.config.js
resources/css/app.css
resources/js/app.js
```

Do not remove a file required by an installed package. Verify with a clean install and browser smoke test.

### 5.4 Dependency acceptance

Milestone 0 must record in `plan.md`:

- exact versions from `composer.lock`;
- package license;
- result of `composer audit`;
- source inspection result for every community runtime plugin;
- whether the plugin publishes assets or views;
- whether vendor publishing is required;
- known maintenance/security concerns.

After acceptance, the entrypoint runs the equivalent of the following inside `app` only when `vendor` is absent or `composer.lock` changed:

```bash
composer install
```

Do not run an unreviewed broad `composer update`.

### 5.5 Environment

The entrypoint creates `.env` from the neutral `.env.example` and creates `database/database.sqlite` only when each is missing. The neutral application file uses portable placeholders:

```dotenv
APP_NAME=AssestMe
APP_ENV=local
APP_DEBUG=true
APP_URL=http://127.0.0.1:8000
APP_LOCALE=it
APP_FALLBACK_LOCALE=it
APP_TIMEZONE=Europe/Rome

DB_CONNECTION=sqlite
DB_DATABASE=/absolute/path/to/AssestMe/database/database.sqlite
DB_FOREIGN_KEYS=true
DB_BUSY_TIMEOUT=5000
DB_JOURNAL_MODE=WAL
DB_SYNCHRONOUS=NORMAL
DB_TRANSACTION_MODE=IMMEDIATE

CACHE_STORE=file
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
QUEUE_CONNECTION=sync

FILESYSTEM_DISK=local
LARAVEL_PDF_DRIVER=weasyprint
LARAVEL_PDF_WEASYPRINT_BINARY=/usr/bin/weasyprint

DEV_ADMIN_NAME=Administrator
DEV_ADMIN_EMAIL=admin@assestme.local
DEV_ADMIN_PASSWORD=
```

`LARAVEL_PDF_DRIVER=weasyprint` records the selected production/development renderer. The binary path stays configurable for equivalent supported environments.

Compose overrides the environment at process level without writing Docker-only paths into `.env`:

```dotenv
APP_URL=http://127.0.0.1:8000
DB_DATABASE=/workspace/database/database.sqlite
LARAVEL_PDF_DRIVER=weasyprint
LARAVEL_PDF_WEASYPRINT_BINARY=/usr/bin/weasyprint
ASSESTME_BACKUP_ROOT=/workspace/backups
```

No `.env.docker` file is used and no secret is stored in Compose. Production environment differences are reserved for the future CloudPanel implementation.

### 5.6 Administrator bootstrap

Create:

```text
php artisan assestme:create-admin
```

Behavior:

- if no user exists, prompt for name/email/password and create it;
- if a user exists, exit non-zero unless `--replace` is supplied;
- `--replace` requires current password or interactive confirmation from the console;
- enforce password policy;
- never create a second user;
- never print a production password;
- local bootstrap may generate and print a random password once;
- the command is intentionally not idempotent: without `--replace`, an existing administrator is an explicit non-zero error.

`migrate:fresh --seed` seeds domain data only. It never creates an administrator.

Tests create their administrator through factories.

The environment-neutral bootstrap queries the singleton first and invokes this command only when no administrator exists. A later container start preserves existing credentials.

### 5.7 Local development

Start the supported profile:

```bash
docker compose -f docker/compose.dev.yml up --build
```

Required URL:

```text
http://127.0.0.1:8000/admin
```

Authorized local-network URL:

```text
http://<development-host-ip>:8000/admin
```

Completion evidence:

- HTTP 200 login page;
- successful real login;
- Dusk smoke test;
- authenticated dashboard screenshot stored as an untracked QA artifact.

### 5.8 Production layout

The approved production destination is a dedicated PHP site managed by CloudPanel. Its layout is not defined or implemented in this activity. A future decision must specify the CloudPanel document root, `.env`, SQLite location, ownership, permissions, scheduler, backups, deployment, update, and rollback before production installation is supported operationally.

The former generic `releases/shared/current` layout is superseded and is not an official installation profile.

### 5.9 Nginx and PHP-FPM

CloudPanel must provide PHP-FPM and the web-server integration in the future production implementation. The existing generic Nginx and PHP stubs are legacy/deprecated regression artifacts only and must not be installed or partially adapted as CloudPanel configuration in this activity.

PHP production limits:

```ini
memory_limit = 512M
upload_max_filesize = 25M
post_max_size = 30M
max_file_uploads = 20
max_execution_time = 120
```

### 5.10 TLS

TLS and hostname handling belong to the future CloudPanel production decision. No CloudPanel or generic-host TLS procedure is implemented by this activity. Local Docker development uses unencrypted HTTP on all host IPv4 interfaces; access control and any required network restriction remain the responsibility of the development host firewall. This exposure does not make the Docker profile suitable for production.

### 5.11 Deployment and rollback

CloudPanel deployment, update, and rollback are not implemented in this activity. `scripts/deploy-production.sh`, `scripts/rollback-production.sh`, and `scripts/rehearse-production.sh` implement the superseded generic-host model and remain legacy/deprecated internal artifacts until a future CloudPanel decision determines whether they are removed or replaced. Technical executability and regression tests do not make them a supported production profile.

### 5.12 Backups

Implement:

```text
php artisan assestme:backup [--output=/absolute/archive.tar.gz]
php artisan assestme:backup:verify <archive>
php artisan assestme:restore-backup <archive>
```

Backup content:

- consistent SQLite backup created using SQLite's backup/VACUUM mechanism after WAL checkpoint;
- private storage;
- generated reports;
- settings and application version metadata;
- manifest with SHA-256 per file.

Schedule daily at 02:30 Europe/Rome through Laravel Scheduler. Registration of the scheduler with CloudPanel belongs to the future production implementation; the existing generic cron stub is legacy/deprecated.

Retention:

- 7 daily;
- 4 weekly;
- 6 monthly.

Backups are stored under the configured absolute `ASSESTME_BACKUP_ROOT` with mode `600`.

Off-host copying and encrypted-volume protection are required operational controls but are outside application code.

Restore:

- requires maintenance mode;
- verifies manifest hashes;
- creates a safety backup before replacement;
- restores database and storage together;
- runs diagnostics;
- exits non-zero on any mismatch.

Run an automated restore test against temporary paths in CI. A CloudPanel production restore test and its operational cadence belong to the future production implementation.

## 6. Architecture and coding method

### 6.1 Architecture

Use a conventional Laravel application with thin Filament resources/pages and explicit application actions.

```text
app/
  Actions/
    Assessments/
    Authentication/
    Evidence/
    Findings/
    Reports/
    Templates/
  Data/
    Assessments/
    Imports/
    Reports/
  Enums/
  Exceptions/
  Filament/
    Components/
    Pages/
    Resources/
    Widgets/
  Models/
  Policies/
  Rules/
  Services/
    Export/
    Import/
    Reporting/
    Storage/
  Settings/
  Support/
```

Do not introduce:

- generic repositories over Eloquent;
- CQRS;
- microservices;
- domain-event infrastructure for ordinary CRUD;
- one-implementation interfaces;
- speculative modules/packages;
- an application API in v1.

### 6.2 SOLID

Apply SOLID pragmatically:

- one action owns one business operation;
- Filament orchestrates but does not own domain rules;
- introduce an interface only for an actual multiple implementation or external boundary;
- prefer composition;
- no inheritance hierarchy without substitutable implementations;
- keep DTOs immutable where practical.

### 6.3 DRY

One authoritative implementation must exist for:

- priority calculation;
- priority override;
- estimate validation/formatting;
- scope formatting;
- template snapshot copying;
- autosave/explicit-save persistence;
- state transitions;
- import normalization and update;
- evidence storage;
- deletion staging/recovery;
- report snapshot building;
- PDF/XLSX display formatting.

Do not create generic helper classes that hide business meaning.

### 6.4 Required actions

Implement typed invokable actions:

```text
CreateAssessment
CompleteAssessment
ReopenAssessment
ArchiveAssessment
CreateBlankFinding
CopyTemplateToAssessment
DuplicateFinding
ReorderFindings
SaveAssessmentPatch
SaveAssessmentWorkspace
CalculateFindingPriority
OverrideFindingPriority
SetRecommendedSolution
SetImplementedSolution
TransitionFindingStatus
StoreEvidence
DeleteEvidence
DeleteEntityAccordingToPolicy
ImportFindingTemplates
ExportFindingTemplates
BuildAssessmentSnapshot
GenerateAssessmentPdf
GenerateAssessmentWorkbook
CreateBackup
RestoreBackup
```

### 6.5 Transactions and locks

Use transactions for all aggregate changes.

Use:

- `Cache::lock("assessment:{id}:save", 10)` with `block(5)`;
- assessment optimistic version checks inside the transaction;
- unique request UUID for idempotency.

Filesystem operations use staging/compensation, not claims of SQL atomicity.

### 6.6 Types

Every new PHP file:

```php
declare(strict_types=1);
```

Requirements:

- typed properties/parameters/returns;
- backed enums;
- readonly DTOs where practical;
- no dynamic properties;
- no untyped public arrays;
- array shapes for internal arrays when DTOs are excessive;
- no PHPStan baseline;
- no ignored errors without written approval.

### 6.7 Comments

Comments and docblocks are English.

Comment only:

- non-obvious business rules;
- transaction/locking reasons;
- framework overrides;
- DOMPDF constraints;
- security-sensitive decisions;
- compensation/recovery behavior.

Do not narrate syntax.

### 6.8 Code style and quality

Use Pint, Larastan level 6, Pest, Dusk, Canary, and Composer audit.

Composer scripts:

```json
{
  "scripts": {
    "quality": [
      "@php vendor/bin/pint --test",
      "@php vendor/bin/phpstan analyse --memory-limit=1G",
      "@php artisan test",
      "@php artisan canary:check --strict",
      "@composer audit --locked"
    ],
    "browser": [
      "@php artisan dusk"
    ],
    "fix": [
      "@php vendor/bin/pint"
    ],
    "serve": [
      "@php artisan serve --host=127.0.0.1 --port=8000"
    ]
  }
}
```

If the installed Canary command differs, update the script to its documented strict zero-skip equivalent and record the exact command in Discoveries.

## 7. Data model and domain contracts

### 7.1 Global database conventions

- integer auto-increment primary keys;
- timestamps stored UTC;
- foreign-key indexes on every relation;
- `restrictOnDelete()` for referenced configuration/master data;
- `cascadeOnDelete()` only for true owned children during permanent deletion;
- soft deletes on archivables;
- all long text fields validate to at most 20,000 Unicode characters;
- money uses `decimal(12,2)`;
- currency uses uppercase ISO 4217 `char(3)`;
- URLs max 2048 characters;
- emails max 254 characters;
- slugs/external IDs max 160 characters;
- titles/names max 255 characters;
- no polymorphic relations in v1.

Unique slugs remain reserved after soft deletion. A new collision receives `-2`, `-3`, etc. Restoring the original record does not collide.

### 7.2 Users

`users`:

| Column | Type | Rules |
|---|---|---|
| `id` | bigint PK | |
| `singleton_key` | tiny integer | fixed `1`, unique |
| `name` | varchar(120) | required |
| `email` | varchar(254) | required, normalized lowercase, unique |
| `password` | varchar(255) | required |
| `app_authentication_secret` | text nullable | encrypted cast |
| `app_authentication_recovery_codes` | text nullable | encrypted cast |
| `mfa_enabled_at` | timestamp nullable | UTC |
| `remember_token` | varchar(100) nullable | |
| timestamps | | |

No email verification and no password-reset email.

Password policy:

- minimum 14 characters;
- uppercase and lowercase;
- number;
- symbol;
- maximum 128 characters.

### 7.3 Clients

`clients`:

- `legal_name varchar(255)` required;
- `trade_name varchar(255)` nullable;
- `vat_number varchar(32)` nullable;
- `tax_code varchar(32)` nullable;
- `email varchar(254)` nullable;
- `phone varchar(40)` nullable;
- `website varchar(2048)` nullable HTTP/HTTPS;
- address fields: `varchar(255)`, postal code `varchar(20)`, province `varchar(100)`, country `char(2)` default `IT`;
- `logo_path varchar(1024)` nullable;
- `internal_notes text` nullable;
- timestamps and soft deletes.

VAT/tax code are normalized uppercase without spaces but are not globally unique. Duplicate values trigger a warning, not a hard error.

### 7.4 Sites

`sites` belongs to a client.

- `client_id` required;
- `name varchar(255)` required;
- address fields as clients;
- `description text` nullable;
- `notes text` nullable;
- timestamps and soft deletes.

An asset may have no site. Site movement history is outside v1.

### 7.5 Asset types

`asset_types`:

- `name varchar(120)` required;
- `slug varchar(160)` unique;
- `description text` nullable;
- `sort_order unsignedInteger`;
- `is_enabled boolean`;
- timestamps.

Seed exactly, in this order:

1. NAS
2. Server
3. Firewall
4. Router
5. Switch
6. Access Point
7. PBX
8. Telefono
9. Postazione di Lavoro
10. Stampante
11. UPS
12. Armadio Rack
13. Altro

### 7.6 Assets

`assets`:

- `client_id` required;
- `site_id` nullable;
- `asset_type_id` required;
- `name varchar(255)` nullable;
- `manufacturer varchar(120)` nullable;
- `model varchar(160)` nullable;
- `hostname varchar(253)` nullable;
- `ip_address varchar(45)` nullable;
- `mac_address char(17)` nullable;
- `serial_number varchar(160)` nullable;
- `description text` nullable;
- `notes text` nullable;
- timestamps and soft deletes.

At least one identifying value is required: name, model, hostname, IP, MAC, or serial.

MAC is normalized uppercase colon-separated. Asset-specific custom JSON fields and interface history are outside v1.

### 7.7 Categories

`categories`:

- name, slug, description;
- `color char(7)` nullable validated `#[0-9A-Fa-f]{6}`;
- sort order;
- enabled;
- soft deletes.

Each finding/template has exactly one category.

Seed exactly, in this order:

1. Governance IT
2. Sicurezza
3. Rete
4. Cablaggio e Infrastruttura Fisica
5. Server
6. NAS e Storage
7. Backup
8. Endpoint
9. Identità e Accessi
10. Cloud e Microsoft 365
11. Posta Elettronica
12. VoIP
13. Videosorveglianza
14. Continuità Operativa
15. Monitoraggio
16. Documentazione
17. Licenze e Conformità
18. Altro

Archived category records remain visible on existing findings and cannot be attached to new records.

### 7.8 Risk configuration

Tables:

- `risk_profiles`;
- `consequence_levels`;
- `likelihood_levels`;
- `priority_levels`;
- `risk_matrix_entries`.

`risk_profiles` contains:

- `code varchar(40)` unique;
- `label varchar(120)`;
- `description text` nullable;
- `is_default boolean`;
- `is_enabled boolean`, not `is_active`;
- timestamps.

Exactly one risk profile is default. Replacing the default is one transaction. The default profile cannot be disabled until another enabled profile becomes default.

`consequence_levels` and `likelihood_levels` each contain:

- risk profile FK with restricted deletion;
- `code varchar(40)`, unique inside the profile;
- `label varchar(120)`;
- `description text` nullable;
- integer `score` from 1 through 4, unique inside the profile;
- strict uppercase `#RRGGBB` color;
- integer `sort_order`;
- `is_enabled boolean`;
- timestamps.

`priority_levels` contains:

- risk profile FK with restricted deletion;
- `code varchar(40)`, unique inside the profile;
- `label varchar(120)`;
- `description text` nullable;
- strict uppercase `#RRGGBB` color;
- integer `sort_order`;
- `is_enabled boolean`;
- timestamps.

`risk_matrix_entries` contains risk profile, consequence, likelihood, and priority FKs, all with restricted deletion, plus timestamps. The three referenced levels must belong to the same profile as the entry. The profile/consequence/likelihood tuple is unique.

`GeneralSettings.active_risk_profile_id` is the only source of truth for new calculations.

Default levels:

Consequence:

1. limited / Limitata
2. significant / Significativa
3. serious / Seria
4. critical / Critica

Likelihood:

1. unlikely / Improbabile
2. possible / Possibile
3. likely / Probabile
4. current / Attuale o imminente

Priority:

1. low / Bassa
2. moderate / Moderata
3. high / Alta
4. critical / Critica

Default matrix:

| Consequence \ Likelihood | Unlikely | Possible | Likely | Current |
|---|---:|---:|---:|---:|
| Limited | Low | Low | Moderate | Moderate |
| Significant | Low | Moderate | Moderate | High |
| Serious | Moderate | High | High | Critical |
| Critical | High | High | Critical | Critical |

Constraints:

- one matrix entry per profile/consequence/likelihood tuple;
- exactly 16 entries for an enabled 4×4 profile;
- referenced profiles and levels cannot be deleted and are disabled instead;
- editing a matrix does not automatically recalculate existing findings;
- recalculation occurs when consequence/likelihood changes or the user invokes recalculation;
- current UI uses live labels/colors;
- generated reports store snapshot labels/colors.

### 7.9 Effort levels

`effort_levels`:

- `code varchar(40)` unique;
- `label varchar(120)`;
- `description text` nullable;
- strict uppercase `#RRGGBB` color;
- integer `sort_order`;
- `is_enabled boolean`;
- timestamps;
- no soft delete after use.

Effort levels are global and independent from risk profiles. Seed low, moderate, high, very_high.

Effort belongs to a solution.

### 7.10 Finding templates

`finding_templates`:

- `external_id varchar(160)` required unique lowercase;
- title;
- category;
- problem required;
- entrepreneur notes nullable;
- technical notes nullable;
- default scope type/description;
- default consequence, likelihood, and priority references nullable;
- priority rationale nullable;
- enabled;
- timestamps and soft deletes.

A template must have one or more solutions and exactly one recommended solution.

### 7.11 Template solutions

`finding_template_solutions`:

- template FK;
- `external_id varchar(160)` required;
- title and description required;
- comparison notes;
- effort and effort notes;
- estimate fields;
- recommended boolean;
- sort order;
- timestamps and soft deletes.

Unique:

```text
(finding_template_id, external_id)
```

Exactly one non-deleted solution is recommended.

### 7.12 Assessments

`assessments`:

- client FK;
- title required;
- `report_title_override varchar(255)` nullable;
- `assessment_date date`;
- scope type;
- scope description;
- status;
- introduction;
- executive summary;
- methodology notes;
- locale fixed `it` in v1;
- `lock_version unsignedInteger default 0`;
- completed timestamp nullable;
- timestamps and soft deletes.

Assessment creation scope is deliberately narrower than finding scope: the UI and authoritative create action accept only `organization`, `selected_sites`, and `custom`. `selected_sites` requires one or more `assessment_site` rows owned by the assessment company; `custom` requires `scope_description`. The initial visible title proposal is generated through the D-044 application rule and remains user-editable without adding persistence state beyond the existing `title` column.

Pivot `assessment_site`.

State machine:

| From | To | Required behavior |
|---|---|---|
| draft | completed | run completion validation; set `completed_at`; make read-only |
| completed | draft | explicit reopen confirmation; clear `completed_at`; increment version |
| draft | archived | confirmation; read-only |
| completed | archived | confirmation; preserve completed timestamp |
| archived | draft | explicit restore/reopen; clear completed timestamp |
| archived | completed | not allowed directly |

Generating a report does not automatically complete the assessment unless `freeze_after_generation` is enabled. In that setting, a successful final PDF generation performs the `draft -> completed` transition in the same application operation.

### 7.13 Findings

`findings`:

- assessment FK;
- source template FK nullable;
- title, category, problem, entrepreneur notes, technical notes;
- scope fields;
- risk fields;
- status;
- recommended/implemented solution FKs nullable;
- resolution notes and timestamp;
- include-in-report boolean;
- sort order;
- timestamps and soft deletes.

Pivots:

- `finding_asset`;
- `finding_site`.

Rules:

- new blank finding is persisted immediately and may be incomplete while draft;
- completion/report validation ignores excluded findings;
- an included finding requires title, category, problem, final priority, valid scope, and at least one solution;
- selected-sites scope requires at least one same-company site and selected-assets scope requires at least one same-company asset; every other scope permits zero site/asset relations;
- priority override always requires a reason;
- `resolved` requires implemented solution or non-empty resolution notes;
- numbering is derived from current sort order and recalculates after reorder;
- number is not a permanent identifier.

Template copying copies:

- title and all text;
- the category reference;
- scope defaults;
- risk defaults/rationale;
- every solution and its import-managed values;
- recommended solution selection.

Later template edits never change the finding. Category label changes remain live for current UI; generated reports preserve snapshots.

Finding state machine:

| From | Allowed destinations |
|---|---|
| open | planned, in_progress, accepted, not_applicable |
| planned | open, in_progress, resolved, accepted |
| in_progress | open, planned, resolved |
| resolved | open, in_progress |
| accepted | open, planned |
| not_applicable | open |

### 7.14 Finding solutions

`finding_solutions` mirrors template-solution fields and includes soft deletes.

Rules:

- at least one active solution before completion/report;
- exactly one recommended solution before completion/report;
- recommended/implemented references point only to the same finding;
- a referenced solution cannot be deleted;
- deleting an unreferenced solution soft deletes it;
- restoring is allowed only if its external/local uniqueness is valid.

Solutions created manually receive an internal stable `external_key` generated as `manual-<id>` after persistence; imported/template-copied solutions retain their source external ID for traceability but are not synchronized.

### 7.15 Evidence

`evidences`:

- finding FK;
- type;
- title;
- file path or URL;
- original filename;
- caption/internal notes;
- include in report;
- MIME, size, SHA-256;
- sort order;
- timestamps and soft deletes.

A URL evidence has no file. A file evidence has no URL.

### 7.16 Generated files

`generated_reports`:

- assessment FK;
- format `pdf` or `xlsx`;
- `version unsignedInteger`;
- file path/name/size;
- file SHA-256;
- payload SHA-256;
- normalized `payload_snapshot` JSON;
- `settings_snapshot` JSON;
- generated timestamp;
- timestamps.

Unique:

```text
(assessment_id, format, version)
```

Version starts at 1 per format and increments under transaction/lock.

Generated rows and files are immutable.

### 7.17 Idempotency and deletion operations

`workspace_save_requests`:

- UUID request ID unique;
- assessment FK;
- expected/applied version;
- payload hash;
- response JSON;
- created timestamp.

Retain 24 hours, then purge through scheduler.

`deletion_operations`:

- UUID;
- entity type/id;
- status: staged, committed, cleanup_failed, cleaned, restored;
- trash path;
- manifest JSON;
- error text;
- timestamps.

The admin dashboard shows unresolved cleanup failures.

## 8. Settings

Use typed Spatie settings classes.

### 8.1 GeneralSettings

Defaults:

| Setting | Default |
|---|---|
| application name | AssestMe |
| timezone | Europe/Rome |
| locale | it |
| currency | EUR |
| currency symbol | € |
| symbol position | after |
| decimals | 2 |
| deletion policy | archive |
| max evidence file | 25 MB |
| max assessment evidence | 250 MB |
| evidence included by default | true |
| captions visible by default | true |
| technical notes in report | false |
| costs in report | true |
| summary solutions | recommended only |
| active risk profile | seeded default |
| new page per finding | true |
| default assessment status | draft |
| autosave | true, read-only |
| dark mode | enabled |
| report excluded findings in XLSX | false |

VAT treatment is not an application setting. No VAT status, display mode, rate, taxable amount, tax amount, enum, configurable label, or fiscal calculation exists. Every entered economic amount is understood as VAT excluded. Client and consultant VAT numbers remain permitted only as explicitly required anagraphic identifiers and never participate in calculations.

### 8.2 ReportSettings

- default title pattern: `Assessment IT — {client}`; `{client}` remains a supported persisted placeholder even though Italian presentation labels use Azienda;
- optional `consultant_name`;
- optional `business_name`;
- optional `consultant_role`;
- optional `consultant_email`;
- optional `consultant_phone`;
- optional HTTP/HTTPS `consultant_website`;
- optional multiline `consultant_address`;
- optional `consultant_vat_number`, used only as an anagraphic identifier;
- optional `consultant_pec` with email shape;
- optional `consultant_tax_code`;
- optional private `consultant_logo_path`;
- optional text-only `signature_name` and `signature_role`; no handwritten-signature upload in v1;
- primary color, strict six-digit hex;
- branding: consultant, client, both;
- cover title mode: `separate` or `combined`;
- show priority descriptions in legend: boolean;
- cover;
- content index without page numbers;
- executive summary;
- risk legend;
- summary table;
- methodology;
- minimal internal header after the cover;
- page numbers;
- `Mostra risoluzione`, enabled by default and captured in new snapshots without changing historical snapshots;
- signature block;
- disclaimer;
- technical notes;
- alternative solutions;
- costs;
- evidence;
- evidence captions;
- new page per finding;
- freeze after generation;
- methodology/disclaimer/footer/signature text.

Defaults:

- cover on;
- index off;
- summary on;
- risk legend on;
- methodology on;
- header and page-number-only footer on after cover;
- cover excluded from numbering and first internal page numbered `1`;
- alternatives on;
- evidence on;
- signature off;
- disclaimer on;
- freeze off.

### 8.3 Setting validation

- colors normalize uppercase `#RRGGBB`;
- logo accepts PNG/JPEG only, max 5 MB;
- consultant email and PEC use email validation; consultant website accepts HTTP/HTTPS only;
- consultant VAT number and tax code are normalized uppercase without spaces and are never used for tax calculation;
- currency must be uppercase ISO 4217 shape;
- numeric limits are bounded;
- report header and footer text max 120 characters;
- the current Canvas page script truncates over-width page chrome with an ellipsis rather than overlapping; D-059 governs its eventual removal;
- changing deletion policy affects future delete actions only;
- changing report settings does not modify existing report snapshots.

## 9. Filament UI/UX specification

### 9.1 Navigation

1. Dashboard
2. Assessment
   - Assessment
3. Aziende
   - Sedi
   - Asset
4. Impostazioni
   - Generale
   - Report
   - Template
   - Categorie
   - Matrice priorità
   - Livelli di impegno

Italian labels; English class names. Aziende remains clickable and is the native parent item of Sedi and `AssetCluster`. Asset and Impostazioni are native Filament clusters; their members use native page sub-navigation and do not appear separately in the main sidebar. Cluster-prefixed URLs and route names are generated through Filament classes or named routes, without hardcoded admin URLs or legacy redirects.

### 9.2 Dashboard

Show four compact clickable operational KPI cards first: draft assessments, completed assessments, open findings, and included open high/critical findings. Each has a visible text/icon meaning in addition to any color and links to the relevant filtered list or Workspace.

Below them show the latest five assessments with Azienda, scope, date, status, open-Finding count, and Workspace action; and a compact list of included open high/critical findings linking to their Workspaces.

`Stato applicazione` is a separate compact section with one row each for latest backup, database integrity, and unresolved file-cleanup operations. Each row includes status, latest event, concise message, and an action when applicable. Operational information is retained without presenting technical checks as large KPI cards or claiming an unenforced block.

### 9.3 Standard resources

Use Filament resources with:

- searchable/sortable tables;
- visible action menus;
- right-click as a duplicate convenience path, never the only path;
- confirmation for destructive actions;
- empty-state primary action;
- slide-over edit for compact records.

Advanced Table Export is allowed for generic lists. The assessment workbook remains dedicated.

Native form layout requirements:

- company create Save and Cancel return to the Aziende list; Save and new opens a blank create form and duplicate-identifier warning remains active;
- site identity keeps Azienda and Nome compact, while Dettagli spans full width and responsive schemas collapse to one column on narrow viewports;
- asset Azienda, Sede, and Tipologia asset share one balanced desktop row, company changes clear the filtered site, Identificazione is balanced, and Dettagli spans full width;
- template Content, Classification/defaults, Solutions, and Advanced options are separate full-width sections; long fields span full width and every solution uses at most two columns with conditional estimate controls;
- risk level identity uses hidden persisted IDs and advanced read-only codes; matrix editing uses the D-049 4×4 grid rather than a vertical repeater;
- generated-file history is a compact table with format/version, filename, configured-timezone date/time, size, and authenticated Download plus confirmed recoverable Delete actions.

### 9.4 Assessment workspace

Route:

```text
/admin/assessments/{assessment}/workspace
```

Sticky header:

- client;
- assessment;
- date;
- status;
- version/conflict state;
- offline/saving/unsaved/saved/error indicator;
- add blank;
- add from template;
- save draft;
- complete/reopen;
- PDF;
- XLSX.

Tabs:

- Finding;
- Dettagli assessment;
- Anteprima riepilogo;
- File Generati.

User-visible Workspace casing is `Workspace Assessment`, `Apri Workspace`, `Aggiungi Finding`, and `File Generati`. “Finding” retains that capitalization throughout the Italian UI.

### 9.5 Central grid

Use the native Filament 5 `Repeater` with `Repeater::table()`, `relationship('findings')`, and all rows loaded. No pagination/lazy loading exists inside the assessment form in v1.

Primary columns:

1. order;
2. title;
3. problem;
4. entrepreneur notes;
5. recommended solution description;
6. priority;
7. effort;
8. formatted estimate;
9. status;
10. include;
11. actions.

All primary columns are present in the table layout. Horizontal scrolling is accepted at 1366×768.

Compact mode means:

- reduced cell padding;
- 3-row initial textareas;
- no repeated visible field labels;
- sticky header where native Filament supports it;
- internal textarea scroll after configured maximum visual height.

The Milestone 0 `recommended_solution_summary` field is explicitly temporary and is treated as a projection of the recommended related solution. The definitive model edits the related solution record and does not keep a second authoritative solution text on the finding.

Selected-Finding editor:

- category;
- scope/sites/assets;
- consequence/likelihood;
- override reason;
- all solutions;
- technical notes;
- evidence;
- resolution.

Do not place nested repeaters or file uploads inside visible grid cells.

### 9.6 Autosave presentation

Statuses:

- Salvato;
- Modifiche non salvate;
- Salvataggio…;
- Conflitto: ricaricare;
- Offline;
- Errore di salvataggio.

Never use only color.

### 9.7 Keyboard

- `Ctrl+S` / `Cmd+S`: save draft;
- `Alt+N`: blank finding;
- `Alt+T`: template picker;
- `Alt+D`: duplicate the row whose action menu is active.

Shortcuts do not fire while an incompatible modal is open and must not replace visible buttons.

### 9.8 Mobile

Mobile essential edits:

- title;
- problem;
- entrepreneur notes;
- priority/status;
- recommended solution;
- evidence capture/upload;
- save.

The grid switches to a vertical finding editor. Do not claim spreadsheet parity.

Use camera capture hint for image upload. HEIC/HEIF remains rejected with an explicit message.

### 9.9 Browser support

Target compatibility:

- latest two stable Chrome;
- latest two stable Edge;
- latest two stable Firefox;
- current iOS Safari;
- current Android Chrome.

Automated Dusk verification uses the available current stable Chrome/Chromium and the maintained browser suite.

Edge, Firefox, iOS Safari, and Android Chrome remain compatibility targets. Their manual checklist is deferred until a reference installation and suitable devices exist and is not a development-milestone blocker under D-041.

### 9.10 Preferences and theme

- Filament dark mode enabled;
- report has no dark mode;
- native table columns are fixed for the first implementation;
- column resizing, visibility preferences, and arbitrary column reordering are not required;
- right-click has visible/touch equivalent.

### 9.11 Performance budget

Reference machine: 2 vCPU, 4 GB RAM, local SSD.

With 50 findings:

- workspace first render p95 ≤ 2.5 s;
- row slide-over open p95 ≤ 500 ms;
- autosave response p95 ≤ 800 ms;
- explicit full save p95 ≤ 2 s;
- reorder p95 ≤ 1.5 s;
- PDF ≤ 30 s and 512 MB;
- XLSX ≤ 10 s.

A failed budget is a blocker for the affected milestone.

## 10. Assessment persistence, autosave, and conflict protocol

### 10.1 Persistence units

- the native Repeater may submit the complete relationship workspace for both controlled autosave and explicit save;
- new Repeater items carry a stable temporary UUID until persistence and receive a server mapping to their database ID;
- adding a blank finding through the definitive application service persists it immediately;
- copying a template persists finding and solutions immediately;
- adding a solution/evidence persists it immediately after valid required input;
- field autosave may send one assessment-metadata/finding patch or the complete workspace state;
- explicit save sends the complete workspace.

No unsaved database row relies on a temporary primary key.

### 10.2 Request contract

Every save request includes:

```json
{
  "request_id": "UUID",
  "assessment_id": 123,
  "expected_version": 7,
  "tab_id": "UUID",
  "scope": "workspace",
  "record_id": null,
  "payload": {},
  "payload_sha256": "..."
}
```

### 10.3 Server algorithm

1. Validate request UUID and payload hash.
2. Return stored response if `request_id` was already applied.
3. Acquire `assessment:{id}:save` lock for 10 seconds, wait max 5 seconds.
4. Start transaction.
5. Update assessment using `WHERE id = ? AND lock_version = expected_version`.
6. If zero rows update, throw `AssessmentVersionConflict`.
7. Validate and persist the patch or complete Repeater workspace, mapping stable temporary UUIDs to persisted integer IDs.
8. Increment `lock_version`.
9. Store idempotency result.
10. Commit and release lock.
11. Return the applied version, normalized saved values, and temporary-UUID-to-ID mapping.

### 10.4 Client scheduling

Only one save request may be in flight per workspace tab.

- multiline fields: save on blur;
- short text fields: trailing 500 ms;
- select/toggle: trailing 250 ms;
- multiple changes in the same finding while a request is in flight are coalesced into the next patch;
- no per-keystroke request for multiline fields.

The client ignores an obsolete response when a newer local state or request sequence already exists. Autosave and the explicit-save button invoke the same application service.

Transport failure:

- retry once after 1 second with the same request UUID;
- no automatic retry for validation, conflict, lock timeout, or server error.

### 10.5 Conflicts and multiple tabs

Multiple tabs are allowed but not merged.

A stale tab receives conflict status and becomes read-only until the user reloads. The server value wins only after explicit reload; local unsaved state is not silently discarded.

### 10.6 Offline/close behavior

When offline:

- show Offline;
- stop sending;
- retain current browser state;
- show `beforeunload` warning for unsaved changes;
- do not claim offline persistence.

Reconnect triggers explicit retry/save by the user.

### 10.7 Buttons

**Salva assessment**:

- draft validation;
- complete workspace persistence;
- does not complete.

**Completa assessment**:

- completion validation;
- transition to completed;
- read-only.

**Riapri assessment**:

- confirmation;
- completed to draft;
- increments version.

### 10.8 Validation

Autosave permits incomplete draft rows but validates type/length/ownership/security constraints.

Completion/report validation applies only to included findings and returns row-numbered errors.

### 10.9 Error preservation

Validation/database errors leave Livewire form state intact. The error remains visible until successful save or explicit dismissal. The last successful version remains unchanged.

## 11. Template JSON import/export

### 11.1 Contract

Use:

```text
schemas/finding-template.schema.json
```

Runtime validator:

```text
opis/json-schema ^2.6
```

Templates require at least one solution and exactly one recommended solution.

Template and solution external IDs are lowercase, stable, and case-sensitive after normalization.

### 11.2 Semantic validation

Before preview reject the entire document when:

- duplicate template external IDs;
- duplicate solution external IDs within a template;
- recommended solution count is not exactly one;
- range minimum exceeds maximum;
- monetary estimate lacks valid amount/currency;
- non-monetary estimate contains amount/currency;
- custom billing lacks non-empty description;
- unknown schema version;
- invalid enum/reference;
- invalid UTF-8 or excessive text length.

### 11.3 Category resolution

Matching is case-insensitive Unicode-normalized by name.

Unknown category:

- create automatically during the import transaction;
- generate slug with Laravel slugification;
- resolve collision with numeric suffix;
- never rename an existing category based on import text.

### 11.4 Preview

Preview shows:

- create;
- replace;
- unchanged;
- invalid;
- field-level differences for replaced templates.

User chooses one global conflict mode:

- replace existing;
- skip existing;
- cancel.

No per-row deselection in v1.

### 11.5 Replace algorithm

Matching template: exact normalized `external_id`.

Replace mode:

1. update all template import-managed fields;
2. sync category;
3. match solutions by solution `external_id`;
4. update matched solutions;
5. create new solutions;
6. soft-delete template solutions absent from the file; detached assessment findings are unaffected;
7. set the one recommended solution;
8. leave unrelated category records untouched.

Fields are full-state: explicit `null` clears a nullable field. Omitted required fields invalidate the file.

`active=false` disables the template only.

Renaming an external ID creates a new template. The old template is not inferred or deleted.

### 11.6 Transactionality

The selected operations for the entire file execute in one transaction. Any error produces zero changes.

### 11.7 Schema versions

Only version 1 is accepted.

A future version requires:

- separate schema file;
- explicit migrator;
- tests;
- signed decision update.

### 11.8 Export

Deterministic JSON:

- UTF-8;
- pretty-printed;
- fixed key order;
- templates sorted by external ID;
- solutions sorted by sort order then external ID;
- no database IDs/timestamps.

Default export includes active templates. UI can explicitly include inactive templates.

Round-trip tests must prove no data loss.

## 12. Evidence files

### 12.1 Allowed files

Allowed extensions/MIME families:

- jpg/jpeg;
- png;
- webp;
- pdf;
- txt/log;
- csv;
- docx;
- xlsx;
- ods.

Explicitly rejected:

- SVG;
- HTML;
- scripts/executables;
- ZIP/archives;
- HEIC/HEIF;
- macro-enabled Office formats;
- arbitrary unknown MIME types.

Validate content MIME and filename extension together.

### 12.2 Limits

Defaults:

- 25 MB per file;
- 20 files per upload action;
- 250 MB total evidence per assessment;
- 12,000 × 12,000 maximum server-accepted image dimensions.

Client-side Filament/FilePond image handling:

- contain within 2400 × 2400;
- preserve aspect ratio;
- retain Filament/FilePond default EXIF orientation correction; do not call `orientImagesFromExif(false)`;
- no server-side recompression package in v1.

### 12.3 Storage

Private local disk:

```text
storage/app/private/clients/{client}/assessments/{assessment}/findings/{finding}/evidence/
```

Use generated physical filenames. Store original name separately. Every Filament upload field uses private visibility, explicit accepted MIME types, and `preventFilePathTampering()`.

Store SHA-256. Reject duplicate file content within the same finding.

### 12.4 URLs

- HTTP/HTTPS only;
- max 2048;
- syntax validation only;
- do not perform network reachability checks;
- reject embedded username/password;
- private/internal URLs are allowed.

### 12.5 Rendering

- images may be previewed through authorized responses;
- PDF may be opened inline through authorization;
- other documents download as attachments;
- no Office/PDF conversion;
- no antivirus integration in v1.

Download headers:

- `X-Content-Type-Options: nosniff`;
- restrictive CSP for inline PDF/image response;
- attachment disposition for non-preview types;
- no public storage URL.

### 12.6 Deletion and orphan recovery

Archive mode soft deletes evidence and retains files.

Permanent deletion:

1. create deletion operation;
2. atomically rename files into private trash directory;
3. delete database records in transaction;
4. on DB failure restore files;
5. after commit remove trash;
6. if cleanup fails mark operation `cleanup_failed` and show dashboard alert.

Create:

```text
php artisan assestme:storage:audit
php artisan assestme:storage:cleanup
```

Audit reports but does not silently delete:

- orphan files;
- missing files;
- hash mismatch;
- unfinished deletion operations.

### 12.7 Missing/corrupt evidence

An included missing/corrupt evidence blocks new PDF generation and identifies the finding/evidence.

Existing generated report files remain valid and downloadable.

### 12.8 Encryption

Application-level file encryption is outside v1. Production must use restrictive permissions and an encrypted server/storage volume where required.

## 13. PDF report contract

Production uses WeasyPrint as its sole renderer. One immutable PDF contains an A4 portrait cover, optional portrait `Quadro generale`, A4 landscape Finding summary, portrait Finding pages, and portrait evidence/final sections. Page geometry and chrome are owned by named CSS pages and margin boxes.

### 13.1 Title and branding

Title precedence:

1. assessment `report_title_override`;
2. ReportSettings title pattern;
3. `Assessment IT — {client}`.

Logo: PNG or JPEG, max 5 MB.

Branding renders the consultant logo, company logo, or both according to the saved setting, loading each from verified private storage. A selected but missing/invalid logo fails visibly; an unselected or genuinely absent optional logo creates no placeholder. Cover-title mode follows D-051: separate mode prints title and company on separate lines, combined mode appends the company exactly once.

Primary color is passed to the view only after strict `#RRGGBB` normalization.

### 13.2 Sections

The production section order is:

1. optional simplified portrait cover;
2. optional portrait `Quadro generale`, controlled by the existing `executive_summary` section setting and optionally containing `Riepilogo Assessment`;
3. landscape Finding summary;
4. portrait detailed Findings, each with one or two logical pages;
5. dedicated portrait evidence pages;
6. any enabled methodology, disclaimer, signature, and attachment sections.

### 13.3 Header/footer/page numbering

WeasyPrint CSS margin boxes provide repeated chrome:

- the cover has no header, footer, or number;
- the minimal internal header contains assessment and company only;
- the footer contains the page number only;
- the first internal page displays `1`;
- there is no `Pagina X di Y`, application-name repetition, left footer text, Canvas driver, or renderer fallback.

### 13.4 Finding summary

The repeating-header landscape table contains:

- number and full title;
- deterministic server-side problem excerpt;
- implemented solution when present, otherwise recommended solution, with its role identified;
- priority;
- effort;
- estimate.

Scope, category, and status are intentionally absent. Excerpts end without splitting a word, prefer a sentence boundary, and never replace full Finding-sheet content.

### 13.5 Finding detail and pagination

The immutable Finding content order is:

1. numbered title and textual priority/status/category badges;
2. optional `Spiegazione del problema`;
3. problem;
4. implemented or recommended primary solution;
5. other enabled solutions in stable order;
6. real configured risk mini-matrix;
7. compact `Sistemi interessati` and assets;
8. optional technical notes;
9. optional resolution when `show_resolution` is enabled.

A typed deterministic planner builds one or two logical pages per Finding, excluding evidence. Every solution block is indivisible, and a second page begins `NN / Titolo finding — continua`. Pagination depends only on validated text quantities and semantic content, never DOM measurements, iterative rendering, coordinates, font reduction, or truncation.

Selected assets show a compact row with non-duplicated name/type, site, manufacturer, and IP. Without assets, only useful synthetic scope is shown; extended free-form scope text is not printed automatically.

### 13.6 Estimates

Format examples:

- `Circa 250 € una tantum`;
- `Indicativamente 500–900 €`;
- `Circa 20 €/mese`;
- `Da sommare ad altre attività`;
- `Richiede preventivo`;
- `Richiede approfondimento`;
- `Variabile in base alla soluzione`;
- `Nessun costo diretto previsto`.

The report contains exactly once, as a general note and never beside each amount:

`Tutti gli importi indicati sono stime orientative e si intendono IVA esclusa.`

The note is fixed, not configurable, and present even when an assessment currently has no numeric estimate.

### 13.7 Evidence

- images embedded from verified local files/data URI;
- evidence heading, image, and optional caption stay in one logical page-break-safe block so the heading cannot be orphaned;
- preserve aspect ratio and use an A4-safe tested maximum height without cropping or overflow;
- multiple evidence blocks may continue on following pages;
- caption when enabled;
- non-image files listed with title/original filename/type;
- uploaded private files do not receive a web download link in the client PDF;
- URL evidence is clickable;
- no merging uploaded PDFs.

### 13.8 Signature/disclaimer

Signature is an optional text block with the configured signature name, role, date line, and empty signature line. No image-signature support in v1.

No PDF/A or certified accessibility requirement in v1.

### 13.9 Snapshot/version

Build a normalized readonly report DTO graph and serialize it to JSON before rendering.

Snapshot includes every displayed and non-displayed value needed to reproduce the chosen report, including:

- client/assessment;
- settings;
- labels/colors;
- findings;
- solutions;
- asset display values;
- evidence metadata/hashes;
- generated timestamp;
- application version.

Version and filename:

```text
AssestMe_<client-slug>_<assessment-date>_vNN.pdf
```

Old generated files are permanently deletable only through explicit confirmation, independently of the global archive setting.

### 13.10 Limits

- synchronous;
- 120-second PHP request maximum, application target 30 seconds;
- 512 MB memory limit;
- maximum output file 50 MB.

Exceeding limits fails visibly.

### 13.11 Verification

Automated tests must verify:

- `%PDF-` signature;
- page count;
- extracted Italian text through the direct `smalot/pdfparser` development dependency;
- `Pagina 1 di Y` and final page number;
- summary fields;
- long text;
- 50 findings;
- page breaks;
- images;
- URLs;
- alternatives;
- missing/corrupt evidence failure.

A manual visual golden checklist compares cover, summary, short/long finding, evidence, footer, and last page. Do not use fragile byte-for-byte PDF golden files.

## 14. XLSX workbook contract

Use `phpoffice/phpspreadsheet` directly.

Filename:

```text
AssestMe_<client-slug>_<assessment-date>_vNN.xlsx
```

Workbook versioning is independent from PDF versioning.

### 14.1 Sheet `Finding`

One row per exported finding.

Row 1 is one merged general-note cell across all exported columns containing exactly:

`Tutti gli importi indicati sono stime orientative e si intendono IVA esclusa.`

The note is fixed, not configurable, and not repeated per finding or amount. Column headers are on row 2, data starts on row 3, the frozen pane is `A3`, and the auto filter starts on row 2.

Columns:

1. Numero
2. Titolo
3. Categoria
4. Ambito
5. Asset
6. Problema
7. Note per l'imprenditore
8. Soluzione raccomandata
9. Soluzioni alternative
10. Priorità
11. Motivazione priorità
12. Impegno
13. Stima economica
14. Stato
15. Note tecniche, when enabled
16. Evidenze

### 14.2 Sheet `Soluzioni`

One row per solution:

- finding number/title;
- solution title;
- description;
- recommended;
- implemented;
- comparison notes;
- effort;
- estimate type;
- min/max;
- currency;
- recurrence;
- estimate notes.

### 14.3 Sheet `Evidenze`

One row per evidence:

- finding number/title;
- type;
- title;
- original filename;
- caption;
- URL/path reference;
- included in report;
- size;
- SHA-256.

URL evidence uses a native hyperlink. Private uploaded files are listed without public link.

### 14.4 Formatting

- native XLSX;
- frozen header;
- auto filter;
- wrapped text;
- vertical top alignment;
- fixed reviewed widths;
- date/currency cells native where a numeric amount is represented;
- non-monetary estimates as localized strings;
- no formulas or total;
- finding exclusions follow action option, default false.

All text fields are validated below 20,000 characters, safely below Excel's cell limit.

### 14.5 Tests

Reopen the generated file with PhpSpreadsheet and assert:

- sheet names;
- row counts;
- values/types;
- filters;
- freeze pane;
- wrapping/styles;
- URL hyperlinks;
- alternative solutions;
- all estimate branches.

## 15. Seed data and fixtures

`DatabaseSeeder` seeds domain configuration only:

- default risk profile/matrix;
- effort levels;
- categories;
- asset types;
- general/report settings;
- base template library.

It does not create a user.

Sample client/assessment data requires:

```text
php artisan db:seed --class=SampleDataSeeder
```

and refuses production unless `--force`.

Fixture coverage is separate from the user-facing template library and must include:

- all estimate types;
- all billing frequencies;
- multiple solutions;
- all scopes;
- exact one recommended;
- invalid zero/multiple recommended;
- invalid range;
- duplicate external IDs;
- update/replace import;
- 10/25/50 findings;
- long text;
- corrupt image;
- all evidence types.

Report/XLSX fixtures include the exact mandatory VAT-excluded estimate note once and contain no VAT treatment/display/rate/tax-calculation field. Anagraphic VAT-number fields remain allowed for clients and consultant contacts only.

## 16. Testing, CI, browser QA, and performance

### 16.1 Test layers

#### Unit

- risk calculation;
- transitions;
- override;
- estimate validation/formatting;
- scope/asset formatting;
- MAC normalization;
- import normalization;
- snapshot hashing;
- filename/version generation;
- deletion manifest.

#### Feature

- singleton admin;
- auth/MFA reset;
- all CRUD;
- import preview/replace/skip/rollback;
- template copy;
- optimistic locking/idempotency;
- autosave failures;
- lifecycle;
- scope validation;
- evidence auth/storage/deletion;
- PDF/XLSX;
- backup/restore.

#### Filament/Livewire

- page/resource mount;
- native table Repeater fields/actions and `HasMany` persistence;
- row add/clone/reorder/remove;
- 10/25/50-finding datasets;
- multiline preservation and validation-state retention;
- slide-over;
- settings;
- error messages.

#### Canary

Zero failures and zero skips. Factories and required route parameters must allow every page to be exercised.

#### Architecture

Enforce:

- strict types;
- no debug calls;
- no `env()` outside config;
- domain actions/services do not depend on Filament;
- no business logic in Blade;
- enum/settings namespaces;
- no vendor edits.

#### Dusk

The maintained current-Chrome suite must automate:

- login;
- multiline editing;
- autosave status;
- explicit save;
- native accessible reorder;
- native clone/delete actions;
- right-click and visible equivalent;
- report/XLSX download;
- browser console has no errors.

Shortcut behavior, physical drag-and-drop, and a real two-window conflict remain implementation requirements covered by application/Livewire tests where practical, but expanded browser automation is deferred unless a regression requires it.

No Node.js.

### 16.2 Deferred manual compatibility QA

When a reference installation and suitable devices become available, record date, exact browser version, operating system/device, viewport, and pass/fail in the Progress/Discoveries section.

Desktop:

- latest stable Edge at acceptance time;
- latest stable Firefox at acceptance time;
- 1366×768;
- 1920×1080;
- light/dark mode.

Mobile:

- latest stable iOS Safari available on the test device at acceptance time;
- latest stable Android Chrome available on the test device at acceptance time;
- camera capture;
- image upload;
- vertical finding edit;
- download/open PDF.

This checklist is not executable in the current development environment and does not block Milestone 4 or the development implementation state. It must remain explicitly deferred and must be completed before claiming the corresponding production browser/device compatibility.

### 16.3 CI

GitHub Actions currently uses the hosted `ubuntu-24.04` runner with PHP 8.3 only. This is CI infrastructure, not an application host requirement or a production compatibility claim.

Jobs:

1. install from committed `composer.lock`;
2. validate Composer;
3. audit locked dependencies;
4. Pint;
5. Larastan;
6. Pest/feature;
7. Canary strict zero-skip;
8. Dusk with Chrome;
9. backup/restore integration;
10. bootstrap script from a clean checkout on the runner.

No coverage percentage gate. Acceptance behavior and failure paths are the gate.

Warnings, notices, deprecations, unhandled console errors, skipped Canary pages, and security advisories fail CI.

### 16.4 Test database

Use a unique temporary file SQLite database for every application, integration, concurrency, Canary, Dusk, benchmark, and verification run. The path must be outside the configured application database path and must be removed after the run.

Every test runner also redirects private storage, generated reports, cache, sessions, backups, pending uploads, and deletion trash to disposable roots. Tests must fail before execution if their resolved database or private-storage path matches the configured application instance.

In-memory SQLite only for pure tests that do not depend on connection/locking/filesystem behavior.

### 16.5 Performance

Provide an Artisan benchmark command:

```text
php artisan assestme:benchmark --findings=50
```

It creates isolated fixture data, measures defined budgets, outputs JSON, and exits non-zero when a budget fails.

Measure Livewire payload sizes and fail when the initial workspace response exceeds 5 MB uncompressed.

### 16.6 Quality commands

Run these application commands inside the `app` service. The first command is the supported host entrypoint; the remainder are container commands shown without the repeated `docker compose -f docker/compose.dev.yml exec app` prefix.

```bash
docker compose -f docker/compose.dev.yml up --build
composer validate --strict
composer install --no-interaction --prefer-dist
composer quality
composer browser
php artisan assestme:benchmark --findings=50
scripts/verify.sh
```

`composer.lock` is mandatory.

For final acceptance, run `scripts/verify.sh` as the single aggregate entry point. During implementation, run proportional focused checks under D-057 and rerun a failing command before broadening scope. `composer quality` and `composer browser` are maintained subgates; when either has already succeeded for the exact current repository and runtime fingerprint, the aggregate verifier consumes its non-versioned receipt instead of repeating the same expensive work. The verifier never treats a focused Dusk selection, stale receipt, failed command, or changed source/dependency/runtime as success.

`migrate:fresh --seed`, standalone Canary, diagnostics, storage audit, and Dusk are executed inside the maintained isolated scripts. They are not accepted as direct development-verification entry points against the configured application environment. `scripts/bootstrap-local.sh` remains the explicit command for initializing an intended local installation.

## 17. Security and operational controls

### 17.1 Authentication

- one administrator;
- login rate: 5 attempts per email+IP per minute;
- remember-me enabled;
- inactivity timeout 120 minutes;
- concurrent sessions allowed;
- no email verification;
- password reset only through `assestme:create-admin --replace`;
- production session encryption and secure cookies.

### 17.2 MFA

Optional TOTP.

- eight recovery codes;
- recovery codes encrypted at rest;
- CLI reset with confirmation;
- no email/SMS service;
- MFA enrollment and recovery tested.

### 17.3 Application

- HTTPS production;
- CSRF;
- policies;
- server-side validation;
- private storage;
- random physical filenames;
- sanitized plain text/Markdown rendering;
- no user HTML;
- CSP/security headers;
- no credentials/client data in logs;
- `APP_DEBUG=false`.

### 17.4 Dependency security

Before accepting a package:

```bash
composer audit --locked
composer licenses
```

Review community plugin source and Composer scripts. Do not allow unapproved Composer plugins or post-install scripts.

### 17.5 SQLite/storage

- permissions from deployment section;
- WAL checkpoint during backup;
- no SQLite file under public root;
- periodic `PRAGMA integrity_check`;
- dashboard warning on backup/integrity failure.

### 17.6 Out of scope

- antivirus;
- application-level evidence encryption;
- SSO;
- hardware-key MFA;
- WAF management;
- off-site backup transport.

These are not silently claimed as implemented.

## 18. Git and agent execution policy

### 18.1 Branches and releases

- `main` always releasable;
- one branch per milestone/coherent vertical slice;
- small imperative English commits;
- final v1 tag: `v1.0.0`;
- intermediate milestone tags are not required.

### 18.2 Agent plan authority

The agent may update without approval only:

- Progress checkboxes;
- Discoveries;
- exact locked package versions;
- command outputs;
- factual implementation notes.

It may not change an APPROVED decision.

Decision statuses:

- `OPEN`: requires user decision;
- `APPROVED`: normative;
- `SUPERSEDED`: replaced by a named later decision;
- `BLOCKED`: technically impossible under current approved constraints.

This plan contains no OPEN decisions at handoff.

### 18.3 Impossible decisions

If an approved central dependency/decision is impossible:

- stop the affected milestone;
- mark BLOCKED;
- provide evidence and one or more explicit alternatives;
- do not implement a fallback;
- do not declare completion.

The agent may continue independent work only when it cannot create rework or hide the blocker.

A mandatory native Filament workspace capability blocks its affected milestone. Renderer validation follows D-009/D-059: a blocking Chrome failure permits only the approved bounded WeasyPrint evaluation, while an accepted production renderer failure blocks the affected milestone without a silent fallback.

### 18.4 Dependencies

- no vendor edits;
- no fork without approval;
- exact versions in `composer.lock`;
- review licenses/advisories;
- no broad update;
- document package quirks in Discoveries.

### 18.5 Vertical slices

For each slice:

```text
migration/model → action/service → Filament UI → automated tests → browser proof
```

Do not produce large untested batches.

### 18.6 Allowed files

The agent may add:

- PHP/code/config;
- migrations/seeders/factories;
- Blade/translations;
- shell scripts;
- CI workflows;
- JSON fixtures/schemas;
- test images/documents.

Do not add additional product documentation Markdown.

### 18.7 Final URL proof

Even when the environment cannot expose a public persistent URL, completion requires:

- running local server;
- successful curl/HTTP check;
- Dusk login;
- final local URL and credentials;
- no claim of a public deployment.

Local test credentials may be shown once in final handoff. Production credentials must never be logged.

## 19. Milestones

### Milestone 0 — Reproducible bootstrap and compatibility gate

This milestone records the historical DOMPDF bootstrap acceptance. D-009/D-059 govern the later renderer spike and do not rewrite its results.

Deliver:

- capability-based PHP preflight;
- clean Laravel/Filament install;
- committed dependency lock;
- admin login;
- Italian panel;
- native Filament 5 table Repeater proof with 10, 25, and 50 multiline rows backed by a real `HasMany` relationship;
- add/clone/delete/reorder browser tests and controlled autosave/explicit-save proof;
- DOMPDF multipage/page-number proof;
- PhpSpreadsheet proof;
- JSON Schema validation proof;
- Canary;
- Dusk;
- quality/CI;
- diagnostics;
- local URL.

Acceptance:

- no Node/frontend build;
- zero package advisories;
- native table Repeater persists multiline Eloquent findings and remains usable with 50 rows;
- PDF works without external binary;
- browser console clean;
- all commands green.

The recorded central-grid or DOMPDF proof failure would have blocked this historical milestone; future renderer validation follows D-009/D-059.

### Milestone 1 — Foundation, security, settings, backup

- singleton administrator;
- optional MFA/recovery/reset;
- clients/sites/assets;
- categories/types;
- risk/effort;
- settings;
- backup/restore;
- dashboard basics;
- archive/permanent deletion infrastructure.

### Milestone 2 — Template library

- template/solution CRUD;
- JSON Schema;
- all semantic validators;
- import preview;
- deterministic replace/skip;
- export round-trip;
- fixtures/base library.

### Milestone 3 — Assessment workspace

- lifecycle;
- native Filament table Repeater workspace;
- row persistence;
- autosave/version/idempotency/conflict;
- explicit save;
- template picker;
- scope/assets/sites;
- solutions;
- evidence;
- completion/reopen;
- 50-finding performance.

### Milestone 4 — PDF and XLSX

- normalized snapshot;
- report settings;
- complete PDF;
- page numbering;
- evidence;
- versioning;
- three-sheet XLSX;
- generated file history;
- isolated automated validation; manual cross-browser/device compatibility is deferred under D-041.

### Milestone 5 — Hardening and delivery

- right-click standard tables;
- generic exports;
- all Italian translations;
- record the browser/mobile checklist as deferred until a reference installation and devices exist;
- storage audit;
- authoritative Docker development profile and legacy classification of superseded generic-host production artifacts;
- future CloudPanel production decision and implementation remain separate work;
- clean-checkout bootstrap;
- final quality/security review;
- app running and credentials supplied.

## 20. Final acceptance criteria

### Environment

- PHP 8.3 capability contract documented and verified without a normative host distribution;
- clean `composer install` from lock;
- no runtime Node/Redis or external cloud renderer; Docker Compose is the authoritative development path, and any local renderer binary must be the single renderer accepted through D-059;
- SQLite PRAGMAs verified;
- local URL reachable;
- real login works.

### Functionality

- one admin only;
- optional MFA;
- client/site/asset;
- template import/export;
- assessment lifecycle;
- multiline grid;
- native add/clone/delete/reorder actions;
- autosave/manual save/conflict handling;
- all scopes;
- multiple solutions;
- risk/override;
- all estimates;
- evidence;
- PDF;
- XLSX;
- archive/permanent deletion;
- backup/restore.

### Failure paths

- invalid import changes nothing;
- duplicate/semantic import errors identified;
- stale save never overwrites;
- offline/failed save stays unsaved;
- invalid included finding blocks completion/report with row;
- missing evidence blocks new report;
- failed deletion remains recoverable;
- failed backup/restore exits non-zero;
- no exception produces success UI.

### Quality

- Pint;
- Larastan without baseline;
- Pest;
- Canary zero skip/failure;
- Dusk;
- Composer audit;
- benchmark;
- autonomous test isolation with no mutation of an initialized application instance;
- no debug/fallback/placeholder/vendor patch;
- Italian labels complete;
- English meaningful comments;
- lock file committed.

### Handoff

Report:

```text
Application URL
Local administrator email
Local administrator password
PHP/Laravel/Filament versions
Exact plugin/package versions
Database/storage paths
Start commands
Deployment/backup commands
Automated test results
Manual QA matrix
Benchmark results
Known blocking defects
```

Any known blocking defect means the plan is not complete.

## 21. Review resolution matrix

This section records how the external blueprint review dated 2026-07-13 was resolved.

| Review ID | Resolution in specification 2.0 |
|---|---|
| B-01 | SUPERSEDED by specification 2.3: PHP 8.3 remains exact, while runtime provisioning and preflight are capability-based with no normative host distribution or package source |
| B-02 | RESOLVED by D-009 on 2026-07-24: `LARAVEL_PDF_DRIVER=weasyprint` selects the sole accepted production renderer |
| B-03 | Nginx/PHP-FPM, paths, ownership, permissions, TLS, release deploy, rollback, cron, and PHP limits defined |
| B-04 | `lock_version`, request UUID, payload hash, one in-flight request, coalescing, stale-tab conflict protocol |
| B-05 | Complete assessment and finding state machines with transition effects |
| B-06 | Immutable normalized report payload/settings snapshots plus payload and file hashes |
| B-07 | Same-filesystem trash staging, compensation on DB failure, retryable cleanup records |
| B-08 | Soft delete for solutions; deletion blocked while recommended or implemented |
| B-09 | Strengthened Draft 2020-12 schema plus mandatory application semantic validation and fixtures |
| B-10 | Full-replace import by stable template and solution external IDs, with deterministic preview and conflict modes |
| B-11 | Complete report stub covering summary, findings, assets, rationale, alternatives, evidence, technical notes, signature, and disclaimer |
| B-12 | RESOLVED by D-059 on 2026-07-24: CSS margin boxes replace Canvas after WeasyPrint acceptance |
| B-13 | Direct `phpoffice/phpspreadsheet` runtime dependency and three-sheet workbook contract |
| B-14 | Laravel Dusk for browser automation plus blocking manual Edge/Firefox/iOS/Android checklist |
| B-15 | SUPERSEDED on 2026-07-13: the unavailable community grid plugin was removed; Milestone 0 now proves the native Filament 5 table Repeater with real relationships and 10/25/50 findings |
| B-16 | Optional TOTP MFA with eight recovery codes and CLI reset; no email dependency |
| B-17 | Application-aware backup, retention, manifest verification, restore command, CI restore test, and quarterly production restore test |
| B-18 | Domain-only seeder, separate admin bootstrap command, factory-created CI users, and one-time local credentials |

No OPEN product decision remains. D-009/D-059 are accepted pending the explicitly bounded technical renderer validation; runtime values such as the production FQDN and secrets remain deployment inputs, not design decisions.

## 22. Progress

The implementation agent must maintain this section.

The report-settings viewport-height workbench refinement started on 2026-07-25 as a continuation of the uncommitted lateral-space slice at commit `dffc8b1d44d4b50333eeea30971d1a759797fb21`. The authoritative Docker application and Selenium services remain healthy. Inspection of the locked Filament page and settings-form implementation confirmed that the save action is a native form footer outside the settings schema, while Filament's page component exposes an existing `full-height` mode. The accepted desktop behavior is now explicit: the report workbench must consume only the viewport height remaining below the application header, page heading, and settings tabs; the native save footer remains visible; the left option group is the sole vertically scrolling region; and the right real-PDF preview remains fixed, fills its available height, and retains fullscreen expansion. Below the existing `xl` split breakpoint the page must return to its deliberate sequential document flow without internal workbench scrolling. This refinement changes no setting, renderer, report content, persistence, route, dependency, or unrelated page.

The report-settings viewport-height workbench refinement completed on 2026-07-25. The page now invokes Filament's native full-height page component through the dedicated `filament.pages.report-settings-page` view, constrains only this desktop page to the same `100dvh - 4rem` height used by Filament's topbar layout, and propagates resolved height/minimum-size rules through the locked schema wrappers. The existing `2 / 3` option/preview allocation fills the residual page content: the option group is the only application-owned `overflow-y: auto` region, the real-PDF iframe flexes to the complete right-panel height without forcing an A4 container ratio, and the native save footer remains fully visible and clickable. The isolated Selenium/Chromium regression passed `1` test with `70` assertions at `1920 × 1080` and `390 × 844`, proving full-width/full-height native page classes, zero document vertical overflow on desktop, left options with content taller than their client height, positive option scroll while `window.scrollY` and preview position remain unchanged, matched option/preview panel heights, preview and save footer within the visible content boundary, a real clickable `36` px save button, real `%PDF-` response, live unsaved-title refresh, fullscreen/Escape behavior, narrow A4 sequential flow with visible rather than internal option overflow, no horizontal overflow, no generated report/file, and no severe console error. The combined settings and authenticated-preview Feature coverage passed `11` tests with `224` assertions; Pint passed on all changed PHP/test files, configured PHPStan passed all `249` application files, Blade cache/clear and `git diff --check` passed, and source/published CSS are byte-identical at SHA-256 `fa6b187ccef4b519926f04ee0f39b06a7cb28dfd28680edb9720fed93d64d401`. The visually inspected normal proof is `storage/app/qa-artifacts/report-settings-viewport-workbench-1920x1080.png` (`110,599` bytes, SHA-256 `8708f95e8008f3bdf573a0c0ee5a5a05823dc0abeff8f096f3d555f35c607e2d`), showing the sole left scrollbar, fixed complete PDF panel, and visible Save action; the expanded proof is `storage/app/qa-artifacts/report-settings-viewport-workbench-fullscreen-1920x1080.png` (`57,819` bytes, SHA-256 `254678e819d36cbd9f881ccc1883ea0c106d1779730fdc3f90dbcb82a5fa1316`). No report setting, renderer, report content, snapshot, persistence, route, dependency, migration, or unrelated page changed. The complete `scripts/verify.sh`, production PDF suites, benchmark, backup/restore, storage audit, full Dusk suite, and manual non-Chromium browser matrix were not run or claimed for this proportional layout-only refinement.

The report-settings lateral-space refinement started on 2026-07-25 at commit `dffc8b1d44d4b50333eeea30971d1a759797fb21` on the clean `develop` branch. The authoritative Docker application and Selenium services are healthy. Read-only inspection found that this page still inherits Filament's default `7xl` content cap and gives its five-column desktop grid three columns to the option group and two to the real-PDF preview. This combination concentrates the page within the centered cap and limits the preview despite available application workspace to the right. This bounded refinement will use Filament's native full content width for this page only and reverse the existing desktop allocation to two columns for options and three for the preview, while retaining the single-column narrow sequence, real WeasyPrint response, settings behavior, fullscreen overlay, persistence, routes, dependencies, and unrelated pages unchanged.

The report-settings lateral-space refinement completed on 2026-07-25. `ReportSettingsPage` now uses Filament's native `Width::Full` content width and assigns the existing five-column desktop grid `2 / 3` to options and preview respectively; the default one-column narrow layout remains unchanged. The isolated Selenium/Chromium regression passed `1` test with `54` assertions at `1920 × 1080` and `390 × 844`, proving the `fi-width-full` page contract, a layout wider than `1,300` px, at most `40` px unused at the right content edge, a preview wider than the option group and wider than `800` px, exact A4 ratio, real `%PDF-` response, live unsaved-title refresh, full-viewport expansion and Escape closure, narrow no-overflow behavior, no generated report/file, and no severe console error. The combined settings and authenticated-preview Feature coverage passed `11` tests with `224` assertions; Pint passed on all three changed PHP/test files and configured PHPStan passed all `249` application files. The visually inspected normal proof is `storage/app/qa-artifacts/report-settings-wide-preview-1920x1080.png` (`116,836` bytes, SHA-256 `2c39111a00ff7195dff38e64215ec4dfdfa5f75122e9ef420ac950cc069cac2c`), showing the left-aligned options, dominant preview, right-side workspace use, and the browser PDF viewer at `52%`; the expanded proof is `storage/app/qa-artifacts/report-settings-weasyprint-preview-fullscreen-1920x1080.png` (`58,509` bytes, SHA-256 `0e66ea71aa554d5602dc41213e8bb1b5bb404db7211bbd546399d100ab7c03cc`). Two preliminary Dusk commands were rejected before browser assertions because the first bypassed the mandatory isolated runner and the second supplied an incorrect container project path; the same focused test then ran through `scripts/dusk-isolated.sh` at `/workspace`, exposed one obsolete height-comparison assertion, and passed after that assertion was corrected to the actual viewport-fill contract. No CSS, report setting, preview/PDF renderer, report content, persistence, route, dependency, migration, or unrelated page changed. The complete `scripts/verify.sh`, production PDF suites, benchmark, backup/restore, storage audit, full Dusk suite, and manual non-Chromium browser matrix were not run or claimed for this proportional layout-only refinement.

The report-settings behavior audit and fullscreen-preview slice started on 2026-07-25 at commit `8e9ad07afafa8dac14025a00f718764a91f5e105` on the clean `develop` branch. The authoritative Docker application and Selenium services are healthy; the application container resolves `LARAVEL_PDF_DRIVER=weasyprint`, `/usr/bin/weasyprint`, and WeasyPrint `57.2`. Production explicitly renders `reports.assessment` through Spatie's WeasyPrint driver, while the authenticated side-effect-free preview renders the same Blade/CSS under D-059 screen media. Read-only field-by-field tracing found that the preview token currently forwards only a subset of the visible unsaved `ReportSettings`; consultant contact/logo/signature values, configurable final-section texts, and General-owned estimate formatting values do not all reach the preview fixture. It also found visible General-owned controls that the accepted WeasyPrint report does not consume: `summary_solutions`, `technical_notes_in_report`, `costs_in_report`, and `new_page_per_finding`; the current PDF instead uses the typed `summary_table`, `technical_notes`, `costs`, deterministic one/two-page Finding planner, and implemented-or-recommended primary-solution rules. This slice will remove only those obsolete or duplicate report-page controls, retain the operative PDF/XLSX/default controls with explicit Italian helper descriptions, make every unsaved operative PDF setting flow through the same preview DTO/view, add a temporary full-viewport preview control with a visible exit path and no dependency, and prove production/preview parity plus unsupported-token and browser failure paths. Immutable generated-report persistence, snapshot hashing/versioning/history, the sole WeasyPrint production renderer, report Blade/CSS, approved deterministic pagination, saved General setting keys, database schema, and unrelated settings remain unchanged.

The authoritative WeasyPrint report-settings preview slice completed on 2026-07-25. Production generation and the authenticated transient preview now call the same application-owned `WeasyPrintReportRenderer`, which explicitly selects Spatie's `weasyprint` driver and renders the same `reports.assessment` Blade/CSS; the preview response is an inline real PDF with `no-store`, creates no `GeneratedReport`, report file, immutable snapshot, version, history entry, or persisted setting, and Composer still contains no DOMPDF package. Shared title resolution, logo validation/embedding, and estimate formatting remove the former production/preview divergence. Every visible visual PDF property now reaches the representative preview DTO and the production snapshot/view, including identity/contact/logo/signature values, configurable final-section text, evidence/captions, solution alternatives, and General-owned currency formatting. All `42` visible controls have Italian scope descriptions; the XLSX exclusion default and post-generation freeze remain visible with explicit non-preview scope, while obsolete duplicate General controls (`evidence_included_by_default`, `captions_visible_by_default`, `summary_solutions`, `technical_notes_in_report`, `costs_in_report`, and `new_page_per_finding`) are no longer rendered on the Report page and their persisted keys remain untouched. A dependency-free viewport overlay expands the real PDF preview, exposes a visible close control, supports Escape, and preserves the deliberate narrow layout. Focused Feature coverage passed `11` tests with `223` assertions; the unchanged production PDF suites passed `60` tests with `674` assertions; the final isolated Selenium/Chromium regression passed `1` test with `48` assertions, including a `%PDF-` probe, live unsaved-setting refresh, full-viewport geometry, close/Escape behavior, narrow `390 × 844` behavior, no generated report/file, and no severe console error. Pint passed on all changed PHP/test files, configured PHPStan passed all `249` application files, Blade cache/clear, published-asset synchronization, and `git diff --check` passed. Source and published CSS are byte-identical at SHA-256 `5ed61e4a03137461a617b07abed8b0f5b7095024f65604dfe8464d4978626b39`. The visually inspected proof is `storage/app/qa-artifacts/report-settings-weasyprint-preview-fullscreen-1440x900.png` (`52,070` bytes, SHA-256 `43161266d1b8f9baf1c773ba436fa3a6a4263fed61c7f606ca8c854bd51dd001`), showing the browser's real PDF viewer, eight generated pages, and the accessible full-viewport exit control. No migration, stored setting key, report DTO contract, accepted page plan, immutable generated-report behavior, dependency, route, database, or unrelated setting changed. The complete `scripts/verify.sh`, benchmark, backup/restore, storage audit, full Dusk suite, and manual non-Chromium browser matrix were not run or claimed for this proportional slice.

The report-preview sizing and heading refinement started on 2026-07-25 from the existing uncommitted report-settings/preview slice at commit `9440b355e0f6fc50596ee80536145c04f049c32c`. The authoritative Docker application and Selenium services remain healthy. The requested scope is presentation-only: increase the wide-screen preview share from approximately one third to two fifths, retain the existing single-column narrow layout and fitted multi-page behavior, remove the explanatory sentence, and render the renamed `Anteprima` heading with an explicit application-owned bold style. Report content, preview fixture, settings, shared report Blade, PDF output, persistence, routes, dependencies, and unrelated layout remain unchanged.

The report-preview sizing and heading refinement completed on 2026-07-25. The native Filament wide-screen grid now allocates three fifths to the settings form and two fifths to the preview, increasing the real 1440×900 iframe width beyond `380` px while preserving the one-column full-width narrow layout. `Anteprima stile` is renamed to `Anteprima`; the explanatory sentence is removed from both the Blade and translations; and the heading uses an application-owned 1 rem/700-weight light/dark style rather than relying on unavailable utility generation. The focused authenticated-preview Feature test passed `2` tests with `34` assertions; the isolated Selenium/Chromium regression passed `1` test with `42` assertions, including the enlarged desktop geometry, exact heading text, computed weight `700`, removed old label/description, eight-page vertical preview, narrow behavior, no generated report/file, and no severe console error. Pint passed on all four changed PHP/test files, configured PHPStan passed all `246` application files, Blade cache/clear and `git diff --check` passed, and source/published CSS are byte-identical at SHA-256 `64f41773d0b4fe8f04d74de7e8d071d5659669087871c6917695d433f6add1d7`. The visually inspected enlarged proof is `storage/app/qa-artifacts/report-settings-a4-preview-1440x900.png` (`47,603` bytes, SHA-256 `a5de9ce64659a0bc9bde60a297327365d814061a2c71c204bfef683de27f81e5`): it shows the bold heading, wider A4 preview, and vertical navigator without crop or horizontal overflow. No report content, fixture, setting, shared print view, renderer, snapshot, generated file/record, dependency, route, or unrelated page changed. The complete `scripts/verify.sh` and unrelated suites were not run or claimed for this proportional presentation-only refinement.

The multi-page representative report-preview refinement started on 2026-07-25 from the existing uncommitted report-settings slice at commit `9440b355e0f6fc50596ee80536145c04f049c32c`. The authoritative Docker application and Selenium services remain healthy. Before application changes, `AGENTS.md`, this complete plan, the shared report preview controller/view/styles, `ReportPreviewFactory`, every report DTO used by that factory, the authoritative Italian base-template library, estimate formatting rules, and connected Feature/Dusk coverage were inspected. The clarified requirement retains the fitted portrait A4 frame but requires vertical navigation across every preview page; the current preview-only script deliberately hides all except one sheet. Its deterministic factory also exposes only one Finding with two quote-required solutions, so it does not exercise the template-derived combinations, solution roles, scope/status variants, or monetary presentations needed for a useful preview. This refinement will fit every shared-Blade sheet to frame width, preserve vertical scrolling while preventing horizontal scrolling, and expand only the side-effect-free preview DTO fixture with representative base-template content, multiple Findings and solutions, recommended/implemented/alternative roles, distinct priorities/scopes/statuses, and exact/range/recurring/analysis/quote/bundled estimate labels. Production report construction, templates, saved Findings, renderer, snapshots, generated files, settings persistence, dependencies, and print CSS remain unchanged.

The multi-page representative report-preview refinement completed on 2026-07-25. The A4-ratio iframe now contains all eight shared-Blade pages and exposes native vertical scrolling; each portrait or landscape sheet is independently scaled to the iframe’s current content width, separated by a neutral page gap, and clipped horizontally, so desktop and narrow layouts can traverse the complete report without horizontal navigation. The side-effect-free preview fixture now mirrors three authoritative base-template Findings: `Ripristino dei backup non verificato`, `Servizio RDP esposto direttamente su Internet`, and `Notifiche del NAS non configurate`. Their seven solutions exercise recommended, implemented, and alternative roles; organization and selected-asset scopes; in-progress, planned, and resolved states; normal and overridden priorities; continuation, asset, resolution, and URL-evidence content; and range one-off, exact annual, exact monthly/per-user, analysis-required, quote-required, bundled, and recurring estimate labels. Focused settings, authenticated-preview, and complete editorial-PDF coverage passed `43` tests with `544` assertions; the final isolated Selenium/Chromium regression passed `1` test with `36` assertions at 1440×900 and 390×844, proving eight fitted pages, working internal vertical movement to Finding 2, no horizontal overflow, all representative content, live unsaved-title refresh, no generated report/file, and no severe console error. Pint passed on the changed factory and two test files, configured PHPStan passed all `246` application files, Blade cache/clear and `git diff --check` passed, and source/published CSS are byte-identical at SHA-256 `26d11cb9d48ed536b0bcd5507c8f03960979d7048f3e84b17231184df6b69298`. Visual inspection passed for the top cover proof `report-settings-a4-preview-1440x900.png` (`44,931` bytes, SHA-256 `1ccbb2e5596c278874b21556cd6b690d12978ca09eb3105b6fa3429a0799a6f1`), the internally scrolled Finding-2 proof `report-settings-a4-preview-scrolled-1440x900.png` (`64,950` bytes, SHA-256 `884877661981d22e2d4a560d658a8c38f81ea1fb4b7916939af01296a8daf80e`), and the narrow proof `report-settings-a4-preview-390x844.png` (`15,212` bytes, SHA-256 `2b4413644aba6aee4b1a88af0741a97f800cafb3f1430c6265419f3adcf075b6`) under `storage/app/qa-artifacts`. No production template, saved Finding/solution, migration, report DTO contract, immutable snapshot/file, renderer, PDF print rule, route, dependency, or unrelated setting changed. The complete `scripts/verify.sh`, benchmark, backup/restore, storage audit, and unrelated suites were not run or claimed for this proportional preview-only refinement.

The report-settings consolidation and A4 preview correction started on 2026-07-25 from the existing uncommitted dashboard/navigation slices at commit `efbf4f5b52e043979609f2502acbd8b81a667472`. The authoritative Docker application and Selenium services remain healthy. Before application changes, `AGENTS.md`, this complete plan, both typed settings classes and Filament settings pages, every settings migration, the shared report view/styles, authenticated preview controller/factory/route, Italian settings translations, and connected Feature/Dusk coverage were inspected. The reproduced defects have three bounded causes: the General page still owns a visible `Valori e report` section despite the dedicated Report destination; `assestme.settings.fields.new_page_per_finding` has no Italian translation; and the preview iframe has an arbitrary `72rem` height while rendering every 210/297 mm sheet at its unscaled CSS size, producing browser scrollbars inside the narrow preview column. This slice will move the report-value/output controls to the dedicated Report page while retaining their approved typed `GeneralSettings` ownership and atomic persistence, keep the non-report initial assessment status on General, add the missing translation, and present the same real report Blade as one scaled portrait A4 sheet without iframe scrollbars. Immutable report snapshots/files, renderer, generated-report persistence, settings storage keys, PDF pagination, routes, dependencies, and unrelated settings remain unchanged.

The report-settings consolidation and A4 preview correction completed on 2026-07-25. General now contains application behavior, the non-report initial Assessment status, and evidence size limits; the dedicated Report page contains the former report currency/output/default controls under `Valori e output` and saves them together with native `ReportSettings` inside the existing settings-page transaction while retaining their approved typed `GeneralSettings` keys. `new_page_per_finding` now renders as `Inizia ogni Finding su una nuova pagina`, never as its translation key. The authenticated same-Blade preview now exposes one real portrait sheet inside an iframe whose CSS aspect is exactly `210 / 297`; a preview-only fit transform scales the sheet to the current frame, hides other document sheets without removing them from the shared DOM, disables iframe scrolling, and leaves print media/PDF output unchanged. Focused settings, authenticated-preview, and complete editorial-PDF coverage passed `43` tests with `536` assertions; the isolated Selenium/Chromium regression passed `1` test with `29` assertions at 1440×900 and 390×844, including exact A4 ratios, sheet/frame geometry, one visible sheet, hidden overflow, live unsaved-title refresh, relocated labels, no raw translation key, no generated report/file, and no severe console error. Pint passed on the seven changed PHP files, configured PHPStan passed all `246` application files, Blade cache/clear and `git diff --check` passed, and source/published CSS are byte-identical at SHA-256 `3e14213946a08b1c0ab26f0462972deb3e29f0198fc9c22d68ac5dc7bed5b53b`. The visually inspected proofs are `report-settings-a4-preview-1440x900.png` (`44,849` bytes, SHA-256 `038617f1b9dd7918440eac402e9baa91f8609bcbe7e45637ab0206812745fea3`) and `report-settings-a4-preview-390x844.png` (`15,152` bytes, SHA-256 `b64365c4c13cf070513bb3c55b723542559b67ccde8e9f898e12bcf0fc2af9b3`) under `storage/app/qa-artifacts`; both show a complete proportional A4 cover without internal navigation bars, clipping, distortion, or overflow. No migration, stored setting key, renderer, report DTO/snapshot, generated file/record, PDF pagination, route, dependency, or unrelated setting changed. The complete `scripts/verify.sh`, benchmark, backup/restore, storage audit, and unrelated suites were not run or claimed for this proportional settings/preview slice.

The primary navigation ordering correction started on 2026-07-25 from the existing uncommitted dashboard presentation slice at commit `efbf4f5b52e043979609f2502acbd8b81a667472`. The authoritative Docker application and Selenium services remain healthy. Before application changes, `AGENTS.md`, this complete plan, the panel provider, the Assessment and Company resources, both native clusters, Filament’s locked navigation ordering implementation, and the existing real-browser navigation regression were inspected. The requested top-level order is `Dashboard`, `Assessment`, `Aziende`, `Impostazioni`: the current discrepancy is caused by Assessment alone declaring a named navigation group, which Filament renders after every ungrouped top-level item regardless of item sort. This focused slice will flatten Assessment into the existing primary navigation, assign explicit non-conflicting top-level sort values, strengthen the browser expectation from unordered membership to exact order, and leave the native Aziende children and Impostazioni cluster unchanged.

The primary navigation ordering correction completed on 2026-07-25. Assessment now participates in the ungrouped primary navigation with explicit sort `1`, followed by Aziende at `2` and Impostazioni at `3`; Filament’s native Dashboard retains its locked sort `-2`, yielding the exact visible order `Dashboard`, `Assessment`, `Aziende`, `Impostazioni`. The focused isolated Selenium/Chromium navigation regression passed `1` test with `20` assertions, proving the exact order both on Dashboard and within Impostazioni, the unchanged Sedi/Asset company branch, the unchanged settings sub-navigation, optional asset-scope behavior, and no severe browser-console error. Pint passed on the four changed PHP files, configured PHPStan passed all `246` application files, and `git diff --check` passed. No navigation component, custom menu, dependency, route, authorization, child ordering, domain behavior, or persistence path was added or changed. The complete `scripts/verify.sh` and unrelated suites were not run or claimed for this proportional ordering correction.

The dashboard account-widget removal and application-status layout correction started on 2026-07-25 at commit `efbf4f5b52e043979609f2502acbd8b81a667472` on the clean `develop` branch. The authoritative Docker application and Selenium services are healthy. Before application changes, `AGENTS.md`, this complete plan, `AdminPanelProvider`, the `ApplicationStatus` widget and Blade view, dashboard translations, dashboard Feature coverage, the Milestone 1 browser path, and the retained 1366×768/1920×1080 dark-mode dashboard artifacts were inspected. The reproduced desktop artifact shows the native Account widget rendering the unwanted welcome/logout card and the application-status table relying on unavailable utility classes, collapsing each check’s icon, label, date, and message into poorly aligned fragments. This focused dashboard slice will remove only `AccountWidget`, retain logout through Filament’s authenticated user menu, replace the status table’s utility-dependent layout with application-owned semantic responsive CSS, add visible column context and narrow-viewport stacking, republish the existing Filament asset, and verify success/failure/unknown operational states in Feature and real-browser coverage. Domain checks, persisted operational status, backup/integrity behavior, navigation, dependencies, and unrelated dashboard widgets remain unchanged.

The dashboard account-widget removal and application-status layout correction completed on 2026-07-25. The native welcome/logout card is no longer registered, while logout remains available from Filament’s authenticated avatar menu. Application status now has four explicit semantic columns (`Controllo`, `Stato`, `Ultimo evento`, and `Dettaglio`) at desktop widths and container-responsive labeled rows at narrow widths, with status icons, labels, dates, and messages aligned without horizontal overflow. The original success, failure, warning, and unknown domain states and their persisted operational data are unchanged. Focused Feature coverage passed `5` tests with `34` assertions; the isolated Selenium/Chromium regression passed `1` test with `29` assertions at 1440×900 and 390×844, including exact display geometry, the absent Account widget, failure-state help, no horizontal overflow, and no severe browser-console error. Pint passed on the four changed PHP files, configured PHPStan passed all `246` application files, Blade cache/clear and `git diff --check` passed, and the published/source CSS are byte-identical at SHA-256 `fd144e9c77ea1f189088d25d85dd902b7dff344d3aabaa898cca722cb92887c6`. The visually inspected dark-mode proofs are `dashboard-status-corrected-1440x900-dark.png` (`61,896` bytes, SHA-256 `decaf49df982741db49b63c16f80c1d7e80d93e58744aafc371213b40b601404`) and `dashboard-status-corrected-390x844-dark.png` (`46,247` bytes, SHA-256 `b324a1cc748b68c6bd4516e08d796ec7f7268501d377acc4fcaeb59dda8fbe71`) under `storage/app/qa-artifacts`; both are legible without overlap, clipping, or overflow. No dependency, operational check, backup/integrity behavior, database schema, persistence, navigation, or unrelated dashboard widget changed. The complete `scripts/verify.sh`, benchmark, backup/restore, storage audit, and unrelated suites were not run or claimed for this proportional dashboard presentation slice.

The editorial Finding hierarchy and risk mini-matrix correction started on 2026-07-25 at commit `afa230784c701f39fddad290786051a1b1cb76ff` on the clean `develop` branch. The authoritative Docker application and Selenium services are healthy. Before application changes, `AGENTS.md`, this complete plan, the shared report orchestrator and relevant Finding/context/style partials, Italian report translations, and connected editorial PDF and shared-preview tests were inspected. This focused presentation slice will reverse only the displayed consequence-row order so the compact matrix reads from low at bottom-left to high at top-right, replace arrow glyphs with parallel textual consequence/probability axis labels, geometrically center the selected-cell white marker, and give Finding content a semantic visible H1/H2/H3 hierarchy without changing risk data, immutable snapshots, renderer, persistence, editorial limits, dependencies, or unrelated report composition. Verification will use focused structural and real WeasyPrint PDF tests plus the shared preview browser regression; the complete gate is not implied by this graphical correction.

The editorial Finding hierarchy and risk mini-matrix correction completed on 2026-07-25. The report now displays consequence rows in reverse visual order while retaining the authoritative likelihood order, placing the lowest matrix corner at bottom-left and the highest at top-right. Arrow glyphs are replaced by `Conseguenza` rotated parallel to the vertical axis and `Probabilità` parallel to the horizontal axis; the selected-cell white mark is absolutely centered at 50%/50%. Every Finding uses its title as H1, section/role titles as H2, and solution titles as H3. `Spiegazione del problema` and `Problema` are prominent 11 pt H2 headings, and the former no longer has additional border/padding indentation; an absent explanation still omits its complete block without suppressing the Problem heading. The complete focused editorial report class passed `35` tests with `443` assertions, including real WeasyPrint output and the optional-content failure path; the shared-preview Dusk regression passed `1` test with `12` assertions and no severe browser error; Pint passed on all six changed Blade/test files; configured PHPStan passed over `246` application files; Blade cache/clear and `git diff --check` passed. Read-only real-instance rendering produced the nine-page A4 WeasyPrint `57.2` artifact `storage/app/qa-artifacts/risk-mini-matrix-20260725.pdf` (`1,284,857` bytes, SHA-256 `eba869c50255ad97703e2dd06760d65b75f95b5634d18b64707fe32683177a2e`). Page 4 was rasterized at 160 DPI to `risk-mini-matrix-20260725-page4.png` and visually inspected: headings, axes, corner progression, selected marker, solution, systems, footer, and whitespace are legible without overlap, crop, truncation, or anomalous pagination. No dependency, renderer, DTO, snapshot, persistence, editorial-limit, generated-report record, or unrelated report behavior changed. The complete `scripts/verify.sh`, benchmark, storage audit, backup/restore, and unrelated suites were not run or claimed for this proportional visual slice.

The WeasyPrint report alignment and compact risk-matrix correction started on 2026-07-24 at commit `903fb42b78ef4a4b4df6a24a236b2428b72d951a` on the clean `develop` branch. The preflight confirmed the requested exact HEAD, a clean worktree, healthy authoritative Docker services, PHP `8.3.32`, Laravel `13.19.0`, Filament `5.6.8`, Spatie Laravel PDF `2.12.0`, `pontedilana/php-weasyprint` `2.7.0`, and WeasyPrint `57.2`. Before application changes, `AGENTS.md`, this complete plan, the authorized report orchestrator and partials, `FindingPagePlanner`, every report DTO, and directly connected editorial PDF, generated PDF, shared-preview, and report-data tests were read through EOF. This slice is limited to CSS 2.1-compatible table/block report composition, balanced deterministic Finding solution planning, focused structural/planner/real-PDF/preview tests, one real production-path QA PDF and its complete 160-DPI visual inspection, and factual Progress/Discoveries evidence. Renderer, dependencies, data model, immutable snapshots, persistence, generated-report storage, editorial limits, XLSX, and unrelated application areas remain unchanged.

The WeasyPrint report alignment and compact risk-matrix correction completed on 2026-07-24. Finding headings, bodies, solution metrics, contextual risk/systems composition, overview priority distribution, and landscape summary alignment now use CSS 2.1-compatible table/block structures; the 4 × 4 risk matrix prints colored symbols only and identifies the current cell with a black border and white center. The deterministic planner evaluates every valid split under the existing editorial weights and balances the real three-solution Finding as `2 / 1`, retaining the primary solution first and the conservative `1 / remaining` fallback. Focused report, planner, generated-PDF, and shared-preview tests passed with `64` tests and `620` assertions; focused Dusk passed with `1` test and `10` assertions; Blade compilation, Pint on the three changed PHP files, PHPStan on the changed application PHP file, and `git diff --check` passed. Production-path version `27` is a searchable nine-page WeasyPrint `57.2` PDF with the summary on A4 landscape and every Finding page on A4 portrait; all pages were rasterized at 160 DPI and inspected, including a grayscale proof of the selected risk symbol. No dependency, renderer, DTO, snapshot, persistence, generated-report model, editorial-limit, or unrelated application change was introduced.

The definitive D-009/D-059 WeasyPrint production migration started on 2026-07-24 at commit `bf2c58e54986f44fc65925d7080abc15724c76e0` on the clean `develop` branch. The preflight confirmed the requested exact HEAD and no later commits requiring review. Before application changes, `AGENTS.md`, this complete plan through Final outcome, the spike commit and its complete changed-file set, Composer manifests and locked renderer packages, PDF/environment/Docker configuration, the real snapshot and PDF actions, all report DTOs and Blade partials, the DOMPDF Canvas driver, the complete spike action/command/controller/factory/view/route/tests, typed report settings and their Filament page/current duplicated preview/settings migrations, and the relevant immutable PDF/snapshot/download/settings/preview tests were inspected. The accepted technical baseline is Spatie Laravel PDF `2.12.0`, `pontedilana/php-weasyprint` `2.7.0`, and container WeasyPrint `57.2`; the rejected candidates still present before this slice are DOMPDF `3.1.5`, `chrome-php/chrome` `1.16.1`, and Debian Chromium `150.0.7871.181`. This slice promotes only WeasyPrint, retains the immutable `AssessmentReportData`/`GeneratedReport` persistence contract, implements the approved real report and same-view authenticated preview, calibrates and enforces deterministic editorial budgets and one/two-page Finding plans, removes every spike/DOMPDF/Chrome runtime path, performs focused and visual acceptance, then invokes `scripts/verify.sh` exactly once.

The definitive D-009/D-059 production migration completed on 2026-07-24. WeasyPrint is the sole installed application renderer; DOMPDF, Chrome PHP, application-image Chromium, Canvas chrome, spike actions/routes/factory/views/translations/tests/fixture, and obsolete configuration are removed. The real immutable report now uses named portrait/landscape pages, deterministic one/two-page Finding plans, indivisible solutions, real risk data, compact assets, dedicated evidence, `show_resolution`, and one Blade/CSS implementation shared with the authenticated unsaved-settings iframe preview. Focused Docker verification passed Pint on `313` files, PHPStan on `246` files, the final rerun of every test class affected by the unsupported-host aggregate errors with `79` tests and `914` assertions except exactly the two known baselines, the focused preview Dusk test with `1` test and `10` assertions, locked Composer audit with no advisories, Canary strict `33/33`, diagnostics, storage audit, and the 50-Finding benchmark (`0.2090` seconds first render, `5.5014` seconds PDF, `0.2496` seconds XLSX). The single `scripts/verify.sh` invocation exited `2`: it was mistakenly started on the host while `.env` intentionally names Docker-only `/workspace` and `/usr/bin/weasyprint` paths, producing `41` cascading environment failures plus the two known baselines before the aggregate could continue. It was not repeated. Running only the failed classes through the authoritative Compose app container eliminated all environment failures and reproduced solely the absent `.github/workflows/quality.yml` and stale Finding-column-N XLSX expectation. No new application regression remained.

The bounded D-009/D-059 Chrome-first renderer spike started on 2026-07-24 at commit `c94047388bdfff6768ba72e9e1a65fc1112b9144` on the clean `develop` branch. The preflight confirmed that HEAD exactly matches the requested starting commit, so no later commit required review. Before implementation, `AGENTS.md`, this complete plan through Final outcome, all locked Composer packages, PDF/Docker/environment configuration, the production PDF and snapshot actions, `DomPdfCanvasDriver`, every report DTO and Blade partial, the report-settings preview/page, relevant PDF/snapshot/persistence/download/browser tests, and the installed Spatie Chrome driver were read through EOF. The authoritative runtime is PHP `8.3.32`, Spatie Laravel PDF `2.12.0`, and DOMPDF `3.1.5`; Chromium and `chrome-php/chrome` are absent before the spike. Source inspection confirms that the locked official Chrome driver sends global paper dimensions when the builder selects a format and does not send CDP `preferCSSPageSize`; this is an identified risk, not yet a declared blocker, and will be tested with the real provisioned Chromium before any approved fallback is considered. The configured database baseline is SHA-256 `ef7c1c2e445459993fea8ad86f8678f2f8fb98c1bb724d237e161f8d86d75888`; the relative path, size, and timestamp inventory of existing immutable report files hashes to `f62a748f5645de731927ba450e3627a279ae05a600a40564ce6712bacc68a5ae`. The slice is limited to a deterministic DTO-backed proof view, isolated local/testing generation and authenticated preview, focused executable evidence, development-only renderer tooling, and factual plan evidence. The global `dompdf` default, production generation action, Canvas driver, immutable report records/files/snapshots, versioning, hashing, history, storage, and downloads remain unchanged.

The bounded D-009/D-059 renderer spike completed on 2026-07-24. The official Spatie `2.12.0` Chrome driver was exercised first with `chrome-php/chrome` `1.16.1` and Debian Chromium `150.0.7871.181`: it preserved eight pages and mixed orientation but emitted US Letter MediaBoxes (`612 × 792` portrait and `792 × 612` landscape), failing the blocking A4 criterion. One targeted diagnosis used the same HTML, Chromium, and installed `chrome-php` API with CDP `preferCSSPageSize=true`; it emitted the required eight A4 pages (`594.96 × 841.92` portrait and the inverse landscape), proving that CSS, browser version, and binary configuration are valid and that the official Spatie driver’s absent option is the functional blocker. No custom driver, vendor patch, or extended Chrome workaround was created. The approved fallback reused the same DTO/view with Spatie’s official WeasyPrint driver, `pontedilana/php-weasyprint` `2.7.0`, and Debian WeasyPrint `57.2`; it passed all blocking criteria and is selected for the later production migration, while Chrome remains installed only to keep its failure independently reproducible.

The selected WeasyPrint proof is `storage/app/qa-artifacts/d059-c940473/weasyprint-renderer-proof.pdf`: eight pages, pages 1–2 and 4–8 A4 portrait, page 3 A4 landscape, `126,134` bytes, SHA-256 `021a6eb5367fc3b0464b3eec21161ae49fc5da2b2901515a8d4a8a0b33ac0065`, and `1.0840` seconds measured generation time. Poppler `22.12.0` extracted every section and `SOL-*` marker, found the continuation heading on physical page 6, confirmed cover without a page number and physical page 2 numbered `1`, and listed one significant `960 × 720` JPEG only. All eight 150-DPI raster pages and the screen-media preview were inspected: format, margins, footer, landscape distinction, text, risk matrix, asset metadata, evidence, indivisible solutions, and continuation are legible without overlap or truncation. Focused spike tests passed with `4` tests and `95` assertions; focused report DTO and immutable DOMPDF snapshot coverage passed with `3` tests and `32` assertions. The snapshot object, `GeneratedReport` count, and immutable report directory remain unchanged by spike generation; the preflight report inventory hash remains exactly `f62a748f5645de731927ba450e3627a279ae05a600a40564ce6712bacc68a5ae`. DOMPDF remains the global/default production driver, and `GenerateAssessmentPdf`, `DomPdfCanvasDriver`, persisted snapshots, versioning, storage, history, and downloads remain untouched.

The spike’s single final `scripts/verify.sh` invocation validated Composer and passed Pint over `313` files, then its PHPStan process terminated with exit `130` before reporting a static-analysis error. The exact failed stage was rerun as required and passed over `245` files with zero errors. Resuming the isolated quality stages passed Pint and PHPStan again and reported `314` application tests and `2,808` assertions passing; its three failures were the spike-related Docker topology assertion that still prohibited Chromium, plus the two already-recorded baseline failures for the absent `.github/workflows/quality.yml` and stale Finding-column-N XLSX expectation. The topology assertion was aligned with the approved development runtime and its focused rerun passed with `52` assertions; the two unrelated baseline failures were not changed. Canary strict passed `33/33` with zero failures or skips. The locked audit then failed on six new Dompdf advisories affecting the unchanged production `3.1.5` and fixed upstream in `3.1.6`; production-renderer maintenance was not folded into this spike. The remaining isolated checks were completed independently: diagnostics, migrations, storage audit, route inventory, and application metadata passed; the 50-Finding benchmark passed every threshold with `0.3636` seconds first render, `3.5827` seconds PDF, and `0.2720` seconds XLSX; aggregate Dusk passed `14` tests and `527` assertions with no reported console error. The complete aggregate gate is therefore not green because of the two pre-existing test failures and current Dompdf advisories, not because of the selected proof.

The PDF renderer architecture review started on 2026-07-24 at commit `73fa2eaaeacb2d742020811074699405b6c9a41b` on a clean `develop` branch. This is a documentation-only decision update: no renderer spike, PDF generation, package installation, dependency or lock-file change, Docker change, Blade/layout edit, test edit, or application-code change is authorized. Before updating the decision, `AGENTS.md`, this complete plan, both Composer manifests, the PDF configuration, generation and snapshot actions, application-owned DOMPDF Canvas driver, complete report Blade/partial tree, report-settings preview and settings class, Docker development profile, reporting/Docker/CloudPanel decisions, and relevant PDF/preview tests were read through EOF. The locked production implementation remains Spatie Laravel PDF `2.12.0` with DOMPDF `3.1.5`; `chrome-php/chrome` is not installed. Upstream primary documentation and source are being used to bound a future deterministic renderer spike without claiming renderer acceptance.

The PDF renderer architecture review completed on 2026-07-24 as a documentation-only update to `AGENTS.md` and this authoritative decision log. D-009 (v1), D-052, and D-059 (v1) retain their historical text and are explicitly superseded where incompatible; current DOMPDF production behavior remains unchanged while D-009/D-059 define the bounded Chrome-first validation and approved escalation order. The new report composition, renderer blockers, single-snapshot VIP Estintori spike, same-view future preview, and immutable persistence constraints are recorded without selecting or running a new renderer. `git diff --check` passed; the targeted contradiction scan found none of the obsolete unqualified clauses, and no repository Markdown lint configuration exists. No application test, Dusk run, package installation, Docker command, PDF generation, or complete verifier was run for this documentation-only change.

The editorial PDF composition and three-solution-limit correction started on 2026-07-24 at commit `0bf7ea9de7f27099574e7b5699bc16866d70b5c3` on a clean `develop` branch. Before application changes, `AGENTS.md`, this complete plan, both Composer manifests and all 188 locked packages, the report orchestrator/styles/overview/Finding/asset/summary/legend/evidence partials, the report-settings preview, report DTOs, snapshot/PDF/Canvas implementations, Finding and template forms/save actions, template schema/import validation, Italian translations, and all tests covering reports, solutions, templates, import, workspace persistence, and relevant browser behavior were read through EOF. The running Compose application and Selenium services are healthy. Read-only inspection found no Finding or template with more than three solutions in the configured local database; the observed maxima are one solution for each aggregate. The seeders create one solution per template/Finding. Two JSON fixtures conflict with the new bound because `valid-all-branches.json` and `invalid-nonmonetary-amount.json` each place five solutions in one template. No fixture solution will be truncated or deleted: the valid fixture's existing eleven solutions will be redistributed losslessly so every template has at most three, and the invalid fixture will retain all existing solution rows across schema-valid template groupings before its intended semantic rejection is asserted. Existing generated reports and immutable snapshots will not be modified. The current DOMPDF presentation emits every solution without slicing; its section heading uses an ambiguous fixed-layout second column and a horizontal pseudo-element, its status glyph/text are separate inline nodes, and its management-note border is overridden with the priority color. The existing VIP Estintori regression proves an exact eight-page cover/overview/summary/four-Finding/evidence allocation but does not yet exercise three solutions in one Finding. This slice will retain the Blade/DOMPDF/Canvas architecture and evidence/XLSX behavior, enforce `array|max:3` and native Repeater/schema bounds at every mutable entry point, update structural and real-render tests, generate a new immutable VIP Estintori report, rasterize every page at 150 DPI, inspect all pages, and invoke the aggregate verifier exactly once after focused acceptance.

The editorial PDF composition and three-solution-limit correction completed its in-scope implementation and real-output acceptance on 2026-07-24. `Panoramica Assessment` now uses an unambiguous auto-layout 24 mm number column and title cell with a primary vertical divider and no horizontal accent; compact KPI, neutral two-column assessment metadata, optional narrative, the single VAT note, inline legend, and optional consultant details follow in the approved order without fabricated content. Finding status and priority are non-breaking inline tokens; the management note and recommended solution share the configured primary-color editorial accent; scope, the three-column consequence–likelihood–resulting-priority evaluation, and compact assets avoid administrative boxes; an asset whose name equals its type case-insensitively is printed only once. All solutions continue to be emitted without renderer slicing, and alternatives remain individually movable when short while oversized content can flow naturally. The maximum of three total solutions is enforced by both Filament Repeaters with Italian guidance, explicit server-side array bounds in Finding and template saves, the completion validator, JSON Schema `maxItems`, and schema-backed import; the existing exactly-one-recommended-on-completion and at-most-one-implemented rules remain intact. No local aggregate was incompatible. The two incompatible fixtures were corrected without deleting or truncating a solution: all eleven valid-fixture solutions and all five intentionally invalid-fixture solutions remain present, redistributed among template aggregates that each satisfy the new structural bound. Modified files are `app/Actions/Assessments/SaveFindingDetails.php`, `app/Actions/Assessments/ValidateAssessmentCompletion.php`, `app/Actions/Templates/SaveFindingTemplate.php`, `app/Filament/Resources/Assessments/Schemas/FindingEditorSchema.php`, `app/Filament/Resources/FindingTemplates/Schemas/FindingTemplateForm.php`, `fixtures/imports/invalid-nonmonetary-amount.json`, `fixtures/imports/valid-all-branches.json`, `resources/lang/it/assestme.php`, `resources/views/filament/pages/report-settings-preview.blade.php`, `resources/views/reports/assessment.blade.php`, `resources/views/reports/partials/asset-details.blade.php`, `resources/views/reports/partials/finding-detail.blade.php`, `resources/views/reports/partials/findings-summary.blade.php`, `resources/views/reports/partials/overview.blade.php`, `resources/views/reports/partials/report-styles.blade.php`, `schemas/finding-template.schema.json`, `tests/Browser/ReportSettingsPreviewTest.php`, `tests/Feature/AssessmentDomainTest.php`, `tests/Feature/EditorialPdfReportTest.php`, `tests/Feature/FindingTemplateCrudTest.php`, `tests/Feature/JsonSchemaProofTest.php`, `tests/Feature/TemplateImportExportTest.php`, `tests/Feature/WorkspacePageTest.php`, `tests/Feature/WorkspacePersistenceTest.php`, and this `plan.md`. Focused report/evidence tests passed with `53` tests and `634` assertions; focused solution-limit/template/import/workspace tests passed with `63` tests and `289` assertions; Pint passed on the `14` changed PHP files; scoped PHPStan passed with no errors; the isolated report-settings-preview Dusk test passed with `1` test and `44` assertions, no severe browser log, and no generated report or file. Real QA used the existing four-Finding VIP Estintori assessment, NAS association, image evidence, new-page-per-Finding setting, compact legend, costs, and three realistic solutions on one Finding, persisted through the signed Finding save protocol. It created new immutable report `22`, version `21`, without overwriting prior output, at `storage/app/private/reports/2/089ff9fc-3396-4cf2-90c4-2dc89d7d0975.pdf`: DOMPDF `3.1.5`, PDF `1.7`, `9` physical A4 pages including the cover, `2,926,200` bytes, SHA-256 `dd7de62debe19a51f763532dc4cf1d40e954a7c2e2f14226060942452b7f4d43`. Every 150-DPI raster in `storage/app/qa-artifacts/vip-estintori-editorial-three-solutions-v21/` was inspected: page 1 is the intact cover; page 2 has the grouped vertical-divider overview without a horizontal accent; page 3 retains the approved summary; pages 4–6 show inline status tokens, primary management-note hierarchy, recommended-plus-two-alternative ordering, natural continuation, compact risk/assets, complete footers, and no omission, truncation, overlap, or anomalous blank page; page 7 is the third Finding; page 8 retains the existing image-evidence order, rendering, caption, and pagination; page 9 is the fourth Finding. Canvas header/footer geometry is intact on all internal pages. The single final `scripts/verify.sh` invocation validated Composer, passed Pint on `306` files and PHPStan on `240` files, and then reported `311` passed application tests with `2701` assertions plus exactly the two pre-existing unrelated baseline failures: missing `.github/workflows/quality.yml` in `DeploymentConfigurationTest` and the stale Finding-column-N expectation in `GeneratedWorkbookReportTest`. It therefore exited `2` before Canary, diagnostics, benchmark, storage audit, and aggregate Dusk, and was not repeated. No evidence implementation, Canvas driver, immutable snapshot/DTO/build pipeline, XLSX implementation, dependency manifest/lock, vendor file, renderer, or production dependency was modified.

The D-059 editorial-risk visual correction started on 2026-07-23 at commit `5d2578a87737de54da890ab702df6e18cc06ff74` on clean branch `develop`, aligned with `origin/develop`. The complete specification, report orchestrator and requested report partials, Finding report DTO, snapshot builder, report-settings HTML preview, Italian report translations, and existing editorial PDF/preview tests were read before application changes. This slice is limited to the approved Blade/CSS presentation: single aligned section-heading accent, reduced horizontal rules, glyph-based priority/status markers, compact/detailed legend variants, a connected consequence–likelihood–priority risk evaluation with separately presented scope, editorial Finding metadata, lighter solution metrics, and compact assets. Existing models, database, enums, DTO/snapshot contracts, settings, renderer, Canvas geometry, page margins/chrome, logos, evidence ordering, asset pagination rules, `new_page_per_finding`, dependencies, and unrelated tests remain unchanged. Verification will use focused editorial PDF/Blade and preview coverage, Pint on changed PHP files, scoped PHPStan only if PHP application code changes, one real four-Finding VIP Estintori PDF with one NAS and one image, complete PNG rasterization and inspection, followed by one final `scripts/verify.sh` invocation only after focused checks pass.

The D-059 editorial-risk visual correction completed on 2026-07-23 without changing models, database, enums, DTOs, snapshot construction, settings, renderer, Canvas driver, margins, page chrome, logos, evidence ordering, asset pagination rules, `new_page_per_finding`, dependencies, or unrelated tests. The report and settings preview now use one short aligned section-title accent; lighter KPI, assessment-information, legend, summary, Finding, solution-metric, and asset structures; DejaVu Sans priority/status glyphs; compact and detailed priority legend variants; separate scope metadata; and one consequence–likelihood–priority risk-evaluation block with the applicable resulting/manual-priority label and a non-duplicated manual rationale. Focused PDF/data coverage passed with `35` tests and `479` assertions, including real DOMPDF generation, the stable eight-page four-Finding/NAS/image composition, asset/evidence continuity, risk/scope/manual-priority semantics, and glyph rendering; the focused settings-preview Dusk check passed with `1` test and `35` assertions, no severe console entry, and no generated report; targeted Pint passed for all three PHP test/translation files. Scoped PHPStan was not separately applicable because no PHP application class changed. Real QA generated immutable report `19`, version `18`, at `storage/app/private/reports/2/27d51349-f840-49cc-9cb5-1557b641c434.pdf`: DOMPDF `3.1.5`, `8` A4 pages, `2,924,338` bytes, SHA-256 `9aefca08315c5b2485db488c6a56dfe20fa7c71c4f8a121f1eade394496c0c87`. All eight 150-DPI PNGs in `storage/app/qa-artifacts/vip-estintori-editorial-v18/` were inspected: page 2 has top-aligned section number/title, one short accent, borderless KPI row, glyph markers, and an unruled compact legend; Finding pages connect the three risk values, separate scope, remove “Classificazione” and boxed indicators, lighten metrics, and compact NAS metadata; the image-evidence page, asset attachments, Canvas header/footer, and eight-page pagination remain free of orphaning or overlap. The single final `scripts/verify.sh` invocation validated Composer, passed Pint on `306` files and PHPStan on `240` files, then stopped after `299` application tests passed and the same two unrelated baseline failures recorded below remained: missing `.github/workflows/quality.yml` in `DeploymentConfigurationTest` and the stale Finding-column-N estimate assertion in `GeneratedWorkbookReportTest`; later Canary, diagnostics, benchmark, storage-audit, and Dusk phases therefore did not run, and the aggregate gate was not repeated.

The D-059 editorial PDF composition correction started on 2026-07-22 at commit `c4d458f51e6529eb5b1b2740de51dcabea1819c1` on clean branch `develop`, aligned with `origin/develop`. The complete specification, current report orchestrator/partials, Canvas driver, PDF action, Finding DTO, and all editorial tests introduced by that commit were read before application changes. Confirmed defects are restricted to Canvas/content geometry, section and Finding title flow, duplicated classification, asset pagination, evidence filename/title presentation, summary visual hierarchy, compact legend behavior, and insufficient composition regression coverage. The existing editorial Blade architecture, DOMPDF Canvas, settings contract, immutable snapshot/persistence pipeline, included-evidence ordering, `new_page_per_finding`, renderer, dependencies, models, and report fields remain authoritative. Verification will use focused report tests, Pint, scoped PHPStan, one real VIP Estintori PDF with four Findings, one NAS asset, one image evidence and new-page-per-Finding enabled, complete PNG rasterization and review of every page, followed by one final `scripts/verify.sh` invocation.

The D-059 editorial PDF composition correction completed on 2026-07-22 without changing dependencies, renderer, settings, models, DTO contracts, persistence, or the existing report architecture. Canvas and Blade now reserve disjoint 13 mm top and 23 mm bottom page bands with 20/17 mm left/right content margins; Canvas draws header text at 3 mm, its rule at 10 mm, the footer rule 20 mm above the page bottom, and footer/page text 14 mm above the bottom, while the cover remains excluded from repeated chrome. Finding headings use an independent number line and full-width 26/22/19 pt title scale; classification contains scope, consequence, likelihood, and only an applicable manual-priority rationale; the category remains only in its badge. Asset collections may paginate, while their heading and first compact asset plus every individual later asset remain indivisible. Evidence keeps `includedEvidence()` order and suppresses a title equal to the original filename case-insensitively. The overview compact legend retains `priorityCounts()`, and the two-row summary retains all approved fields without CRUD-style boxes or vertical dividers.

Accepted focused evidence is 42 report/DTO tests with 488 assertions, Pint clean over the four affected PHP/test files and the final changed fixture, scoped PHPStan with zero errors over the PDF action and Canvas driver, and a real immutable VIP Estintori PDF v14 at `storage/app/private/reports/2/22b74ba0-9159-421b-94ee-383837426758.pdf` (2,925,286 bytes, SHA-256 `3a667af1ddb4c3d49d0befcb438eb3e931162ba954b6ef6b44cec99f52766eb8`). Poppler installed only in the disposable running development container rasterized all eight A4 pages at 150 dpi to `storage/app/qa-artifacts/vip-estintori-editorial-final/page-01.png` through `page-08.png`; complete visual review plus PDF text/coordinate and bitmap-band checks prove the cover, overview, summary, Findings 01–03, evidence, and Finding 04 allocation, repeated chrome on all seven internal pages, no technical image filename, and no orphan asset page/header. The single final `scripts/verify.sh` invocation passed Composer validation, Pint over 306 files, PHPStan over 240 files, and 288 application tests/2,500 assertions, but stopped with three failures: the two unchanged baseline failures for missing `.github/workflows/quality.yml` and the Finding-workbook N-column expectation, plus a random `AssetFactory` IP that made the new composition fixture nine pages. The fixture was made deterministic for the specified NAS fields and the failed composition test then passed with 29 assertions; the complete focused 42-test set passed again. The aggregate gate was not rerun, and its later Canary, diagnostics, benchmark, storage audit, Composer audit, and Dusk stages were therefore not executed.

The D-059 single editorial assessment-report layout slice started on 2026-07-22 at commit `e90f3a5` on clean branch `develop`, aligned with `origin/develop`. The Docker `app` and optional `selenium` services are healthy, the real login endpoint returns HTTP 200, and the locked runtime is PHP 8.3.32, Laravel 13.19.0, Filament 5.6.8, Livewire 4.3.3, Spatie Laravel PDF 2.12.0, and DOMPDF 3.1.5. The authorized scope is the one-layout A4 Blade presentation, report-setting helper copy and same-page unsaved-state HTML preview, deterministic report DTO helpers, Canvas page chrome, complete removal of `confidentiality_label` through a forward settings migration, Italian translations, focused PDF/settings/snapshot/DTO/Dusk coverage, at most three untracked QA images, and this authoritative decision/progress record. Generated-report persistence, existing immutable snapshots/files, hashing, versioning, history, authenticated download, database models/tables, dependencies, renderer, and XLSX behavior remain unchanged. Before implementation the only modified file is this `plan.md`; no pre-existing unrelated working-tree change was present. Verification will proceed through focused Docker tests, Pint on changed PHP, scoped PHPStan, one isolated Dusk preview test, and a single final `scripts/verify.sh` only after focused checks pass.

The D-059 implementation completed on 2026-07-22. `resources/views/reports/assessment.blade.php` is the readable orchestrator for centralized `report-styles`, cover, overview, content index, priority legend, per-Finding summary tables, Finding detail, mixed-order evidence, consultant information, and final sections. The single A4 layout uses DejaVu Sans, the approved neutral palette, the configured primary color only as an identity accent, preserved priority colors, DOMPDF-compatible tables/block flow, deterministic long-title classes, page-safe blocks, and no decorative or unsupported layout mechanism. Cover logo ownership follows consultant/client/both branding; overview KPIs follow snapshot legend order; summary blocks remain independently breakable; Finding pages preserve every supported solution, classification, asset, technical/resolution, estimate-note, methodology, disclaimer, signature, attachment, consultant, index, and legend datum; evidence is emitted once in its stored image/URL/file order without cropping or internal paths. Canvas remains the only repeated header/footer implementation and uses snapshot-only fallbacks, neutral rules, no centered text, and no confidentiality.

The settings slice removes `confidentiality_label` from the typed settings, Filament form, new snapshots, PDF generation, Canvas, translations, stubs, and related assertions. Forward settings migration `2026_07_22_000020_remove_report_confidentiality_label` ran successfully in the configured Docker application; existing generated reports and their immutable snapshots/files were not rewritten. The report-settings page retains the existing settings, improves the requested Italian labels/helper text, and uses a native Filament `View` beside the form for one sticky responsive unsaved-state preview. Only the approved fields are live; local Alpine switches cover/internal views, while the preview creates no PDF, generated-report row, snapshot, temporary file, iframe, poll, nested Livewire component, global JavaScript, or additional dependency.

Files modified by D-059 are `app/Actions/Reports/BuildAssessmentSnapshot.php`, `app/Actions/Reports/GenerateAssessmentPdf.php`, `app/Data/Reports/AssessmentReportData.php`, `app/Data/Reports/ReportFindingData.php`, `app/Filament/Pages/ReportSettingsPage.php`, `app/Services/Reporting/DomPdfCanvasDriver.php`, `app/Settings/ReportSettings.php`, `config/laravel-pdf.php`, `database/settings/2026_07_22_000020_remove_report_confidentiality_label.php`, `resources/lang/it/assestme.php`, `resources/views/filament/pages/report-settings-preview.blade.php`, `resources/views/reports/assessment.blade.php`, the new report partials `consultant-information`, `content-index`, `cover`, `final-sections`, `finding-detail`, `finding-evidence`, `findings-summary`, `overview`, `priority-legend`, and `report-styles`, `stubs/reports/assessment.blade.php.stub`, `stubs/reports/dompdf-page-numbers.php.stub`, `tests/Browser/ReportSettingsPreviewTest.php`, `tests/Feature/EditorialEvidencePdfTest.php`, `tests/Feature/EditorialPdfReportTest.php`, `tests/Feature/GeneratedPdfReportTest.php`, `tests/Feature/SettingsFoundationTest.php`, `tests/Unit/ReportDataTest.php`, and this `plan.md`. No dependency manifest/lock, vendor file, model, application table, renderer, generated-report persistence/download implementation, XLSX implementation, or unrelated feature was changed.

Accepted focused evidence is 85 relevant Unit/Feature tests with 1,303 assertions; real `%PDF-` and parser checks for all 18 requested editorial scenarios plus mixed included evidence with real vertical/horizontal images and captions on/off; the existing real 50-Finding PDF path; Pint clean over 16 changed/new PHP files; PHPStan clean over eight changed application/migration files; successful compiled-view cache/clear; and one isolated Chromium Dusk test with 23 assertions covering unsaved title, color, header, and footer changes, zero generated reports/files, and zero severe console errors. Manual visual review retained exactly three non-versioned PNGs under `storage/app/qa-artifacts`: `editorial-cover.png` (21,113 bytes), `editorial-findings-summary.png` (30,883 bytes), and `editorial-finding-evidence.png` (24,511 bytes). They render the actual Blade with the same snapshot used for a real parsed six-page PDF; the headless Chromium PDF viewer returned blank raster captures, so exact DOMPDF raster geometry is not claimed.

The single final `scripts/verify.sh` invocation validated Composer, passed Pint over 306 files, passed PHPStan over 240 files, and reached 285 passing application tests/2,459 assertions before stopping on two pre-existing unrelated failures. `DeploymentConfigurationTest` reads `.github/workflows/quality.yml`, which is absent from the current `HEAD` and was already recorded as removed before this slice; `GeneratedWorkbookReportTest` expects the estimate in Finding column N although the unchanged authoritative workbook builder writes estimate to M and status to N. The workbook failure was reproduced once in isolation before the final gate and was not corrected because XLSX is outside D-059. The stopped gate did not execute its later Canary, diagnostics, benchmark, storage-audit, Composer-audit, or complete Dusk stages and was not repeated. Manual Edge, Firefox, iOS Safari, and Android Chrome QA, pixel-perfect comparison, and exact raster review of DOMPDF pages were not executed or claimed.

Assessment-workspace live-field scroll-continuity remediation started on 2026-07-22 as a focused extension of D-058. The reported reproduction changes a reactive Finding field such as Tipo stima after scrolling away from the editor origin; Livewire then morphs conditional form content and the visible position returns to its default. The investigation is limited to preserving the current navigator, sequential form, dominant editor, contextual-properties, and document scroll positions across a morph of the existing workspace component. Intentional selection/navigation resets, validation focus, native conditional fields, shared `findingData`, signed persistence, and all previously accepted D-058 presentation behavior remain unchanged. A focused isolated Dusk regression will reproduce the reactive select at a non-zero scroll position on both desktop split and narrow sequential layouts before acceptance. No dependency, migration, schema, model, persistence action, vendor file, alternate form implementation, or new document is introduced.

The D-058 live-field scroll-continuity remediation completed its proportional verification on 2026-07-22. The application now captures one immutable scroll snapshot at Livewire's complete `morph` boundary and restores navigator, sequential form, dominant editor, contextual-properties, and document positions after the complete `morphed` boundary and Filament's two-frame control stabilization. A subsequent Finding selection or validation failure cancels the scheduled restoration so intentional navigation and invalid-field focus remain authoritative. Workspace recognition covers both the Finding workbench, including navigator-only narrow state, and the separate assessment-details save-status surface. The native reactive schema and conditional Tipo stima fields are unchanged; only a stable Dusk selector was added to that native Select. The officially published JavaScript is byte-identical to source at SHA-256 `4ba4aef69b49028d1a564ccfa56addc89fdde54dcad278cc5f95780e179b0748`; the unchanged source/published CSS remains byte-identical at SHA-256 `e7b3c010cad82f40117d01c1bfb0fc9edd81a0401bb8212aab07f13def16780a`.

The baseline isolated Chromium regression failed visibly because changing Tipo stima from a scrolled desktop position changed editor `scrollTop` from `950` to `0`. After remediation the same test passes both a 1440×900 split editor and a 1280×800 sequential form with their non-zero positions unchanged within two pixels. Accepted focused evidence is Pint clean over five affected PHP/test files, PHPStan clean over the changed schema class, 12 workspace Feature tests/73 assertions, and the final isolated browser group with 7 tests/235 assertions covering scroll continuity, responsive geometry, search and keyboard enhancement after morphs, selection resets, validation focus, unsaved-warning clearing, save/conflict behavior, lifecycle, and zero severe console errors. The complete `scripts/verify.sh`, benchmark, backup/restore, Canary, diagnostics, storage audit, report suites, and manual cross-browser/device QA were not run or claimed for this focused interaction correction. No dependency, lock file, migration, model, persistence action, vendor file, or report implementation changed.

Assessment workspace navigator, properties scrolling, summary-tab removal, and generated-file history refinement started on 2026-07-22 at commit `c035574a65d078a15dc6875e8a6184fdf53a1cf8` on clean branch `develop`. The slice is limited to the existing D-055/D-056 workbench presentation: it keeps Filament Table as the authoritative Finding navigator, retains native search through a compact icon that expands on interaction, removes the cramped optional filters, gives `Nuovo finding` a centered dedicated row, presents native reorder as an icon-only action beside search without hiding row titles, and prevents navigator text overlap while scrolling. The selected-Finding properties area will retain the one shared available-height contract with fixed workbench chrome, compact collapsible groups including Stato e report, automatic reveal of newly expanded lower content, and an independently scrollable body, avoiding page-level horizontal overflow. The obsolete summary preview tab is being removed because it has no domain operation, immutable snapshot, or report contract beyond the superseded pre-workbench tab list; PDF/XLSX generation and generated-file history remain unchanged. Generated files will receive an application-owned responsive history presentation with aligned metadata and actions. No dependency, migration, model, persistence action, report generator, vendor file, Node.js toolchain, alternate grid, or additional Markdown document is introduced.

The D-058 assessment-workspace refinement completed its proportional verification on 2026-07-22 without running or claiming the complete acceptance gate. The compact navigator now gives `Nuovo finding` a centered dedicated row, keeps the native Filament search as a 36 px icon control that expands only on click/focus or while a query exists, removes all navigator filters, places the native icon-only reorder control in the same toolbar row, and confines scrolling to the record body. Reorder mode retains the complete Finding summary cell and readable saved title. The contextual column is an ordinary bounded aside rather than an outer HTML `details`; Stato e report, Classificazione, Si applica a, Rischio, and Risoluzione are native compact collapsed Filament sections, and opening a lower section scrolls its revealed content into view inside the properties body. `Anteprima riepilogo` and its non-report preview method/translation were removed. File Generati now has explicit archive context, a semantic fixed-column table, aligned native download/delete actions, and contained horizontal overflow only where a narrow viewport requires it. The published assets are byte-identical to source after `filament:assets`: CSS SHA-256 `e7b3c010cad82f40117d01c1bfb0fc9edd81a0401bb8212aab07f13def16780a`, JavaScript SHA-256 `6e38fab13bd2b357c76e4ca01acd4f7b545e56f6f0531b78794eec8196f22dfa`.

Accepted focused evidence is Pint clean over the eight changed PHP/test files, PHPStan clean over the three changed application classes, and 32 relevant Feature tests/219 assertions covering workspace rendering/search/query bounds and real generated-report success/failure paths. The isolated Docker/Selenium browser selection passed 7 tests/253 assertions across `MilestoneZeroTest`, `MilestoneThreeWorkspaceTest`, `MilestoneFourPdfTest`, and `WorkspaceResponsiveTest`, including click-to-expand search on desktop and narrow viewports, search filtering/reset, aligned toolbar geometry, navigator-only scrolling, saved titles during native reorder, all five collapsed property groups, automatic lower-section reveal with real internal overflow, semantic generated-file alignment, real PDF/XLSX history access, and zero severe console errors. Retained non-versioned visual evidence includes `workspace-list-1440-light.png`, `workspace-inspector-1440-light.png`, `workbench-after-1920x1080.png`, `workbench-narrow-editor-390x844.png`, and `generated-files-refined-1920x1080-dark.png`. `scripts/verify.sh`, the complete application/browser suites, benchmark, backup/restore, Canary, diagnostics, storage audit, and manual Edge/Firefox/iOS Safari/Android Chrome QA were not run or claimed for this presentation slice. No dependency, lock file, migration, model, persistence action, report generator, vendor file, or commit changed.

The D-057 implementation slice completed its proportional verification on 2026-07-22 without running or claiming the complete acceptance gate. Native `AssetCluster` and `SettingsCluster` are discovered alongside the existing resources/pages, use top-positioned native sub-navigation, suppress duplicate main-sidebar entries, and redirect only to their first real destinations. Cluster membership changes Asset URLs to `/admin/asset/assets` and `/admin/asset/asset-types`; settings destinations now use `/admin/settings/...`. All affected application and test destinations use Filament class URL generation or named routes. Tag runtime classes, relationships, state, validation, persistence, import/export/preview, schema/fixtures, report snapshots, PDF presentation, workbook column, translations, policies, and tests were removed. Forward migration `2026_07_22_000019_remove_tags` drops `finding_tag`, `finding_template_tag`, then `tags`, and recreates the original dependency-safe schema in `down()`. The configured non-reference database was deliberately not mutated and therefore reports this new migration as pending; both upgrade and fresh-install proofs used marked disposable file-SQLite environments.

Accepted focused evidence for the slice is: Pint clean over all 54 changed/new PHP files; PHPStan clean over all 31 changed/new application and migration files; 147 relevant Feature tests with 1,314 assertions; the focused immutable workbook test with 57 assertions; two focused PDF/scope tests with 25 assertions; valid parsing of every changed JSON fixture and schema proof that Tag is absent while `additionalProperties` remains false; isolated `migrate:fresh --seed` success; isolated upgrade success from populated Tag tables (`tags=1`, `finding_template_tag=1`, `finding_tag=1`) to all three tables absent; Canary strict with 33 passed, zero failed, zero needs-auth, and zero skipped; and focused Dusk with one test/20 assertions covering real sidebar clicks, native hierarchy/sub-navigation, cluster destinations, cancel routing, absence of Tag navigation, scope-dependent Asset visibility/required state, validation failure, and zero severe console errors. `scripts/verify.sh`, the complete Dusk suite, 50-Finding benchmark, backup/restore suite, complete PDF/XLSX suites, and full storage audit were intentionally not run; focused success is not final acceptance. No dependency, lock file, vendor file, custom navigation view/plugin, legacy redirect, gate receipt, or commit was created or changed.

Hierarchical Filament navigation, proportional verification, Tag removal, and Finding asset-optionality implementation started on 2026-07-22 at commit `541a81c4837e12b224728fbb51b943696847e165` on clean branch `develop`. The authoritative Docker services are healthy. The configured non-reference database is not empty: every existing migration has run, with one administrator, three clients, two assessments, eighteen Tag rows, twelve `finding_tag` rows, and twenty-four `finding_template_tag` rows. Composer locks Filament `v5.6.8`. Installed source inspection confirms that `Cluster` extends `Page`, inherits native `navigationParentItem`, `navigationGroup`, icon, label, sort, slug, and sub-navigation position capabilities, and redirects its own route to the first accessible clustered component; `discoverClusters()` discovers clusters plus their contained pages/resources; clustered components suppress independent main-navigation registration; and cluster membership prefixes both URLs and route names. The native navigation manager resolves children by parent label within the same navigation group, so `AssetCluster` can be a child of the clickable `Aziende` resource without custom navigation. This coherent slice will add D-057, update the superseded sidebar and proportional-verification rules, remove Tag through a forward migration and every runtime/import/export/report contract, preserve schema version 1 with strict rejection of `tags`, and verify the existing D-045 asset rule for every Finding scope. No dependency, vendor file, custom Blade navigation, legacy redirect, alternate storage/report implementation, or full `scripts/verify.sh` invocation is authorized by this slice.

Finding workbench palette and navigator-density refinement started on 2026-07-21 at commit `433f21c46f55a97dae509a195c675e9134ff7dc0` on branch `develop`. The initial working tree was not clean: `.github/workflows/quality.yml` was already deleted before this activity and is being preserved untouched as unrelated user work. D-056 records the user-approved four-color presentation, rare Lime primary hierarchy, success-only Green usage, aggregated export/creation actions, and selection-focused navigator rows. The authoritative three-area layout, Filament Table, `WorkspaceAssessment`, `findingData`, signed optimistic/idempotent persistence, lifecycle, reports, evidence, read-only state, and responsive modes remain unchanged. The healthy Docker profile served `http://127.0.0.1:8000/admin`; the isolated Chromium baseline passed 1 test/119 assertions and produced `palette-before-1920x1080-dark.png`, `palette-before-1440x900-dark.png`, and `palette-before-1440x900-light.png` under the non-versioned QA-artifact directory. The optional `agent-browser` CLI was unavailable, so the repository-authoritative Laravel Dusk/Selenium browser path supplied the real baseline evidence without adding a tool or dependency.

Finding workbench palette and navigator-density refinement completed on 2026-07-21. `AdminPanelProvider` now registers explicit 50–950 Lime and success-green Filament scales; `WorkspaceAssessment` groups unchanged PDF/XLSX operations under the neutral `Esporta` action; `AssessmentFindingsTable` groups unchanged blank/template creation under the neutral `Nuovo finding` action and exposes compact labelled reorder/search/filter controls; the Finding row Blade renders only number, two-line title, textual priority, secondary menu, and applicable non-open/incomplete/report-exclusion exceptions. The existing workspace Blade and application CSS provide one solid Lime `Salva`, neutral secondary actions, green-only success states, coherent neutral light/dark surfaces, Lime tabs/focus/selection, a three-column row grid, stable padding, fixed menu track, and no blue or purple workbench token. Italian translations, focused Feature/Dusk assertions, and the officially published CSS asset were updated; application JavaScript, domain, schema, persistence, editor structure, evidence, reports, lifecycle, and responsive modes were not changed. Source and published CSS are byte-identical SHA-256 `2c8ef66b90cc6616a0a49b1f0b3b6fec9fd5f3212f359d9dc83156d12b47ce8f`; unchanged source and published JavaScript are byte-identical SHA-256 `8d7b6fec72bb1df48397a2f6fa6bf574aa87e3ffd309ee7f87720d993de6174d`.

Files modified by this slice are `app/Providers/Filament/AdminPanelProvider.php`, `app/Filament/Resources/Assessments/Pages/WorkspaceAssessment.php`, `app/Filament/Resources/Assessments/Tables/AssessmentFindingsTable.php`, `resources/views/filament/resources/assessments/pages/workspace-assessment.blade.php`, `resources/views/filament/resources/assessments/tables/finding-workbench-state.blade.php`, `resources/views/filament/resources/assessments/tables/finding-priority.blade.php`, `resources/css/assestme-workspace.css`, its generated `public/css/app/assestme-workspace.css` copy, `resources/lang/it/assestme.php`, `tests/Feature/WorkspacePageTest.php`, `tests/Browser/MilestoneZeroTest.php`, `tests/Browser/MilestoneThreeWorkspaceTest.php`, `tests/Browser/MilestoneFourPdfTest.php`, `tests/Browser/WorkspaceResponsiveTest.php`, and this `plan.md`. `AGENTS.md`, `resources/js/assestme-workspace.js`, dependency manifests, migrations, models, actions, and report code are unchanged.

Final real-browser artifacts under `storage/app/qa-artifacts` are `palette-after-1920x1080-dark.png`, `palette-after-1440x900-dark.png`, `palette-after-1440x900-light.png`, `palette-after-navigator-selected-dark.png`, `palette-after-new-finding-menu-dark.png`, `palette-after-export-menu-dark.png`, `palette-after-editor-footer-dark.png`, and `palette-after-conflict-1920x1080-dark.png`; `workbench-validation-error-1920x1080.png` retains the required error-state evidence. The complete Docker browser gate passed 11 tests/432 assertions with no severe console errors, including a direct conflict-state assertion that the inner Filament table does not overflow the navigator horizontally. Focused workspace Feature coverage passed 12 tests/78 assertions; focused real PDF/XLSX Feature coverage passed 22 tests/215 assertions; Composer validation passed; Pint passed over 303 files; PHPStan passed over 244 files; Canary strict passed 34/34 with zero skips; and the locked audit reported no advisories. Diagnostics passed every check. The isolated 50-Finding benchmark passed every budget with HTTP 200, 0.2737 s first render, 791,071 bytes, 12 list queries, 0.0026 s selected-Finding open, 0.0255 s single-Finding save, 0.0252 s inline save, 0.0042 s assessment save, 0.0134 s reorder, 2.3201 s PDF, and 0.2276 s XLSX.

The authoritative aggregate gate is not recorded as successful. `composer quality` reached 251 passing tests/2,104 assertions but failed one unrelated deployment-configuration test because the pre-existing user deletion of `.github/workflows/quality.yml` makes its `file_get_contents()` fail; restoring that unrelated file was outside this activity and the deletion remains untouched. The storage audit also reports one pre-existing orphan, `reports/1/370edbde-e4fb-492f-96e5-c6f554b65121.pdf`, dated 2026-07-18, with no missing file, hash mismatch, or incomplete deletion operation; it was not deleted. `scripts/verify.sh` was therefore not repeated because it would rerun the already-known failed quality gate without a reusable receipt. The Docker application and Selenium services remain healthy and the real login page returns HTTP 200 at `http://127.0.0.1:8000/admin/login`. Edge, Firefox, iOS Safari, and Android Chrome manual QA were not executed or claimed.

Finding three-area workbench UX validation started on 2026-07-21 at commit `5cef6e5976e999ad308d78cbd08a4b7a64fd33df` on clean branch `develop`. D-055 records the user-approved replacement of only the list/inspector visual presentation with a compact Filament Table navigator, dominant selected-Finding editor, and contextual properties panel. The existing `WorkspaceAssessment` page, single `findingData` state, signed `SaveFindingDetails` path, optimistic locking, request UUID idempotency, conflict/offline/read-only states, lifecycle, reports, and evidence actions remain authoritative. The Docker application was healthy at `http://127.0.0.1:8000/admin/login`; the focused isolated baseline browser suite passed 3 tests/127 assertions and measured 1920×1080, 1440×900, 1280×800, and 390×844 without horizontal overflow. The initial 1920×1080 screenshot and geometry are stored as non-versioned QA artifacts `workbench-before-1920x1080.png` and `workbench-before-geometry.json`. This slice introduces no dependency, migration, model, API, nested Livewire component, Node.js/Vite/Tailwind build, alternate upload service, or second save path.

Finding three-area workbench UX validation completed on 2026-07-21. The authoritative Filament Table now renders a compact Finding navigator with search, filters, ordering, count, status, priority, scope metadata, and completeness; the selected Finding retains one `findingData` state and one signed `saveFinding` submission while its fields are presented as a dominant editor and contextual properties panel. Desktop containers at least `64rem` use fixed 18rem navigator and properties tracks with a flexible editor, one JavaScript-maintained available-height property, and independent vertical scrolling. Narrow containers deliberately switch between the preserved navigator state and a sequential full-width editor whose properties remain directly accessible below the main content. Previous/next navigation, visible save state, focus/scroll preservation, validation and conflict states, lifecycle read-only behavior, direct authorized evidence links, drag/selection uploads, and screenshot paste through the existing FilePond/Livewire temporary upload path are covered without changing the persistence action, evidence storage action, domain, report, or lifecycle. Source and published Filament assets are byte-identical; the retained non-versioned artifacts are `workbench-after-1920x1080.png`, `workbench-editor-scroll-1920x1080.png`, `workbench-validation-error-1920x1080.png`, `workbench-narrow-navigator-390x844.png`, `workbench-narrow-editor-390x844.png`, and `workbench-after-geometry.json`.

Accepted final Docker evidence is `composer validate --strict` clean; Pint clean over 303 files; PHPStan clean over 244 files; 251 application tests/2,119 assertions; Canary strict 34/34 with zero skips; locked audit with zero advisories; and Dusk Chromium with 11 tests/419 assertions and no severe console errors. `scripts/verify.sh` reused the exact quality/browser receipts and passed diagnostics, storage audit with 0 orphan/missing/mismatched/incomplete items, routes/about, and every 50-Finding benchmark budget: HTTP 200, 0.4335 s first render, 800,882 bytes, 12 list queries, 0.0026 s selected-Finding open, 0.0248 s single-Finding save, 0.0249 s contextual status save, 0.0041 s assessment save, 0.0132 s reorder, 2.3386 s PDF, and 0.2259 s XLSX. Exact browser geometry proved 288/943/288 px navigator/editor/properties at 1920×1080 and 288/463/288 px at 1440×900; 1280×800 and 390×844 used the sequential mode, and every measured document had zero horizontal overflow. Edge, Firefox, iOS Safari, and Android Chrome manual QA were not executed or claimed; this remains the explicitly non-final UX validation phase allowed by D-055.

Finding workspace two-mode responsive refinement started on 2026-07-21 at commit `b40f924cb9593149dedeb21d5e85d4c9f0bc63d2` on clean branch `develop`. D-054 records the user-approved removal of only D-008's intermediate absolute inspector: the authoritative Filament Table, single-Finding inspector, domain, signed optimistic/idempotent persistence, reports, evidence, and assessment lifecycle remain unchanged. The slice will first reproduce and measure the current browser layout and loaded asset provenance at 2560×1440, 1920×1080, 1440×900, 1280×800, and 390×844; it will then implement exactly the `64rem` desktop split and narrow sequential modes, simplify row/editor presentation, publish the application-owned Filament assets, add geometric Dusk coverage and required screenshots, and execute the maintained Docker verification gates. No Node.js toolchain, dependency, vendor edit, duplicate Blade view, absolute inspector overlay, alternate layout, or persistence path is introduced.

Finding workspace two-mode responsive refinement completed on 2026-07-21. One Blade structure now contains an outer inline-size query container and one list/inspector grid: its only container query enables the `64rem` lateral split, while the default sequential mode deliberately switches between full-width list and inspector. The former 70rem self-query, 48–70rem absolute overlay, every absolute inspector declaration, and visible per-row edit/up/down/delete controls were removed. The Filament Table retains whole-row selection, native reorder mode, inline status/report mutations, and one secondary-action menu; selected rows use background, inset edge, stronger title weight, focus outline, and `aria-selected`. The compact inspector keeps title/problem/entrepreneur notes immediately visible, moves technical notes into a collapsed section, and retains separate scope/risk, solutions, evidence, and status/report sections plus always-visible save state/actions. Signed idempotent optimistic persistence, conflicts, read-only lifecycle, offline state, keyboard commands, and all domain fields remain unchanged. Source/public assets were republished through `php artisan filament:assets` and are byte-identical. Five required screenshots and exact geometry JSON are under `storage/app/qa-artifacts`.

Accepted Docker evidence is `composer validate --strict` clean; Pint clean over 303 files; PHPStan clean over 244 files; 249 application tests/2,104 assertions; Canary strict 34/34 with zero skips; locked audit with zero advisories; and Dusk Chromium 150 with 11 tests/413 assertions and no severe console errors. `scripts/verify.sh` reused the exact quality/browser receipts and passed diagnostics, storage audit, routes/about, and every 50-Finding benchmark budget: HTTP 200, 0.5300 s first render, 1,248,828 bytes, 13 list queries, 0.0025 s inspector open, 0.0261 s single-Finding save, 0.0266 s inline save, 0.0041 s assessment save, 0.0148 s reorder, 2.3360 s PDF, and 0.2283 s XLSX. A newly published 2026-07-20 locked audit advisory required the permitted exact dependency update `guzzlehttp/guzzle` 7.14.1→7.15.1 and compatible `guzzlehttp/psr7` 2.12.5→2.13.0; no dependency was added. The first post-update quality run intentionally refused its receipt because a concurrently running Dusk suite changed test artifacts during its fingerprint window; the subsequent isolated quality run passed and wrote the accepted stable receipt.

Environment-driven administrator bootstrap remediation started on 2026-07-18. The configured Laravel `.env` already contains non-empty `DEV_ADMIN_NAME`, `DEV_ADMIN_EMAIL`, and `DEV_ADMIN_PASSWORD`, but `scripts/bootstrap-local.sh` currently reads only exported shell variables, so Docker Compose startup silently falls back to the placeholder name/email and a generated password instead of using the file. This slice makes the existing `assestme:create-admin` command explicitly load the three values through application configuration, preserves the random one-time local password behavior only when the configured password is empty, keeps the command non-idempotent, and leaves an existing singleton administrator unchanged. No credential value will be recorded in tests, command evidence, or this plan.

Environment-driven administrator bootstrap remediation completed on 2026-07-18. `config/assestme.php` is now the only PHP boundary that reads `DEV_ADMIN_NAME`, `DEV_ADMIN_EMAIL`, and `DEV_ADMIN_PASSWORD`; `assestme:create-admin --from-env --no-interaction` validates those configured values, generates and prints a one-time local password only when the configured password is empty, and retains the existing non-idempotent replacement protection. `scripts/bootstrap-local.sh` invokes that mode only after proving that the singleton is absent, so a clean Docker Compose initialization uses `.env` while every later startup preserves the existing administrator. Focused acceptance passed 21 tests/249 assertions plus focused Pint. The accepted container-only `composer quality` passed Pint over 302 files, PHPStan over 244 files with zero errors, 248 application tests/2,096 assertions, Canary 34/34 with zero skips, and the locked audit with zero advisories. The final `scripts/verify.sh` reused that exact quality receipt and passed isolated diagnostics, zero-item storage audit, every 50-Finding benchmark budget, and Dusk 8 tests/283 assertions; the benchmark measured HTTP 200, 0.5578 s first render, 1,476,481 bytes, 13 list queries, 0.0025 s inspector open, 0.0241 s single-Finding save, 0.0243 s inline save, 0.0041 s assessment save, 0.0130 s reorder, 2.3089 s PDF, and 0.2282 s XLSX. The normal instance remained reachable with HTTP 200 and exactly one administrator. No credential was printed or recorded. An accidentally started direct-host quality command was interrupted during PHPStan and rejected; all accepted evidence comes from the authoritative Docker `app` service.

Verification-gate deduplication started on 2026-07-18 after the user rejected repeated execution of the same expensive suites. The existing `composer quality` already runs Pint, PHPStan, all application tests, strict Canary, and the locked audit, while `scripts/verify.sh` repeated all five before running operational checks and Dusk. This slice makes the aggregate verifier consume exact repository/dependency/runtime fingerprint receipts from successful complete quality and browser subgates, invalidates them on any relevant change, prevents focused Dusk runs from certifying the complete browser gate, removes duplicate validation/audit work from CI setup, and adds executable receipt invalidation and orchestration coverage. No gate is omitted and no stale or user-asserted success flag is accepted.

Verification-gate deduplication completed on 2026-07-18. `scripts/gate-receipts.sh` fingerprints the Git commit and executable diff, untracked executable inputs, the authoritative plan through section 21, Composer manifests and installed package state, PHP version, and loaded extensions. `composer quality` and a complete browser run clear stale evidence before execution and write mode-0600 receipts only after a stable successful run; focused Dusk selections cannot write the browser receipt. The aggregate verifier contains no duplicate Pint, PHPStan, application-test, Canary, or audit commands and reuses only matching receipts. CI installs dependencies once and delegates validation/audit to the verifier. Executable coverage proves a receipt match, invalidation after a tracked-source change, and preservation after a factual Progress-only plan update. The final aggregate invocation explicitly reported one quality execution, one browser execution, and success; it passed Pint over 302 files, PHPStan over 244 files with zero errors, 244 application tests/2,075 assertions, Canary strict 34/34 with zero skips, a locked audit with zero advisories, diagnostics, storage audit, benchmark, and Dusk 8 tests/283 assertions.

Finding list/detail workspace redesign started on 2026-07-18. The user-approved D-008 now supersedes the native table Repeater only as the primary Finding-list workspace and authorizes a Filament Table structured list with one complete lateral inspector, while retaining native Repeaters for bounded child collections such as solutions. Repository audit confirmed that the existing main form serializes the complete Finding collection through `SaveAssessmentWorkspace`, while `SaveFindingDetails` persists the selected Finding aggregate without participating in the assessment version/idempotency protocol. This slice will converge complete editor persistence, inline status/report controls, reorder, creation, template copy, duplication, and deletion on assessment-locked optimistic mutations; keep assessment metadata on its separate signed path; avoid global `fillForm()` refreshes; and add query, Livewire, browser, responsive, dark-mode, and 10/25/50-Finding evidence. No dependency, vendor file, Node.js toolchain, external data grid, generic repository, nested Livewire component, or additional Markdown document is introduced.

Finding list/detail workspace redesign completed on 2026-07-18. The primary native table Repeater was removed and replaced by one authoritative `WorkspaceAssessment` page that renders a Filament Table structured list and a single-Finding schema in a custom adaptive Blade shell. Native Table `Split`/`Stack` columns provide progressive number, clamped title/problem, category/scope/count metadata, textual/configured-color priority, status and report inline controls, centralized specific completeness indicators, visible open/reorder actions, duplication, and confirmed deletion. Search covers Finding text, entrepreneur notes, solution descriptions, and associated assets; filters cover priority, status, category, report inclusion, and reusable domain completeness. Eager relations and counts avoid N+1 behavior, and the unpaginated deterministic list retains global reorder through the single `ReorderFindings` action for the approved 50-Finding bound.

The inspector loads only the selected aggregate and uses collapsible Filament sections for description, scope/risk, readable solution Repeaters, and existing/new file or URL evidence. Selection is retained in the query string; create, template copy, and duplicate select their result; previous/next, close, save, save-and-next, Ctrl/Cmd+S, Ctrl/Cmd+Enter, Alt+N, Alt+T, Esc, dirty/offline/conflict/error states, and completed-assessment read-only behavior are implemented and browser-covered. CSS Grid/container queries produce side-by-side layout only with sufficient real container width, otherwise an overlay/sequential mobile detail; list and inspector use `min-width: 0`, visible focus, dark mode, sticky header/footer, and no row-level or 1440-pixel workspace horizontal scroll. Non-versioned screenshots are stored under `storage/app/qa-artifacts` for the full-width list, open inspector, 1440-pixel viewport, mobile, and 1920-pixel dark mode.

`SaveFindingDetails` is the sole full-Finding editor persistence boundary: a typed `FindingSaveData` payload is signed, request-UUID idempotent, validated, executed under the assessment lock and database transaction, and returns the applied assessment version. Assessment metadata remains on the separate signed `SaveAssessmentWorkspace` path. Inline status/report changes, creation, template copy, duplication, evidence storage, deletion, and deterministic reorder mutate only the affected aggregate and increment the same assessment `lock_version`; stale versions surface a blocking conflict and failed validation/persistence never emits success. The list never serializes the complete Finding collection and no global `fillForm()` follows record mutations.

Accepted workspace evidence is the final deduplicated aggregate verifier: 244 application tests/2,075 assertions; Dusk 8 tests/283 assertions with no severe console errors; Canary 34/34 with zero skips; clean Composer validation/audit, Pint over 302 files, and PHPStan over 244 files; green diagnostics and storage audit; real PDF/XLSX and backup/restore coverage. The final 50-Finding run measured HTTP 200, 0.5450 s initial render, 1,476,481 response bytes, 13 list queries, 0.0023 s inspector open, 0.0236 s single-Finding save, 0.0250 s inline status save, 0.0039 s metadata save, 0.0126 s reorder, 2.3321 s PDF, and 0.2283 s XLSX. Separate 10/25 runs passed every budget at 0.2143/0.2827 s initial render, 398,560/802,585 bytes, 13/13 list queries, 0.0025/0.0025 s inspector open, 0.0246/0.0248 s single save, 0.0251/0.0259 s inline save, 0.0039/0.0078 s reorder, 0.5660/1.1322 s PDF, and 0.1168/0.1800 s XLSX. Manual Edge, Firefox, iOS Safari, and Android Chrome remain deferred under D-041 and are not claimed.

Final local handoff verification on 2026-07-18 reached `http://127.0.0.1:8000/admin/login` with HTTP 200 and completed a real credential-based Selenium login to `/admin`. Four existing immutable local reports remained present and SHA-256-valid after restore; the latest PDF parsed to 5,019 text characters and the real XLSX reopened with the required `Finding`, `Soluzioni`, and `Evidenze` sheets. `/workspace/backups/assestme-workspace-final-20260718.tar.gz` was created and verified with 28 manifest files, restored in maintenance mode, and produced safety archive `backups/assestme-safety-20260718-171929-289821.tar.gz`; post-restore diagnostics, report hashes, HTTP reachability, and real login all passed. The local one-time handoff credentials are reported only in the final user handoff and are not stored in this plan.

Custom Filament risk-matrix field implementation started on 2026-07-18. This user-approved decision supersedes only D-049's earlier schema-generated `ViewField`/select presentation: the edit page will use an application-owned, risk-specific Filament `Field` with an accessible semantic 4×4 Blade table, nested Livewire state keyed by persisted level IDs, and stable repeater item UUIDs only while newly created levels have no database ID. The existing `SaveRiskProfileConfiguration` aggregate remains the sole transactional persistence path and `risk_matrix_entries` remains the sole authoritative matrix store. No Composer plugin, JavaScript library, JSON column, Node.js/Vite pipeline, vendor edit, automatic Finding recalculation, or second save path is introduced. The normal database and private storage will not be used by verification; focused and final evidence will run through the maintained isolated Compose entry points.

Custom Filament risk-matrix field implementation completed on 2026-07-18. `App\Filament\Components\RiskMatrixField` and its application Blade view now render the initial Matrice tab as a semantic consequence-by-likelihood table with sixteen independent Livewire-bound priority selects, accessible two-dimension labels, visible priority text and color indicators, scoped responsive CSS, and light/dark presentation. Consequences, probabilities, priorities, and profile fields occupy the four subsequent tabs. Persisted IDs are the edit-state identities; stable repeater UUIDs exist only in unsaved form rows and are mapped to integer IDs by the existing aggregate transaction before the sixteen relational entries are written. The authoritative action rejects foreign or unknown IDs, 15/17-cell and extra-key payloads, duplicate identities, fewer than four enabled dimensions, and partial database failures while retaining form state and suppressing success. Focused tests additionally prove label/order/color changes preserve associations and codes, disabled priorities do not erase entries, and matrix edits do not recalculate existing Finding priorities.

Accepted evidence is `composer quality` and the final `scripts/verify.sh` executed inside the authoritative Compose `app` service: strict Composer validation; locked audit with zero advisories; Pint over 295 files; PHPStan over 237 files with zero errors; 244 application tests/2,010 assertions; Canary strict 34/34 with zero skips; diagnostics fully green; storage audit with zero orphan, missing, hash-mismatched, or incomplete items; and Dusk 8 tests/281 assertions with no severe console errors. The isolated 50-Finding benchmark passed every check with HTTP 200, 1.0771 s first render, 2,689,874 response bytes, 0.0534 s explicit save, 0.0478 s reorder, 2.2984 s PDF, and 0.2257 s XLSX. Chromium QA at 1366×768 and 1920×1080 covered light/dark mode, necessary-only horizontal scrolling, readable dimensions and priorities, keyboard focus, hover, retained validation state, save/reload persistence, and absence of false `beforeunload`; the dedicated non-versioned evidence is under `storage/app/qa-artifacts`. Before and after the complete verifier, the configured database remained SHA-256 `2e4ddbfe9e99e9e0121a578dbee049cc9750277ecae86a973170dfac11ec2330`, the private-storage manifest remained `430ee982f2695b446ce580a83771cb33a75649b1e30f1068677ee9ab4904c2ff`, and the backup manifest remained `abcfa6a9d4df344d1781bc2560b5e4cdcae08b39ed303063535e7e1e926a304a`. No dependency lock, vendor file, plugin, JavaScript asset, JSON persistence, new Markdown document, or runtime dependency changed. Manual Edge, Firefox, iOS Safari, and Android Chrome QA remains deferred under D-041 and is not claimed.

Report-settings migration blocker remediation started on 2026-07-18. Repository inspection confirms that the typed `ReportSettings` properties and the settings migration `2026_07_18_000015_add_report_presentation_settings` already exist with the approved `separate` and `true` initial values, while the four-hour-old bind-mounted Docker application reports that migration as pending and reproduces `MissingSettings` when resolving the class. This vertical slice applies the pending migration through the authoritative Compose service, clears settings and Laravel caches, adds real migrated-state and Filament/PDF regression coverage, and runs only the maintained isolated verification entry points after recording the configured database and private-storage hashes.

Report-settings migration blocker remediation completed on 2026-07-18. The existing migration was the only pending migration and was applied through `docker compose -f docker/compose.dev.yml exec -T app php artisan migrate --force`; `settings:clear-cache` and `optimize:clear` then completed successfully. Resolving `ReportSettings` now returns `cover_title_mode = separate` and `show_priority_descriptions = true`. A real authenticated Chromium visit reached `/admin/report-settings-page`, rendered both controls without `MissingSettings`, and produced zero severe console errors. Focused isolated coverage passed 23 tests/150 assertions for migrated settings, the Filament page, real PDFs in separate/combined modes, enabled/disabled descriptions, a null priority description, parsing, persistence, and hashes; the report-settings-only rerun passed 6 tests/38 assertions while saving both controls through Filament. The accepted complete verifier was run inside the authoritative Compose `app` service and passed strict Composer validation, locked audit with zero advisories, Pint over 292 files, PHPStan over 236 files with zero errors, 228 application tests/1,903 assertions, Canary 34/34 with zero skips, diagnostics, zero-item storage audit, the isolated benchmark, and Dusk 7 tests/245 assertions. The benchmark measured HTTP 200, 1.0845 s first render, 2,689,860 response bytes, 0.0528 s explicit save, 0.0531 s reorder, 2.2784 s PDF, and 0.2225 s XLSX. The configured database changed once, as authorized, from SHA-256 `8d47ece98b115e2240a8964026d5bf40723ce886f9ba5b47408a7458a4d91ebb` to post-migration baseline `2e4ddbfe9e99e9e0121a578dbee049cc9750277ecae86a973170dfac11ec2330`; the private-storage and backup manifests remained respectively `430ee982f2695b446ce580a83771cb33a75649b1e30f1068677ee9ab4904c2ff` and `abcfa6a9d4df344d1781bc2560b5e4cdcae08b39ed303063535e7e1e926a304a` across all isolated gates. The initially green verifier invocation from the host was rejected as non-authoritative and rerun completely inside `app`; no host result is used as acceptance evidence.

Approved UX/UI and functional decision implementation started on 2026-07-18. D-043 through D-052 now record the authoritative company terminology, assessment title/scope/navigation, conditional finding relations, dashboard information architecture, native resource layouts, stable external identifiers, immutable risk identities and 4×4 matrix, authoritative immediate PDF download, report settings, and entrepreneur-oriented PDF composition. Only the incompatible secondary rules in D-015, the previous dashboard presentation, the vertical risk matrix, the history-only PDF behavior, and PDF §§13.4–13.5 were marked SUPERSEDED; native Filament Repeater, DOMPDF, SQLite, immutable snapshots/hashes, recoverable deletion, optimistic locking, and idempotency remain unchanged. The protected baseline recorded before any verification command is database SHA-256 `37e2ab6d532fca9fa35ece6f4930070626ab29d6de6c22f5dda428a570c068ad`, private-storage manifest SHA-256 `430ee982f2695b446ce580a83771cb33a75649b1e30f1068677ee9ab4904c2ff`, and backup manifest SHA-256 `abcfa6a9d4df344d1781bc2560b5e4cdcae08b39ed303063535e7e1e926a304a` using the explicit relative-path/content algorithm documented in Discoveries.

The D-043–D-052 implementation slice completed its isolated automated and Chromium desktop acceptance on 2026-07-18. `composer quality` passed Pint over 292 files, PHPStan with zero errors, 225 application tests/1,875 assertions, Canary strict 34/34 with zero skips, and the locked audit with zero advisories. The complete `scripts/verify.sh` rerun passed strict Composer validation, the same static/application gates, isolated diagnostics, storage audit with zero orphan/missing/hash-mismatched/incomplete items, and Dusk with 7 tests/245 assertions and no severe console errors. The final isolated 50-Finding benchmark passed every check with HTTP 200, 1.0194 s first render, 2,689,856 response bytes, 0.0511 s explicit save, 0.0463 s reorder, 2.2191 s PDF, and 0.2265 s XLSX. Browser QA used Chromium `150.0.7871.114` and produced 32 non-versioned PNG artifacts under `storage/app/qa-artifacts`: the approved interface pages in dark mode and the real light PDF at 1366×768 and 1920×1080, including scrolled proofs for generated files, application status, template solutions, and the risk matrix. The first aggregate Dusk run exposed four failing browser expectations/fixtures; none was accepted as success, and focused then complete isolated reruns passed after the corrections recorded in Discoveries. The configured normal database, private storage, and backups remained byte-identical after all commands at their baseline hashes `37e2ab6d532fca9fa35ece6f4930070626ab29d6de6c22f5dda428a570c068ad`, `430ee982f2695b446ce580a83771cb33a75649b1e30f1068677ee9ab4904c2ff`, and `abcfa6a9d4df344d1781bc2560b5e4cdcae08b39ed303063535e7e1e926a304a`. No dependency lock, vendor file, competing Markdown document, or `docs/adr` directory changed. Edge, Firefox, iOS Safari, Android Chrome, hosted CI, CloudPanel, and device/camera QA were not executed or claimed; Milestone 5 and overall product acceptance remain open under D-041.

Unsaved-change warning remediation started on 2026-07-18. The defect is confined to the application-owned workspace JavaScript: because the asset is registered panel-wide, its unscoped Livewire input listener marks every Filament form dirty, while only the assessment workspace renders the save-status marker that can clear that state. The remediation will scope dirty tracking to the workspace Livewire component, make post-save reset independent of asset/Livewire initialization order, and add browser regression evidence for both ordinary Filament forms and the assessment workspace without changing persistence behavior.

Unsaved-change warning remediation completed on 2026-07-18. `resources/js/assestme-workspace.js` now accepts input and offline dirty-state transitions only when the assessment workspace save-status marker belongs to the same Livewire component, so ordinary Filament forms cannot inherit the workspace `beforeunload` warning. The Livewire morph hook is registered whether the asset loads before or after `livewire:init`, while a confirmed `saved` marker remains the only path that clears a genuinely dirty workspace. The published `public/js/app/assestme-workspace.js` was refreshed through `php artisan filament:assets`. `tests/Browser/MilestoneZeroTest.php` proves a saved client form has no unload prevention, an edited workspace retains it, a successful explicit workspace save clears it, both records persist, and no severe console error occurs. Accepted evidence is focused Pint; the focused regression with 1 test/6 assertions; `composer quality` with Pint over 285 files, PHPStan over 230 files with zero errors, 208 application tests/1,780 assertions, Canary strict 34/34 with zero skips, and zero locked advisories; aggregate Selenium Dusk with 6 tests/111 assertions; and a complete successful `scripts/verify.sh`. The final verifier benchmark passed with HTTP 200, 1.0941 s first render, 2,697,529 response bytes, 0.0526 s explicit save, 0.0488 s reorder, 2.0302 s PDF, and 0.2260 s XLSX. The normal database, private/generated storage, and backups manifest remained byte-identical across aggregate Dusk and the final verifier at SHA-256 `139ea207f8c9a23fae6c5628513f966bf6bd37f2f29956bef0dd93561667a32a`.

Docker development profile implementation started on 2026-07-18. The approved scope is limited to the authoritative `docker/compose.dev.yml` development environment, an optional profiled Selenium service, environment-neutral reuse of the existing local bootstrap, remote-driver-aware isolated Dusk execution, normative architecture tests, and executable lifecycle/data-isolation evidence. CloudPanel is recorded as the approved production destination but is not implemented in this slice. The existing generic Nginx/PHP-FPM release scripts and stubs are being preserved only as legacy/deprecated technical artifacts pending the future CloudPanel decision; they are not an installation profile.

Docker development profile implementation completed and was verified on 2026-07-18. Created files are `docker/compose.dev.yml`, `docker/dev/Dockerfile`, `docker/dev/php.ini`, and `docker/dev/entrypoint.sh`. Modified files are `AGENTS.md`, `plan.md`, `scripts/bootstrap-local.sh`, `scripts/dusk-isolated.sh`, `tests/DuskTestCase.php`, and `tests/Feature/DeploymentConfigurationTest.php`. No application behavior, production stub, CloudPanel artifact, root Compose file, additional Markdown document, CI workflow, Laravel environment example, dependency lock, or functional application file was changed. The exact development versions are PHP `8.3.32-cli-bookworm`, Composer `2.10.2`, Xdebug `3.5.3`, and Selenium standalone Chromium `4.46.0-20260707`; the pulled Selenium repository digest is `sha256:3400b92f1cddb2dfaaf358654e8f7d83d7be45192fb73c5f28c25faa28d36504`.

Accepted Docker evidence is Docker Engine `29.4.2` and Compose `5.1.3`; successful default and browser-profile Compose rendering; final image build `sha256:7061935e79b1d42dbf93c0055a226e95878772892139b63e4601d007207c1061`; healthy application and internal-only WebDriver services; complete D-004 extensions; Xdebug loaded with every feature disabled under `XDEBUG_MODE=off`; preflight, `artisan about`, and diagnostics green; 208 application tests with 1,777 assertions; Pint over 285 files; PHPStan over 230 files with zero errors; Canary strict 34/34 with zero skips; locked audit with zero advisories; Dusk through remote Selenium with 5 tests and 105 assertions and no severe browser-console errors; and a complete successful `scripts/verify.sh`. The final isolated 50-finding benchmark passed with HTTP 200, 1.0775 s first render, 2,697,509 response bytes, 0.0534 s explicit save, 0.0482 s reorder, 2.0226 s PDF, and 0.2227 s XLSX. The normal database, private/generated storage, and backups manifest remained byte-identical across application tests, `composer quality`, successful and failing Dusk, and the final verifier; the final protected-manifest SHA-256 was `3632c8dabd69ad3aff1de9767bf147baca87ba183d00a42f83885f9db82cc32a` before and after verification. Normal runtime logs changed as expected and were excluded from the protected application-data comparison.

Functional Docker evidence includes a clean temporary checkout with no `.env`, `vendor`, or SQLite database; successful bootstrap and a second idempotent startup; exactly one preserved administrator; host ownership `12345:12345` for newly created environment, Composer, Laravel, and SQLite paths; immediate bind-mount visibility for temporary PHP, Blade, Italian translation, application CSS, and application JavaScript edits; a real administrator login at `/admin` without severe console errors; real parsable PDF and XLSX generation through feature, browser, and benchmark gates; real isolated backup creation, manifest/hash verification, maintenance-only restore rejection, successful maintenance-mode restore, and database/private-file validation; bind-mounted SQLite WAL/SHM creation; and preservation of logical database content and all private/generated/backup files across stop, start, container removal, recreation, image rebuild, and forced recreation. Dusk temporary roots were absent after both success and an intentional unavailable-driver failure.

This Docker slice does not complete Milestone 5 or the overall product acceptance. CloudPanel configuration and procedures are intentionally not implemented, hosted GitHub Actions were not rerun, and manual Edge, Firefox, iOS Safari, Android Chrome, camera, and device-specific QA remain deferred under D-041. No unexecuted check is reported as passed.

On 2026-07-18, the user explicitly superseded D-042 v1's loopback-only host publication. The approved D-042 now publishes `0.0.0.0:8000:8000` for authorized local-network access while retaining HTTP-only development status and host-firewall responsibility. `docker compose config` rendered `host_ip: 0.0.0.0`; the recreated `app` container was healthy and listened on `0.0.0.0:8000`; both `http://127.0.0.1:8000/admin` and `http://10.0.0.30:8000/admin` returned HTTP 302 to their corresponding login URL; and the focused deployment configuration suite passed 13 tests with 196 assertions. No image rebuild was required because only Compose, the normative plan, and its architecture test changed.

- [x] Milestone 0 — Native Filament table Repeater and compatibility proof completed on 2026-07-13
- [x] Milestone 1 — Foundation and settings completed on 2026-07-13
- [x] Milestone 2 — Template library completed on 2026-07-13
- [x] Milestone 3 — Assessment workspace completed on 2026-07-13
- [x] Milestone 4 — Reporting and exports completed on 2026-07-15
- [ ] Milestone 5 — Completion and hardening

Current state: Milestones 0 through 4 are complete. Milestone 5 remains in progress. The application includes the singleton administrator, optional TOTP MFA, clients/sites/assets, classifications, owned risk profiles and matrices, global effort levels, typed settings, application-aware backup/restore, actionable dashboard, recoverable deletion, the versioned template library, the definitive assessment workspace, immutable PDF/XLSX generation, generated-file history, and staged generated-file deletion. The workspace uses the approved native table Repeater with detached template snapshots, lifecycle validation, signed idempotent optimistic persistence, explicit conflict/offline/save states, scope and risk editing, nested solutions, private evidence, accessible actions and keyboard shortcuts, and the four approved workspace tabs. D-037 remains limited to standard resource tables with visible-action parity and generic CSV/XLSX export.

Milestone 5 backup/restore delivery slice implementation completed on 2026-07-17, but Milestone 5 remains open. The authoritative `CreateBackup` and `RestoreBackup` operations and every command/composed consumer now use the approved typed invokable contracts, with an architecture dataset proving all 25 required actions exist, expose public `__invoke()`, and have no `handle()` alias. Of 49 current actions, 29 are invokable and 20 expose `handle()`; none of the remaining `handle()` actions is in the approved required-action inventory. The repeatable isolated production-like rehearsal installs two clean `--no-dev` releases from `composer.lock`, creates and verifies pre-deployment backups, enforces maintenance-only restore, creates restore and pre-rollback safety archives, migrates, publishes assets, optimizes, diagnoses, performs two atomic current-link switches, and proves database/private-storage rollback markers. It also proves non-zero rejection of a missing archive and restore outside maintenance mode. The first rehearsal exposed production config caching blocked by the Canary resolver Closure; the resolver is now a serializable callable and production `config:cache` has executable regression coverage. The successful rehearsal intentionally stubs and logs only `systemctl` and public HTTPS, so host service reload and TLS reachability are not claimed.

Accepted code/gate evidence for this slice is strict Composer validation, Pint clean, PHPStan over 230 files with zero errors, 204 application tests/1,678 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/105 assertions with no severe console errors through the maintained isolated entrypoint, green isolated diagnostics/storage audit, and a complete successful `scripts/verify.sh`. Its final isolated 50-finding benchmark passed with HTTP 200, 1.1125 s first render, 2,697,531 response bytes, 0.0528 s explicit save, 0.0493 s reorder, 2.0742 s PDF, and 0.2341 s XLSX. A direct unisolated `php artisan dusk` attempt failed with `ERR_CONNECTION_REFUSED` but reached browser-test setup first and changed the configured non-reference development database and operational-status files from the protected hashes `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328` and `f232151aa30eaecfef301508e45c6fefa7fa903205b9c857897dc62090aea9a8` to `d3616ae3cbe8ca6482d24d81739bf2e9c1f3b7698fc96b9dd2596ac55265b83d` and `a5d2729477d958d2dc394098caca453188d2399b73702c65c4a606623734113b`. No pre-attempt archive or matching SQLite copy exists locally, so the original bytes were not fabricated. Dusk now fails fast unless the marked disposable environment exists; a focused failure check and the final complete verifier left the new hashes unchanged. The hosted GitHub Actions jobs still have not been rerun, the changed local commits have not been pushed, and manual browser/device QA remains deferred under D-041. Milestone 5 is not complete.

Milestone 5 immutable-report invocation-conformance slice completed and verified on 2026-07-17. `BuildAssessmentSnapshot`, `GenerateAssessmentPdf`, and `GenerateAssessmentWorkbook` form one authoritative immutable-report pipeline and now use the approved typed invokable contracts across Filament, benchmark, and all feature tests; the old entry points were removed. Snapshot validation/settings, DOMPDF and PhpSpreadsheet generation, file/database compensation, independent versioning, optional excluded-finding behavior, and failure visibility are unchanged. Focused coverage proves immutable PDF/XLSX versioning and hashes, real parsable output, complete snapshots/settings, evidence and branding, estimate/note rules, 50-finding generation, optional excluded findings, missing/corrupt evidence rejection, invalid logo rejection, tamper rejection, and no generated row on failure. Accepted evidence is Pint clean, PHPStan over 229 files with zero errors, 176 application tests/1,557 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/105 assertions with no severe console errors, green isolated diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The final isolated 50-finding benchmark passed with HTTP 200, 1.0870 s first render, 2,697,537 response bytes, 0.0572 s explicit save, 0.0502 s reorder, 2.0738 s PDF, and 0.2389 s XLSX. Before and after all gates, the configured database SHA-256 remained `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328`; the explicit relative-path/content private-storage manifest remained `f232151aa30eaecfef301508e45c6fefa7fa903205b9c857897dc62090aea9a8`. Of 49 current actions, 27 are invokable and 22 expose `handle()`; only the approved backup create/restore actions still require invocation-convention conversion. Milestone 5 remains in progress.

Milestone 5 template-transfer invocation-conformance slice completed and verified on 2026-07-17. The authoritative `ImportFindingTemplates` apply operation and `ExportFindingTemplates` operation, including Filament, seeder, and round-trip test consumers, now use the approved typed invokable contract; the old apply/export entry points were removed. Preview remains a distinct non-mutating method on the same import action, while replace/skip semantics, schema/semantic validation, and atomic failure behavior remain unchanged. Focused coverage proves valid atomic import, data-equivalent export/re-import, create/replace/unchanged preview, skip behavior, duplicate/invalid document rejection with zero database changes, and domain seeding. Accepted evidence is Pint clean, PHPStan over 229 files with zero errors, 176 application tests/1,557 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/105 assertions with no severe console errors, green isolated diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The final isolated 50-finding benchmark passed with HTTP 200, 1.0941 s first render, 2,697,539 response bytes, 0.0544 s explicit save, 0.0501 s reorder, 2.0825 s PDF, and 0.2360 s XLSX. Before and after all gates, the configured database SHA-256 remained `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328`; the explicit relative-path/content private-storage manifest remained `f232151aa30eaecfef301508e45c6fefa7fa903205b9c857897dc62090aea9a8`. Of 49 current actions, 24 are invokable and 25 expose `handle()`; five approved actions still require invocation-convention conversion. Milestone 5 remains in progress.

Milestone 5 deletion-protocol invocation-conformance slice completed and verified on 2026-07-17. The authoritative `DeleteEvidence` and `DeleteEntityAccordingToPolicy` operations and every direct/composed consumer now use the approved typed invokable contract; the old entry points were removed rather than retained as aliases. Archive/permanent policy selection, assessment serialization, private-file staging, database/filesystem compensation, cleanup-state visibility, and path validation are unchanged. Focused coverage proves archive and permanent success, assessment/evidence/generated-report file handling, restricted-FK restoration, traversal/missing-file rejection, non-archivable rejection, and visible cleanup failure/retry behavior. Accepted evidence is Pint clean, PHPStan over 229 files with zero errors, 176 application tests/1,557 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/105 assertions with no severe console errors, green isolated diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The final isolated 50-finding benchmark passed with HTTP 200, 1.0969 s first render, 2,697,559 response bytes, 0.0549 s explicit save, 0.0503 s reorder, 2.0828 s PDF, and 0.2369 s XLSX. Before and after all gates, the configured database SHA-256 remained `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328`; the explicit relative-path/content private-storage manifest remained `f232151aa30eaecfef301508e45c6fefa7fa903205b9c857897dc62090aea9a8`. Of 49 current actions, 22 are invokable and 27 expose `handle()`; seven approved actions still require invocation-convention conversion. Milestone 5 remains in progress.

Milestone 5 assessment-authoring invocation-conformance slice completed and verified on 2026-07-17. The existing authoritative `CreateAssessment`, `CreateBlankFinding`, `CopyTemplateToAssessment`, `DuplicateFinding`, and `CalculateFindingPriority` implementations and all their application, command, feature, and browser consumers now use the approved typed invokable contract; the old entry points were removed instead of retained as aliases. Focused coverage proves valid assessment/site creation, foreign-site rejection, immediate blank finding creation, detached template copy, complete aggregate duplication, read-only rejection, matrix calculation, and cross-profile rejection. Accepted evidence is Pint clean, PHPStan over 229 files with zero errors, 176 application tests/1,557 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/105 assertions with no severe console errors, green isolated diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The final isolated 50-finding benchmark passed with HTTP 200, 1.0893 s first render, 2,697,557 response bytes, 0.0555 s explicit save, 0.0495 s reorder, 2.0573 s PDF, and 0.2365 s XLSX. Before and after all gates, the configured database SHA-256 remained `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328`; the explicit relative-path/content private-storage manifest remained `f232151aa30eaecfef301508e45c6fefa7fa903205b9c857897dc62090aea9a8`. Of 49 current actions, 20 are invokable and 29 expose `handle()`; nine approved actions still require invocation-convention conversion. Milestone 5 remains in progress.

Milestone 5 evidence-storage action conformance slice completed and verified on 2026-07-17. The separate `StoreEvidenceFile` and `StoreEvidenceUrl` implementations were removed; the required typed invokable `StoreEvidence` action is the single authoritative implementation for file and URL branches through an explicit `EvidenceType` discriminator. It preserves every approved extension/MIME/container, size, image-dimension, duplicate-hash, credential-free URL, private-path, and assessment-total rule; serializes storage under the assessment save lock, reloads persisted read-only state inside its transaction, and compensates a newly written private file when database persistence or commit fails. Every application/test call site now uses it, with no forwarding wrapper. Focused coverage proves all 11 approved file extensions, URL storage, authenticated delivery, duplicate/mismatched/generic-ZIP/credential-bearing rejection, persisted read-only rejection, and forced database-failure file compensation. Accepted evidence is Pint clean, PHPStan over 229 files with zero errors, 176 application tests/1,557 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/105 assertions with no severe console errors, green isolated diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The final isolated 50-finding benchmark passed with HTTP 200, 1.0927 s first render, 2,697,505 response bytes, 0.0537 s explicit save, 0.0491 s reorder, 2.0686 s PDF, and 0.2319 s XLSX. Before and after all gates, the configured database SHA-256 remained `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328`; the explicit relative-path/content private-storage manifest remained `f232151aa30eaecfef301508e45c6fefa7fa903205b9c857897dc62090aea9a8`. All 25 approved action names now exist; 15 of 49 current actions are invokable while 34 expose `handle()`, and 14 approved actions still require invocation-convention conversion. Milestone 5 remains in progress.

Milestone 5 signed-persistence action conformance slice completed and verified on 2026-07-17. The existing `SaveAssessmentWorkspace` protocol remains the sole public save boundary and continues to own request integrity, idempotency, locking, transactionality, and result storage. The required `SaveAssessmentPatch` action now owns the optimistic assessment-metadata/version update and site synchronization, while the required `ReorderFindings` action validates the complete surviving finding set and applies its authoritative order after create/update/delete; both are typed, invokable, and composed inside the signed workspace transaction, without an unsigned persistence fallback. Focused coverage proves the atomic metadata/create/update/delete/reorder path, idempotent replay, stale-version rejection, invalid-payload rollback, 10/25/50 rows, and rejection of an incomplete reorder set without partial updates. Accepted evidence is Pint clean, PHPStan over 230 files with zero errors, 175 application tests/1,557 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/105 assertions with no severe console errors, green isolated diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The final isolated 50-finding benchmark passed with HTTP 200, 1.0784 s first render, 2,697,551 response bytes, 0.0536 s explicit save, 0.0504 s reorder, 2.0766 s PDF, and 0.2324 s XLSX. Before and after all gates, the configured database SHA-256 remained `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328`; the explicit relative-path/content private-storage manifest remained `f232151aa30eaecfef301508e45c6fefa7fa903205b9c857897dc62090aea9a8`. The approved inventory now has one absent name (`StoreEvidence`), and 14 of 50 current actions are invokable while 36 expose `handle()`. Milestone 5 remains in progress.

Milestone 5 priority-override action conformance slice completed and verified on 2026-07-17. The required `OverrideFindingPriority` operation is a typed invokable action with the explicit `Finding`, `PriorityLevel`, and reason contract. It reloads persisted assessment, finding risk levels, and priority inside a transaction, normalizes the reason, rejects cross-profile selections, and is the row slide-over aggregate's path for complete manual overrides; incomplete draft priority state remains permitted by the approved draft-validation contract. Focused coverage proves successful normalized override, missing-reason rejection, cross-profile rejection, completed-assessment rejection, stale-caller protection, and integration through `SaveFindingDetails`. Accepted evidence is Pint clean, PHPStan over 228 files with zero errors, 174 application tests/1,547 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/105 assertions with no severe console errors, green isolated diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The final isolated 50-finding benchmark passed with HTTP 200, 1.0886 s first render, 2,697,511 response bytes, 0.0434 s explicit save, 0.0554 s reorder, 2.0608 s PDF, and 0.2321 s XLSX. Before and after all gates, the configured database SHA-256 remained `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328`; the explicit relative-path/content private-storage manifest remained `f232151aa30eaecfef301508e45c6fefa7fa903205b9c857897dc62090aea9a8`. The approved inventory now has three absent names, and 12 of 48 current actions are invokable while 36 expose `handle()`. Milestone 5 remains in progress.

Milestone 5 finding-status action conformance slice completed and verified on 2026-07-17. The divergent `TransitionFinding` action was removed; the required `TransitionFindingStatus` operation is typed and invokable, and `SaveFindingDetails` delegates status changes to it. Transition validation reloads the persisted finding and assessment inside the transaction, so stale callers cannot authorize a transition from obsolete state. Focused coverage proves allowed and invalid transitions, the resolved requirement, read-only enforcement, resolved timestamp persistence, and stale-caller rejection without a state change. Accepted evidence is Pint clean, PHPStan over 227 files with zero errors, 173 application tests/1,533 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/105 assertions with no severe console errors, green isolated diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The final isolated 50-finding benchmark passed with HTTP 200, 1.0933 s first render, 2,697,521 response bytes, 0.0406 s explicit save, 0.0546 s reorder, 2.0640 s PDF, and 0.2322 s XLSX. Before and after all gates, the configured database SHA-256 remained `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328`; the explicit relative-path/content private-storage manifest remained `f232151aa30eaecfef301508e45c6fefa7fa903205b9c857897dc62090aea9a8`. The approved inventory now has four absent names, and 11 of 47 current actions are invokable while 36 expose `handle()`. Milestone 5 remains in progress.

Milestone 5 finding-solution action conformance slice completed and verified on 2026-07-17. The boolean-mode `SetFindingSolutionReference` action was removed; the required `SetRecommendedSolution` and `SetImplementedSolution` operations are distinct typed invokable actions and are the workspace aggregate's authoritative assignment and explicit-clear paths. Both actions validate current persisted assessment and solution state inside a transaction. Focused coverage proves assignment, clearing, foreign and soft-deleted solution rejection, completed-assessment rejection, referenced-solution deletion protection, and deletion after a valid clear. Accepted evidence is a clean install from the committed lock, strict Composer validation, Pint clean, PHPStan over 227 files with zero errors, 172 application tests/1,527 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/105 assertions with no severe console errors, green isolated diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The final isolated 50-finding benchmark passed with HTTP 200, 1.0819 s first render, 2,697,513 response bytes, 0.0415 s explicit save, 0.0542 s reorder, 2.0661 s PDF, and 0.2283 s XLSX. Before and after all gates, the configured database SHA-256 remained `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328`; the explicit relative-path/content private-storage manifest remained `f232151aa30eaecfef301508e45c6fefa7fa903205b9c857897dc62090aea9a8`. The approved inventory now has five absent names, and 10 of 47 current actions are invokable while 37 expose `handle()`. Milestone 5 remains in progress.

Milestone 5 committed-evidence-fixture slice completed and verified on 2026-07-17. Reusable valid JPEG (`.jpg` and `.jpeg`), PNG, WebP, PDF, TXT, LOG, CSV, DOCX, XLSX, and ODS files are committed under `fixtures/evidence`; the evidence acceptance test reads those exact files, while the generated generic-ZIP-as-ODS case continues to prove rejection. Accepted evidence is strict Composer validation, Pint clean, PHPStan over 216 files with zero errors, 149 application tests/1,381 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/102 assertions with no severe console errors, green diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The isolated 50-finding benchmark passed with HTTP 200, 1.0908 s first render, 2,697,513 response bytes, 0.0428 s explicit save, 0.0552 s reorder, 2.0560 s PDF, and 0.2348 s XLSX. Milestone 5 remains in progress.

Milestone 5 authorization-policy slice completed and verified on 2026-07-17. One explicit Laravel policy is registered for all 12 models directly exposed by the ten Filament resources or the authenticated evidence/generated-report download routes. It authorizes only the persisted singleton administrator, the download controllers now invoke the policy, and bulk delete plus direct force-delete abilities are denied so they cannot bypass the approved record-scoped staged deletion protocol. Accepted evidence is Pint clean, PHPStan over 217 files with zero errors, 162 application tests/1,406 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/102 assertions with no severe console errors, green diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The isolated 50-finding benchmark passed with HTTP 200, 1.0846 s first render, 2,697,553 response bytes, 0.0418 s explicit save, 0.0547 s reorder, 2.0698 s PDF, and 0.2315 s XLSX. Milestone 5 remains in progress.

Milestone 5 operational-status slice completed and verified on 2026-07-17. Successful and failed backup attempts now persist an atomic private operational-status snapshot; a dedicated SQLite integrity check runs daily at 03:30 Europe/Rome after the managed backup and persists success/failure through the same typed invokable action. The dashboard keeps the newest managed successful-backup timestamp while adding explicit textual warnings for a later failed attempt and for failed database integrity, without exposing stored technical error text. Accepted evidence is strict Composer validation, Pint clean, PHPStan over 224 files with zero errors, 168 application tests/1,472 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/105 assertions with no severe console errors, green diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The final isolated 50-finding benchmark passed with HTTP 200, 1.0887 s first render, 2,697,559 response bytes, 0.0414 s explicit save, 0.0541 s reorder, 2.0699 s PDF, and 0.2385 s XLSX. Before and after the focused Dusk run, `composer quality`, and `scripts/verify.sh`, the configured database SHA-256 remained `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328` and the private-storage manifest SHA-256 remained `80e5f8185b9337b350a513095d04fa1a4634e8af74a44799cd81a7e709faac88`. Milestone 5 remains in progress.

Milestone 5 assessment-lifecycle action conformance slice completed and verified on 2026-07-17. The consolidated target-driven `TransitionAssessment` action was removed; the required `CompleteAssessment`, `ReopenAssessment`, and `ArchiveAssessment` operations are distinct typed invokable actions, and the shared completion validator is invokable. Persisted-state validation now runs under the existing assessment save lock and database transaction, so a stale caller cannot apply an obsolete transition. The workspace uses the required complete/reopen actions, while focused domain coverage proves successful completion/archive/reopen, row-numbered completion failure, invalid repeated transitions, and stale-state rejection without a version change. Accepted evidence is strict Composer validation, Pint clean, PHPStan over 226 files with zero errors, 169 application tests/1,484 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, focused Dusk 1 test/8 assertions, aggregate Dusk 5 tests/105 assertions with no severe console errors, green diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The final isolated 50-finding benchmark passed with HTTP 200, 1.0866 s first render, 2,697,527 response bytes, 0.0425 s explicit save, 0.0545 s reorder, 2.0485 s PDF, and 0.2307 s XLSX. Before and after the gates, the configured database SHA-256 remained `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328`; the explicitly defined relative-path/content private-storage manifest remained `f232151aa30eaecfef301508e45c6fefa7fa903205b9c857897dc62090aea9a8`. The approved inventory now has seven absent names, and 8 of 46 current actions are invokable while 38 expose `handle()`. Milestone 5 remains in progress.

Milestone 5 runtime-portability and deterministic-CI slice completed and verified on 2026-07-17 after explicit user approval to supersede the operating-system decisions. Specification 2.3 removes every normative host-distribution and package-source requirement while retaining PHP 8.3 and Nginx/PHP-FPM production deployment; Docker is permitted for future packaging but is not required by current development, verification, or runtime. Preflight now verifies only the application capabilities it uses and has executable success/incomplete-project failure coverage. Deployment paths, runtime group, PHP-FPM service, Nginx public root/upstream, and cron runtime values are operator inputs instead of distribution conventions. The clean-checkout job remains a reproducibility test on hosted CI infrastructure and no longer installs OS tools unused by the application. Laravel and the maintained isolated environment now default explicitly to Italian/Europe-Rome, so a checkout without `.env` cannot render `assestme.*` keys because of an inherited English locale. The 48-test/352-assertion focused regression covering the failures supplied from GitHub passed with an externally forced `en`/`UTC` environment. Accepted evidence is strict Composer validation, Pint clean, PHPStan over 226 files with zero errors, 171 application tests/1,511 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, green diagnostics/storage audit, a successful local `RUN_DUSK=0 scripts/verify.sh` under externally forced `en`/`UTC`, and a complete successful `scripts/verify.sh` with Dusk 5 tests/105 assertions and no severe console errors. A separate local bootstrap built a new application exclusively from tracked files and `composer.lock`, passed diagnostics, and produced exactly one administrator, 18 categories, zero assessments, and eight finding templates. The final isolated 50-finding benchmark passed with HTTP 200, 1.0981 s first render, 2,697,507 response bytes, 0.0419 s explicit save, 0.0554 s reorder, 2.0594 s PDF, and 0.2327 s XLSX. Before and after all gates, the configured database SHA-256 remained `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328`; the explicit relative-path/content private-storage manifest remained `f232151aa30eaecfef301508e45c6fefa7fa903205b9c857897dc62090aea9a8`. The hosted GitHub Actions jobs have not been rerun and are not claimed as passed. Milestone 5 remains in progress.

Milestone 4 closure remediation started on 2026-07-15 after a repository and runtime audit. Confirmed gaps are: the benchmark reads templates from and creates records in the configured application database; its setup happens before its cleanup guard and can leave client/assessment records on failure; the maintained Dusk invocation can target the configured application database; `scripts/verify.sh` and CI do not create one shared disposable verification environment; `DatabaseSeeder` incorrectly includes the 50-finding demonstration seeder while the required separate `SampleDataSeeder` is absent; the workspace-save request retention schedule is absent; fixture coverage does not yet include every declared evidence type; the required action naming/invokable convention and policy layer require a separate architecture review; and the configured local database currently has no status as an authoritative installation. No application database is considered a reference database during active development. The user approved D-041: automated verification must be autonomous and isolated, while manual Edge, Firefox, iOS Safari, Android Chrome, camera, and device-specific QA is deferred and does not block Milestone 4. Milestone 4 remains in progress until isolated verification, benchmark cleanup, seeding separation, focused regression tests, and all revised automated gates pass.

Milestone 4 closure remediation completed and accepted on 2026-07-15. PHPUnit, Canary, Dusk, the definitive benchmark, `composer quality`, CI, and `scripts/verify.sh` now create marked disposable file-SQLite and storage roots; cleanup runs on success/failure, and reuse of an unmarked or project-overlapping root is rejected. The standalone benchmark creates and seeds every prerequisite inside its own temporary environment before measuring the real workspace, PDF, and XLSX paths. `DatabaseSeeder` now installs domain configuration only; the explicit production-guarded `SampleDataSeeder` owns the sample client/assessment/50 findings. Expired workspace-save request UUID rows are purged hourly. Diagnostics include `PRAGMA integrity_check`. Runtime-generated fixtures exercise every approved evidence type; valid ODS/OOXML ZIP containers are structurally identified while generic renamed ZIP archives remain rejected. Dead Milestone 0 report generators and their isolated proof view/test were removed, and the report uses an application-owned partial without changing the one-note PDF contract. Accepted evidence is strict Composer validation, Pint clean, PHPStan over 216 files with zero errors, 148 application tests/1,378 assertions, Canary strict 34/34 with zero skips, locked audit with zero advisories, Dusk 5 tests/102 assertions with no severe console errors, green diagnostics/storage audit, and a complete successful `scripts/verify.sh`. The isolated 50-finding benchmark passed with HTTP 200, 1.0906 s first render, 2,697,535 response bytes, 0.0452 s explicit save, 0.0546 s reorder, 2.0757 s PDF, and 0.2343 s XLSX. Before and after `composer quality`, Dusk, the standalone benchmark, and the final verification script, the configured development database SHA-256 remained `4af2bdd29112214c3fd5f81199bd9a282cd111fac7eaaaf9037cf59e759865fb`, the private-storage manifest SHA-256 remained `80e5f8185b9337b350a513095d04fa1a4634e8af74a44799cd81a7e709faac88`, and counts remained one user, one client, one assessment, and zero generated reports. Those records are development residue and are not declared a reference installation. Under D-041, deferred manual browser/device compatibility does not block this closure.

Known work remaining for Milestone 5 is explicit and not included in Milestone 4 completion: after the assessment-lifecycle conformance slice, the approved required-action inventory remains structurally divergent for seven names whose behavior is consolidated in tested typed actions, and 8 of 46 current actions are invokable while 38 expose `handle()`; the corrected GitHub Actions jobs still require an actual hosted rerun even though the exact non-Dusk verification command now passes locally; manual Edge/Firefox/iOS Safari/Android Chrome/camera/device QA remains deferred; and the production-like backup/restore and final deployment handoff remain incomplete.

After the isolated Milestone 4 acceptance, the user explicitly requested initialization of the configured local development instance. Domain migrations/seeds and Filament assets were applied, the singleton administrator was explicitly replaced from the local `DEV_ADMIN_*` values, MFA was reset, diagnostics passed, and the application was started at `http://127.0.0.1:8000/admin`. A real headless Chromium/ChromeDriver login reached `Dashboard - AssestMe` at `/admin` with zero severe console errors. The unavailable optional `agent-browser` CLI was not claimed; ChromeDriver from the locked Dusk toolchain provided the executable browser proof. The one-time local credential is delivered in the handoff and is not stored in this plan.

Milestone 5 deletion-policy integration slice completed and verified on 2026-07-15: every supported archivable Filament resource now reads `GeneralSettings.deletion_policy` through one typed application action and delegates to the existing staged filesystem recovery protocol. Permanent assessment deletion serializes against workspace/report writes, stages all referenced generated reports and active or archived file evidences, and allows the database cascade only after every file has been verified and moved. Client logos use the same path; restricted foreign keys restore staged files and produce an Italian failure without a success notification. Visible, edit-page, and right-click actions share the same implementation, while unstaged delete/force-delete bulk bypasses were removed and an architecture test prevents their reintroduction. Accepted evidence is `composer quality` with 134 tests/1,340 assertions, Pint clean, PHPStan over 217 files with zero errors, Canary strict 34/34 with zero skips, and locked audit with zero advisories; aggregate Dusk passed 5 tests/101 assertions including visible/right-click “Archivia” parity and no severe console errors. Diagnostics and storage audit are fully green. The definitive 50-finding benchmark passed at 1.1380 s first render, 2,701,579 response bytes, 0.0493 s explicit save, 0.0549 s reorder, 2.0196 s PDF, and 0.2331 s XLSX. Milestone 5 remains in progress.

Milestone 4 PDF vertical slice implemented and verified on 2026-07-15: definitive A4 DOMPDF generation now uses a normalized readonly assessment/settings snapshot, persisted independent PDF versions and payload/file hashes, private generated-file storage, authenticated hash-verified download, complete report sections, verified image/file/URL evidence, consultant/client branding, Canvas page chrome, visible failure handling, freeze-safe workspace reload, storage audit integration, and generated-file history. The obsolete user-accessible Milestone 0 proof routes were removed. Milestone 4 remains in progress: the dedicated three-sheet XLSX implementation, the definitive benchmark-command migration, and the manual visual/browser acceptance checklist are not claimed by this slice.

Milestone 4 generated-file deletion vertical slice implemented and verified on 2026-07-15: generated PDF/XLSX history now exposes an explicit destructive action with accessible confirmation; the typed report action always uses permanent deletion independently of the global archive setting, serializes against assessment generation, and delegates exclusively to the existing staged filesystem recovery protocol while the model continues to reject normal deletion. Successful cleanup, missing-file rollback, immutable-model protection, and committed cleanup-failure warning paths are automated; cleanup failure never produces a success notification and remains retryable through the existing deletion operation. Accepted evidence is `composer quality` with 123 application tests/1,133 assertions, Pint clean, PHPStan over 213 files with zero errors, Canary strict 34/34 with zero skips, and locked audit with zero advisories; aggregate Dusk passed 5 tests/95 assertions, including confirmation, staged deletion, history refresh, file removal, and zero severe console errors. The complete `scripts/verify.sh` gate passed. Its maintained proof-generator benchmark remains green at 1.1807 s first render, 0.0517 s explicit save, 0.0566 s reorder, 0.2267 s PDF, and 0.1045 s XLSX; these report timings are not claimed as the definitive benchmark. Composer validation strict, diagnostics, and storage audit also pass; storage audit reports zero orphan, missing, hash-mismatched, or unfinished items. The local database was restored after Dusk and diagnostics confirm exactly one administrator. Milestone 4 remains in progress and no XLSX, definitive benchmark, or manual browser/visual completion is claimed.

Milestone 4 definitive XLSX vertical slice implemented and verified on 2026-07-15: the workspace now generates the approved native PhpSpreadsheet workbook with `Finding`, `Soluzioni`, and `Evidenze` sheets; the fixed VAT-excluded note appears exactly once in the merged first row, headers/filter/freeze panes and reviewed widths are applied, long text wraps, monetary cells remain numeric, and URL evidence uses native hyperlinks while private files remain non-public path references. Optional technical notes and explicit/default inclusion of excluded findings are honored without making incomplete excluded rows block export. XLSX files use independent immutable versions, physical UUID names, payload/settings snapshots including the effective export option, payload/file SHA-256, private storage, authenticated hash-verified downloads, generated-file history, and staged permanent deletion. Missing included evidence produces a visible error and no file/report row. The benchmark now measures the definitive persisted PDF and XLSX actions instead of Milestone 0 proof generators. During the acceptance restore, hidden private-storage files were found to be absent from backups; backup copy, manifest, and archive enumeration now include hidden files, with automated round-trip coverage for `.trash` content and `.gitignore`. Accepted evidence is `composer quality` with 128 tests/1,240 assertions, Pint clean, PHPStan over 215 files with zero errors, Canary strict 34/34 with zero skips, and locked audit with zero advisories. The final complete `scripts/verify.sh` run passed, including aggregate Dusk with 5 tests/99 assertions and no severe console errors. The definitive 50-finding benchmark passed at 1.1787 s first render, 2,701,599 response bytes, 0.0519 s explicit save, 0.0572 s reorder, 2.0873 s PDF, and 0.2402 s XLSX. A real fixed backup contained `storage/private/.gitignore`, restored successfully, and post-restore diagnostics remained green. Milestone 4 remains in progress only because the blocking manual visual and cross-browser/mobile acceptance checklist is not completed or claimed by this slice.

Milestone 1 completed in the then-current reference environment with the approved owned risk-profile schema, 16-entry matrix, global effort levels, typed general/report settings, native Filament management, and failure-path tests. Accepted evidence is 87 application tests with 774 assertions, Pint clean, Larastan level 6 with zero errors, Canary strict 31/31 with zero skips, locked audit with zero advisories, and Dusk Chromium 150 with 1 test/28 assertions covering every Milestone 1 browser surface and no severe console errors.

Milestone 2 completed with native template and nested-solution CRUD, stable external identifiers, semantic aggregate validation, versioned JSON Schema validation, two-stage import preview, deterministic replace/skip behavior, atomic failure handling, equivalent export round trips, and an eight-template/eight-solution Italian base library. Accepted evidence is 99 application tests with 851 assertions, Pint clean, Larastan level 6 with zero errors, Canary strict 34/34 with zero skips, locked audit with zero advisories, and a focused Dusk Chromium run with 1 test/5 assertions and no severe console errors.

Milestone 3 completed with the complete assessment/finding schema, native Filament table Repeater workspace, immediate blank/template/duplicate persistence, detached finding and solution snapshots, lifecycle and row-numbered completion validation, signed idempotent optimistic saves, explicit save/conflict/offline states, four approved tabs, row slide-over scope/risk/solutions/evidence editing, private evidence validation and authorized delivery, storage audit, visible action parity, and keyboard shortcuts. Accepted evidence is 111 application tests with 1,001 assertions, Pint clean, Larastan level 6 with zero errors, Canary strict 34/34 with zero skips, locked audit with zero advisories, focused Dusk Chromium with 1 test/8 assertions and no severe console errors, diagnostics fully green, and the 50-finding benchmark passing at 1.1605 s first render, 0.0527 s explicit save, 0.0601 s reorder, 0.2245 s PDF, and 0.1060 s XLSX.

---

## 23. Discoveries and deviations

- 2026-07-25: Filament's `full-height` page class alone cannot resolve a percentage height when its containing main panel supplies only `min-height: calc(100dvh - 4rem)`; the Report page must adopt that same locked topbar subtraction explicitly at the desktop workbench breakpoint. Height must then propagate with `min-height: 0` through the page content, content schema, form component, embedded form schema, grid wrapper, and panel components. The settings form renders two direct `.fi-sc` siblings: the first is the body and the second is the native action footer. Applying flex growth to both placed the valid 36 px Save button outside its clipping boundary even though its DOM rectangle remained inside the viewport. Limiting flex growth to the first sibling and preserving the last sibling at natural size keeps the action entirely visible and hit-testable. The browser regression deliberately failed on document overflow, option scrollability, and then the clipped save boundary before the final clean rerun passed.
- 2026-07-25: Filament's page-level `Width::Full` removes only the inherited `7xl` cap and composes directly with the existing responsive schema grid; no application CSS or panel-wide width change is required. After the preview grew from two fifths to three fifths, its normal A4-ratio iframe became taller than the browser viewport, so the old assertion that fullscreen must increase its numeric height became invalid even though the full-viewport viewer remained more usable. The correct expansion contract is fixed positioning over the complete client viewport with the iframe filling the remaining height below its toolbar; the focused rerun proves that behavior and preserves the narrow sequential layout.
- 2026-07-25: Reusing a report Blade view in browser screen media does not prove renderer parity. Exact production/preview engine parity requires both callers to terminate in the same renderer service; the accepted transient response is therefore a real WeasyPrint PDF displayed by the browser's native PDF viewer with private `no-store` controls. A complete field trace also found six visible General-owned report controls that the accepted WeasyPrint DTO/view never consumed; removing those controls from this page while preserving their stored keys avoids presenting inert or misleading DOMPDF-era behavior. Filament's single-file upload raw state is still an array, so preview logo parity requires explicit first-path normalization before applying the same validation and data-URI builder as production. Chromium's fixed overlay fills the document client viewport, which correctly excludes the browser scrollbar width; the first focused geometry assertion used `innerWidth` and failed, then the corrected client-viewport assertion and the Escape-path selector were each rerun before the final passing browser test.
- 2026-07-25: Filament’s native responsive `Grid` accepts five wide-screen columns and matching `3 / 2` child spans, producing the requested modest preview enlargement without custom grid CSS or changing the narrow one-column flow. The former `text-base font-semibold` preview heading also depended on utility availability; an application-owned semantic heading class now guarantees the measured `700` weight in the published no-build stylesheet.
- 2026-07-25: Scaling the complete report body cannot preserve mixed portrait/landscape dimensions or correct scroll height because CSS transforms do not change document flow. The preview instead wraps each existing direct report page in a flow-sized slot, measures its physical screen dimensions, and scales that page independently to the iframe content width. Introducing the vertical scrollbar reduces iframe `clientWidth` after the first fit; the initial browser rerun exposed the stale pre-scrollbar width and failed rather than accepting overflow. A second animation-frame fit uses the stabilized scrollbar-aware width and the final desktop/mobile browser proof passes. A separate first rerun exposed that the cover inherited auto width after becoming a slot child; assigning its existing physical 210 mm width restored the exact A4 ratio before the final passing rerun.
- 2026-07-25: The report-value fields remain normative properties of typed `GeneralSettings`, so relocating their controls did not require a data migration or duplicate `ReportSettings` properties. Filament’s settings page can load those values into the shared form state and save the two typed settings objects within its existing transaction by partitioning the validated state before the primary save and persisting the General subset in `afterSave`. For the preview, an iframe ratio alone is insufficient because screen CSS expresses the shared sheet in physical millimeters; the accepted path preserves that real 210 × 297 mm page and applies only a preview-class scale to one visible portrait sheet. Chromium computes a replaced iframe with suppressed overflow as `clip` rather than `hidden`, while `scrolling="no"`, the iframe’s clipped overflow, and hidden root/body overflow jointly produce no navigation bars. The initial browser assertion expecting only the `hidden` computed token failed and the same focused test passed after accepting the browser-equivalent `clip` value; application behavior did not change for that test-only correction.
- 2026-07-25: Filament `5.6.8` always sorts the blank navigation group before named groups; item-level `navigationSort` values apply within a group and cannot place a named Assessment group between ungrouped Dashboard and Aziende. Removing only Assessment’s redundant one-item group and assigning explicit top-level sorts produces the requested order while preserving the native Aziende parent/children and Impostazioni cluster behavior.
- 2026-07-25: The dashboard status defect was presentational rather than operational: `ApplicationStatus` returned the correct backup, database-integrity, and cleanup rows, but its Blade template depended on utility layout classes that are not part of the application’s published no-build stylesheet. The retained desktop artifact consequently showed table content collapsed into misaligned fragments. Application-owned semantic CSS with a container-responsive narrow mode fixes the geometry without changing status calculation or persistence. Filament’s separately registered `AccountWidget` was the sole source of the unwanted `Benvenuto`/`Disconnetti` card; removing that registration does not remove logout from the authenticated avatar menu.
- 2026-07-25: The risk DTO order is already authoritative low-to-high for both consequence and likelihood. The reported diagonal therefore required no data or snapshot mutation: reversing consequence rows only at render time yields low at bottom-left and high at top-right while likelihood continues left-to-right. WeasyPrint `57.2` renders the rotated `Conseguenza` label reliably when it is centered absolutely inside a fixed-height table cell, and centers the white selected mark reliably with absolute 50% positioning plus translation. A direct experimental PHPStan invocation against two test files outside the configured `app`, `database`, and `routes` paths exited `1` on six existing test-inference findings; it was not treated as project evidence. The maintained configured PHPStan command was then run and passed all `246` application files with zero errors.
- 2026-07-24: WeasyPrint `57.2` aligns the Finding number, title, metadata, effort, estimate, compact risk matrix, and affected systems deterministically when the critical composition uses fixed-layout presentation tables and a single 21 mm body offset. The real VIP Estintori snapshot produced version `27` at `storage/app/qa-artifacts/production-weasyprint-report-v27.pdf`: nine pages, `1,281,015` bytes, SHA-256 `cd2d982c39e2603f8db6659bac6c5876ac1be35e5b49403d3e8353517bfd0ce8`. Its three-solution Finding is distributed `2 / 1`; the second page contains the remaining solution, 4 × 4 symbol-only risk matrix, and affected systems. Poppler confirmed searchable complete text, A4 landscape only for the summary, and A4 portrait for Findings. Inspection of all nine 160-DPI raster pages found no overlap, crop, or truncation, and the grayscale raster retains an unambiguous current-cell black ring and white center.
- 2026-07-24: Definitive real-path QA created four Findings through application factories and actions, including all principal priorities, three solutions with distinct recommended/implemented roles, a deterministic two-page Finding, configured risk matrix, asset, near-limit text, and image evidence. `GenerateAssessmentPdf` produced `storage/app/qa-artifacts/production-weasyprint-report.pdf`: WeasyPrint `57.2`, `12` pages, `51,478` bytes, SHA-256 `33026cb2c88f13c57b9f636660751f67105e7c3ca14a33cb01ae4ece4ddaabb0`. Poppler proved page 1 portrait and unnumbered, page 2 portrait numbered `1`, page 3 landscape numbered `2`, pages 4–12 portrait, and selectable text. Every page rasterized at 150 DPI was inspected: cover, overview, summary, continuations, risk, assets, evidence, footer, whitespace, and complete indivisible solutions are legible without crop, overlap, or truncation.
- 2026-07-24: D-059 selected WeasyPrint after the official Chrome candidate failed the blocking A4 MediaBox requirement. With no builder paper format supplied, Spatie `2.12.0` passes `printBackground` to `Page.printToPDF` but omits `preferCSSPageSize`, so Chromium `150.0.7871.181` honors named-page orientation while normalizing CSS A4 to US Letter. A raw installed-API diagnosis with `preferCSSPageSize=true` proved the integration gap; no custom driver was created. The definitive production migration removed `chrome-php/chrome`, application-image Chromium, Chrome configuration, and spike-only failure evidence. Selenium Chromium remains optional browser-test infrastructure only.
- 2026-07-24: Debian WeasyPrint `57.2` and `pontedilana/php-weasyprint` `2.7.0` require `timeout => null` in Spatie’s WeasyPrint config because the wrapper otherwise emits `--timeout`, introduced only in WeasyPrint 60. Named pages, mixed A4 sizes, margin boxes, page-counter reset, break controls, selectable text, and images pass. Real one/two/three-solution QA established authoritative limits in `ReportEditorialLimits`: Finding title `180`, solution title `140`; one-solution narrative/problem/solution-description budgets `900/1400/1000`, two-solution `750/1100/800`, and three-solution `600/850/350`, with proportionally reduced comparison/effort/estimate/technical/resolution notes. These limits are shared by UI counters, signed saves, templates, import, copying, completion, and generation; legacy data is rejected with field-specific errors and never truncated.
- 2026-07-24: Removing `dompdf/dompdf` eliminated the six Dompdf advisories published on 2026-07-22. The locked production renderer set is now Spatie Laravel PDF `2.12.0`, `pontedilana/php-weasyprint` `2.7.0`, and container WeasyPrint `57.2`; Composer contains neither DOMPDF nor Chrome renderer packages.
- 2026-07-24: The locked `spatie/laravel-pdf` `2.12.0` includes a `ChromeDriver` that delegates directly to `chrome-php/chrome`, but `chrome-php/chrome` is not present in `composer.lock`. Source inspection shows that the driver sends global paper dimensions/orientation and does not set Chromium's `preferCSSPageSize` option. Chromium and `chrome-php/chrome` expose CSS-preferred page sizing, but mixed portrait/landscape pages are therefore unproven in the exact locked integration and remain a blocking D-059 spike criterion. No package was installed, no renderer was run, and no PDF was generated during this architecture review.
- 2026-07-22: DOMPDF 3.1.5 applies the root `html` box to the shared base `@page` style; the former `html, body { margin: 0; }` rule therefore zeroed the configured page margins and allowed Blade content into Canvas bands. Keeping the zero margin on `body` only preserves the 13/17/23/20 mm page geometry. Empty page-break elements also made reflow evidence harder to reason about, so `page-break-before: always` remains directly on the existing Finding/evidence page roots. A random optional IP from `AssetFactory` can add an asset row and legitimately create a ninth page; the composition regression now fixes the exact requested NAS payload rather than relying on random optional fields.

- 2026-07-22: Livewire morphing initially restored stale `x-cloak`/`x-show` state in the report-settings preview after an unsaved reactive update. The accepted native-view implementation keeps the preview root through `wire:ignore.self`, stores the selected cover/internal view in a local data attribute, and lets local Alpine tab handlers update that attribute and accessibility state. Filament/Livewire still morph every reactive preview value, with no nested component, poll, iframe, or global JavaScript; the final isolated Dusk regression passes.
- 2026-07-22: The unchanged workbook builder writes estimate in Finding column M and status in N, while `GeneratedWorkbookReportTest` line 248 expects the estimate in N and receives `Aperto`. A focused rerun reproduced the unrelated failure before the final gate. D-059 changes neither XLSX code nor this assertion, and no PDF-layout work was expanded into an unrequested workbook correction.
- 2026-07-22: Headless Chromium displayed blank content when asked to rasterize the generated PDF through its built-in viewer. The retained three-image QA set therefore captures the actual current Blade rendered from the same immutable snapshot used to generate and parse the six-page DOMPDF file; it verifies cover, Finding summary, and Finding evidence content/style, but does not claim exact DOMPDF raster geometry.
- 2026-07-22: Livewire `morph.updated` runs once for many individual nodes rather than once for a complete component morph. During a reactive conditional-field update, the browser reset the editor to zero and the captured scroll event overwrote the previous non-zero global value before a later node hook attempted restoration; the reproduced desktop value was `950 → 0`. Livewire's complete `morph`/`morphed` boundaries provide the required immutable before/after lifecycle. Filament finishes real Select layout/focus stabilization in the next two animation frames, so one cancellable post-frame restoration is required; explicit Finding-selection and validation-failure events cancel it to preserve their deliberate focus/scroll behavior. Navigator-only state has no save-status element, while Dettagli assessment has no Finding container, so the common hook identifies the owning workspace component through either stable surface.
- 2026-07-22: Filament `v5.6.8` native table search can remain authoritative while presenting as an icon-only navigator control: constraining its existing wrapper to 36 px, focusing its unchanged native search input from the wrapper click, and expanding on `:focus-within` preserves Livewire debouncing, query state, accessibility label, and result behavior without a second search implementation. The focused Chromium proof filters to one row, clears back to ten rows, repeats the click-first interaction at 390×844, and keeps reorder unavailable while a query exists. The optional `agent-browser` CLI named by the applicable verification skill was not installed in the development image, so the maintained isolated Laravel Dusk/Selenium path supplied the real browser evidence without adding a dependency.
- 2026-07-22: An outer HTML `details` used as the desktop contextual grid item retained an intrinsic expanded height of 1,065 px in a 900 px viewport, making the supposed internal body boundary equally tall and unable to scroll. All property groups already use native collapsible Filament sections, so the redundant outer disclosure was replaced by a semantic `aside` with `grid-template-rows: auto minmax(0, 1fr)`, zero minimum height, bounded alignment, and overflow containment. The previously failing browser geometry now proves the body acquires real overflow after lower groups expand, scrolls above zero, keeps the expanded Risoluzione section inside its own viewport, and passes the full responsive class.
- 2026-07-22: Filament `v5.6.8` resolves Sedi and `AssetCluster` as native children of the clickable Aziende item because all three have the same blank navigation group and their parent labels match. Its sidebar renders a clickable parent item's children while that item or one of its children is active; clicking Aziende therefore opens the company list and exposes Sedi/Asset through the native branch behavior, with no custom expansion Blade or third sidebar level. Cluster routes and route names are automatically prefixed, and the Cluster mount redirects to the first sorted accessible sub-navigation item.
- 2026-07-22: Filament Canary `v1.2.0` treats every authorized 3xx response as `needs-auth`, including the two intentional native Cluster-to-first-page redirects. The application testing adapter follows only an authenticated redirect whose source route is exactly `AssetCluster` or `SettingsCluster` and whose target exactly matches the approved first resource/page; guests and every other redirect retain Canary's original status. The strict rerun covers both Cluster routes as passed pages rather than excluding or skipping them: 33 passed, zero needs-auth, zero skipped.
- 2026-07-21: Filament's generated color helper normalizes lightness across its scale and does not retain a supplied high-luminance Lime as the intended central token. Registering an explicit, non-flat 50–950 array preserves `#E1FB15` at 500 while still giving hover, active, light, and dark Lime tones; Filament's native color manager accepts that array and emits its runtime variables without a frontend build.
- 2026-07-21: Native Filament `ActionGroup` preserves the existing nested action names, callbacks, modal schema, keyboard shortcuts, permissions, and report download behavior. It therefore aggregates creation and export without application JavaScript, a new Livewire component, or an alternate domain/persistence path. The PDF download action leaves its open menu available for choosing XLSX; the browser test now follows that native state rather than toggling the menu closed.
- 2026-07-21: The final Chromium screenshots at 1920×1080 dark, 1440×900 dark, and 1440×900 light show the same three-area geometry with neutral surfaces, no blue primary action, one Lime editor save action, neutral completion/export/creation actions, and selected rows using only a narrow Lime edge plus a lightly mixed neutral surface. Navigator rows use the fixed `1.75rem minmax(0, 1fr) 2rem` grid, two-line titles, textual priorities, conditional non-open state, missing-information text, and an accessible compact report-exclusion icon; no row renders category, scope, problem excerpt, counts, repeated `Aperto`, repeated `Completo`, or percentages.
- 2026-07-21: Full verification is limited by two pre-existing workspace facts outside this visual slice. `.github/workflows/quality.yml` was already deleted at the initial clean-state check, so `DeploymentConfigurationTest` fails while reading it; the normal private volume also contains the 28,356-byte orphan PDF `reports/1/370edbde-e4fb-492f-96e5-c6f554b65121.pdf`, last modified 2026-07-18 19:19:29 Europe/Rome with SHA-256 `e51aa4947d5b74af19485f4d6992526a86788472bd3b135cf663a929c3d5e568`. Neither item was changed or removed.
- 2026-07-21: Splitting one Filament form presentation into main-editor and properties schemas while retaining the same nested `findingData` state requires filling the properties schema before the main schema. That order lets the main schema initialize and retain the nested `evidence_uploads` FileUpload path; both schema renderings still dehydrate through one raw state and the existing signed `saveFinding` method, so no second persistence path exists.
- 2026-07-21: Filament FileUpload/FilePond supports ordinary file selection and drag/drop natively, and `pasteable()` exposes its upload capability, but a screenshot delivered to the application document's clipboard event is not automatically forwarded to the nested pond in this layout. The minimal application adapter forwards only clipboard image `File` objects to the already initialized FilePond instance via `addFile`; Livewire temporary upload, MIME validation, `StoreEvidence`, private storage, hash generation, and failure handling remain unchanged. Dusk pasted and persisted a real PNG through that path.
- 2026-07-21: Livewire 4 HTTP upload testing selects a disk named `tmp-for-tests` before the browser test can install its in-process fake; without configuration the real upload request failed reproducibly with `Disk [tmp-for-tests] does not have a configured driver`. An isolated local disk rooted at `storage/framework/testing/disks/tmp-for-tests` now supplies that test-only transport boundary. Production's default local/private storage, the evidence action, and vendor code are unchanged.
- 2026-07-21: Final CDP-backed geometry used exact inner viewports. At 2560×1440 the workbench was 2161 px wide with 288/1583/288 px navigator/editor/properties; at 1920×1080 it was 1521 px with 288/943/288 px; at 1440×900 it was 1041 px with 288/463/288 px. All desktop editor and properties bodies computed independent `overflow-y:auto`. At 1280×800 and 390×844 the selected state intentionally hid the navigator and rendered the editor/properties sequentially at 864 px and 326 px respectively. Every viewport had zero document horizontal overflow. Published and source assets are byte-identical: CSS SHA-256 `810c299f88fc53a97ed2efb442d1a3f84ca1e52d1ae40d2395cada6a15ed963c`, JavaScript SHA-256 `8d7b6fec72bb1df48397a2f6fa6bf574aa87e3ffd309ee7f87720d993de6174d`.
- 2026-07-21: The desktop inspector-below-list defect was reproduced in the isolated Docker/Selenium profile by `WorkspaceResponsiveTest` with ten realistic Findings containing varied title lengths, multiline problems, four priorities, complete/incomplete states, report inclusion/exclusion, and multiple statuses. At the requested 2560×1440 window the effective `.assestme-findings-workspace` width was 2161 px but its computed grid remained one 2159 px column: the list occupied `top=277`, `bottom=1579.5`, and the static inspector began at `top=1579.5`, producing zero vertical overlap. At 1920×1080 the 1521 px workspace likewise remained one 1519 px column and the inspector again began exactly at the list bottom. At 1440×900 and 1280×800 the 1041/881 px containers activated the intermediate rule, producing a 608 px `position:absolute` inspector over the list. At 390×844 the existing narrow query intentionally hid the list while the selected inspector was visible. In every case `has-inspector` was present, computed `container-type` was `inline-size`, the document had no horizontal overflow, the selected row exposed `aria-selected=true`, and the inspector body was `overflow-y:auto`.
- 2026-07-21: The exact cause is CSS container-query subject selection, not a stale asset or merely the numeric breakpoint. `.assestme-findings-workspace` is itself the query container, while the `@container (min-width: 70rem)` rule tries to change `grid-template-columns` on that same element. A container cannot use its own inline size as the query container for styling itself, so the desktop grid rule never applies; once the descendant-only intermediate absolute rule stops at 70rem, the unchanged one-column grid places the static inspector in the next row. Browser evidence loaded `http://assestme-app:<isolated-port>/css/app/assestme-workspace.css?v=5.6.8.0` as stylesheet 1 of 3 and the JavaScript marker was `loaded`. Before implementation, source and published CSS were byte-identical SHA-256 `3d52abe04c7a0cfc0af2b150a4e3deb3c37abcc6818092bfb8f5e55eb816971f`, and source and published JavaScript were byte-identical SHA-256 `c77e7ef6b38257beb2cc9f2541452b61e6197ed89c514da8a5f6e6d748c1d50f`; source/public modification times differed only because `filament:assets` copies the registered source into `public`.
- 2026-07-21: The selected D-054 implementation uses one application-owned outer query container and retains one inner workspace structure, so the inner grid is a descendant that can legally respond to the outer container's width. Default CSS is the narrow sequential list/detail mode; one `@container (min-width: 64rem)` rule enables the lateral desktop grid. The 70rem rule, the 48–70rem intermediate rule, and every absolute inspector declaration are removed. One JavaScript-maintained `--assestme-workspace-available-height` property derives from the actual Filament workspace top and current dynamic viewport height, allowing list and inspector-body scrolling while keeping inspector header/footer visible without duplicated viewport-offset calculations.
- 2026-07-21: Final CDP-backed Dusk measurements use exact inner viewports. At 2560×1440 the 2161×1148 workspace produced 1151.453/1007.547 px list/inspector columns with 1146 px vertical overlap; at 1920×1080 the 1521×788 workspace produced 810.125/708.875 px columns with 786 px overlap; at 1440×900 the 1041×608 workspace produced 554.125/484.875 px columns with 606 px overlap. In all three split cases list and inspector had the same `top=277`, the inspector's left edge equalled the list's right edge, inspector position was `static`, list/inspector scroll widths did not exceed client widths, header/footer remained inside the viewport, the body computed `overflow-y:auto`, and document horizontal overflow was zero. At 1280×800 the effective 881 px container intentionally used a single 879 px track; at 390×844 the effective 343 px container intentionally used a single 341 px track. Both narrow viewports proved list-only before selection and full-width inspector-only after selection, with close restoring preserved search/filter/page/order state. The selected row exposed `aria-selected=true`; Enter, Space, and whole-row click all opened it.
- 2026-07-21: The final application asset source and published files are byte-identical after the official Filament publisher: CSS SHA-256 `7ac5b77aa91f79edcd25211555e83213c684d5bd2aad76c2ff37917b03bfb99d`, JavaScript SHA-256 `e0ffb6d673d52540fc832026cb701a6e3dd5a3a44f99af831e54b43095fddcbe`. Dusk loaded `/css/app/assestme-workspace.css?v=5.6.8.0` and `/js/app/assestme-workspace.js?v=5.6.8.0`, observed `data-assestme-workspace-asset=loaded`, and recorded no severe console entry. The retained browser artifacts are `workspace-desktop-split-1920x1080.png`, `workspace-desktop-long-form-1920x1080.png`, `workspace-narrow-list-390x844.png`, `workspace-narrow-inspector-390x844.png`, `workspace-validation-error-1920x1080.png`, and `workspace-final-geometry.json`.
- 2026-07-18: Laravel loads `.env` values for Artisan through application configuration, but Bash does not automatically export values from the same file. The previous bootstrap therefore saw `DEV_ADMIN_*` only when a parent process explicitly exported them, even though the values were non-empty in `.env`. Reading them in `config/assestme.php` and exposing an explicit command mode avoids unsafe shell parsing and keeps direct `env()` calls out of application code. Read-only verification found that the existing singleton credentials do not match the current `.env`; D-042 requires existing credentials to remain unchanged, so no automatic replacement was performed. A deliberate replacement continues to require `assestme:create-admin --replace` with the current password or interactive confirmation.
- 2026-07-18: Filament Repeater uses stable UUID container keys in live form state but dehydrates relationship-style rows through a numerically indexed payload. New consequence, likelihood, and priority rows therefore retain that UUID in the hidden application `_form_key` field solely for the unsaved aggregate transaction. `SaveRiskProfileConfiguration` validates each temporary identity as a distinct UUID, maps it to the newly persisted integer ID inside the existing transaction, and writes only integer foreign keys to `risk_matrix_entries`; no UUID reaches the database and no second persistence path was added.
- 2026-07-18: A custom Filament field containing nested inputs must participate in Filament's public nested recursive validation contract for cell errors to remain associated with the field. The risk-specific field implements that contract, while the Blade view renders both aggregate and per-cell Italian validation messages and leaves the manipulated Livewire state intact. A forced SQLite trigger failure proved the profile, level, and matrix writes roll back together without a success notification.

- 2026-07-18: The report-presentation settings migration was present and correct but had reached the bind-mounted repository after the already-running application container started: the container start was `2026-07-18T09:35:49Z` (11:35 Europe/Rome), while the migration file modification time was 13:39 Europe/Rome and its commit was later still. The Docker entrypoint runs `scripts/bootstrap-local.sh`, including `php artisan migrate --force`, only when the container process starts; a bind mount makes later source files immediately visible but does not rerun the entrypoint or watch for database migrations. Therefore every vertical slice that introduces a migration while `app` is already active must execute that migration in the same Compose service before browser verification. Restarting/recreating the container would also invoke bootstrap, but relying on a later restart leaves the current runtime inconsistent and caused the reproduced `MissingSettings` failure.
- 2026-07-18: Filament's distributed CSS does not contain arbitrary Tailwind utilities introduced only in Blade, so `md:grid-cols-[...]` and `min-w-[48rem]` left the application-status checks stacked and generated-history columns cramped in the real browser even though the DOM was valid. The accepted implementation uses semantic tables, standard Filament-compatible utilities, and deterministic column widths; the regenerated 1366×768 and 1920×1080 artifacts show one compact status row per check and a readable generated-file history without a frontend build or new dependency.
- 2026-07-18: Risk-profile creation needs two distinct code paths: manually created levels may arrive without technical codes and require deterministic generation, while seed/import payloads already carry contract identities that must be preserved. The first focused implementation regenerated those supplied codes and broke the default `high`/`critical` identities. The authoritative action now preserves explicit creation codes, generates only absent ones, maps persisted rows by hidden ID or existing code, and rejects every post-save code mutation; focused and aggregate tests prove label edits retain level identity and all sixteen matrix cells.
- 2026-07-18: The first aggregate Dusk run failed four tests with 3 passing/104 assertions: three assertions still expected superseded dashboard/workspace strings or a lazy widget before scrolling, and the ordinary-form save fixture inherited a random phone that could violate the real server-side regex. The tests now assert the approved non-blocking dashboard copy and Finding capitalization, scroll the lazy widget into view, and set the unrelated phone to null. The focused reruns and two subsequent complete isolated suites passed at 7 tests/245 assertions without weakening validation or hiding a browser error.
- 2026-07-18: The application-owned workspace asset is registered panel-wide, so checking only for a generic Livewire ancestor caused every Filament form input to set the workspace closure's `dirty` flag. Non-workspace pages cannot render the assessment save-status marker and therefore had no path to clear that flag after a successful save. Scoping the listener to the Livewire component that contains the marker fixes the false warning without weakening the workspace protection or enabling Filament's unrelated panel-wide alert mechanism.
- 2026-07-18: The new Dusk regression initially ended while a queued Livewire save request was still in flight. Aggregate database truncation then removed its authenticated user, causing that late request to follow the login redirect and expose an HTML response to the Livewire JSON parser; the same class passed in isolation. A preserved marked Dusk root showed no application exception. The test now counts requests through Livewire's request lifecycle callbacks, waits for a 250 ms zero-request quiet window, and checks its own severe console log before teardown. The diagnostic root was removed after inspection, and both aggregate Dusk and the complete verifier pass.
- 2026-07-18: The first PHP image build attempted to rebuild every D-004 extension separately and failed reproducibly while compiling `xmlreader` because the modular DOM build did not expose `ext/dom/dom_ce.h`. The official `php:8.3.32-cli-bookworm` base already provides `ctype`, `curl`, `dom`, `fileinfo`, `filter`, `iconv`, `libxml`, `mbstring`, `openssl`, `PDO`, `pdo_sqlite`, `session`, `SimpleXML`, `tokenizer`, `xml`, `xmlreader`, `xmlwriter`, `zlib`, and OPcache. The final image uses the official helpers only for the absent `bcmath`, `gd`, `intl`, and `zip` extensions and executable runtime inspection proves the complete required set; no third-party extension installer or vendor patch was added.
- 2026-07-18: A static Compose `user: "${UID:-1000}:${GID:-1000}"` made the repository unwritable in the root-owned verification workspace. The final entrypoint reads the bind-mounted repository UID/GID, accepts explicit numeric overrides, and uses `setpriv` to drop from root before bootstrap when the resolved owner is non-root. A clean checkout owned by UID/GID 12345 proved that `.env`, `vendor`, SQLite, storage, and cache files are created as 12345 without `chmod -R 777`; root-owned repositories remain executable without inventing an inaccessible default owner.
- 2026-07-18: Chromium 150 interprets the bare Compose DNS name `app` as the public `.app` name and applies the preload HSTS policy. The isolated PHP server logged rejected TLS requests and Dusk failed with `ERR_CONNECTION_CLOSED`; disabling ordinary HTTPS-upgrade features did not override HSTS. The service remains named `app` for every official Compose command and has no `container_name`, while the default Compose network supplies the non-HSTS alias `assestme-app` to the remote browser. Chromium then reached the isolated server, and the Dusk-only secure-origin flag prevented an insecure-download console warning during the real PDF/XLSX history test. The accepted full suite is 5 tests/105 assertions with no severe console errors.
- 2026-07-18: Laravel 13's `artisan serve` passes only a small allowlist of environment variables to its child PHP server when `.env` reload monitoring is enabled. CLI diagnostics used the Compose SQLite override, but a real Livewire login request instead read the old direct-host path from `.env` and failed visibly. Adding Laravel's `--no-reload` option preserves every Compose process override in the child server; the subsequent real login reached `/admin` without console errors. This keeps one neutral `.env`, adds no Docker environment file, and source-code changes remain immediately visible because the PHP development server does not cache application source.
- 2026-07-18: The real isolated restore proof first exited non-zero, as required, when restore was attempted outside maintenance mode. The accepted rerun entered maintenance mode, restored the verified archive, returned the isolated application to live mode, and proved both the administrator value and a private-file sentinel returned to their archived values. No failure was converted into success and the normal database/storage were not used.
- 2026-07-18: The domain seeders are result-idempotent but rewrite technical database timestamps during each container bootstrap, so byte-for-byte SQLite identity is not a valid container-lifecycle persistence measurement. Lifecycle proof instead compared every domain table after removing only `created_at`/`updated_at` fields and separately compared SHA-256 for every private/generated/backup file; both signatures remained identical across stop, removal, recreation, rebuild, and forced recreation. Verification commands were held to the stricter byte-for-byte protected manifest and passed it.

Record unexpected package behavior, version incompatibilities, and material design corrections here.

- 2026-07-17: The production-like deployment rehearsal initially failed at `php artisan optimize` because `config/filament-canary.php` stored its administrator resolver as a Closure, which Laravel cannot serialize for configuration caching. Replacing only that resolver representation with the application-owned static callable `ResolveCanaryAdministrator::resolve` preserved Canary behavior, passed production `config:cache`, and allowed both clean `--no-dev` deployments and the compensating rollback to complete. This was a production blocker found by the real rehearsal, not a simulated failure.
- 2026-07-17: Running `php artisan dusk` directly without the maintained isolated server wrapper produced five `ERR_CONNECTION_REFUSED` failures after `DatabaseTruncation` setup had already reached the configured non-reference development instance. Its database and the two operational-status snapshots changed, and exhaustive local searches found no backup/archive or SQLite copy matching the protected pre-run hashes. Exact byte restoration is therefore unavailable without external backup material. `Tests\DuskTestCase` now rejects every browser run unless `ASSESTME_TEST_ISOLATED=1`, `ASSESTME_TEST_ROOT` is present, and its `.assestme-test-root` marker exists. The maintained `COMPOSER_ALLOW_SUPERUSER=1 composer browser` and `scripts/verify.sh` paths remain green and isolated; subsequent before/after comparisons proved they did not change the newly observed configured hashes.
- 2026-07-17: `TransitionFinding` validated the status and assessment relation carried by the caller, allowing an obsolete in-memory finding to pass a transition that was invalid for the current persisted state. The required `TransitionFindingStatus` replacement reloads both records inside its transaction. Focused tests prove stale status and completed-assessment callers are rejected without modifying the finding.
- 2026-07-17: `DeleteFindingSolution` previously used `loadMissing('finding')`, so a caller that first attempted deletion while the solution was referenced retained stale relationship state and received the same rejection after both references had been explicitly cleared. The focused failure-path test reproduced this behavior. Deletion now reads current persisted finding state inside its transaction; the two new solution-reference actions likewise validate current persisted assessment and active-solution state before saving.
- 2026-07-17: A direct read-only storage audit of the configured non-reference development instance reported the existing orphan `reports/1/370edbde-e4fb-492f-96e5-c6f554b65121.pdf`; the configured database contains zero generated-report rows and the file was already part of the supplied private-storage manifest hash. D-041 verification did not delete or adopt it. The maintained isolated storage audit inside `scripts/verify.sh` passed with zero orphan, missing, hash-mismatched, or unfinished items, while the configured database and storage hashes remained unchanged.
- 2026-07-15: A closure audit reproduced that `assestme:benchmark --findings=50` failed when the configured database had no enabled organization-scope template. The command created its assessment and factory client before loading that template and before entering its `try/finally`, so the failure left both records behind. This was not unavoidable framework behavior: it resulted from treating a previously seeded development database as a fixture source and from placing setup outside the cleanup boundary. The audit-created records were identified exactly and removed. The remediated command now creates, migrates, seeds, measures, and removes its own temporary database/storage and a regression test proves the caller's configured connection and records are restored unchanged.
- 2026-07-15: The current local database is not an installation authority and contains development-test residue rather than the required domain seed. D-041 now makes the maintained verification paths independent from it. Tests and benchmarks neither repair nor depend on that database; repeated SHA-256 and record-count comparisons around all accepted gates proved no mutation.
- 2026-07-15: The user approved deferring manual Edge, Firefox, iOS Safari, Android Chrome, camera, and device-specific verification until a reference installation and suitable devices exist. Existing automated tests, Canary, and maintained Dusk/Chromium coverage remain the development gates; deferred checks must not be claimed as passed.
- 2026-07-15: `DatabaseSeeder` called `MilestoneZeroSeeder`, which created a sample client, assessment, and 50 findings. This contradicted the domain-only seed contract and explained why benchmark/CI behavior accidentally depended on seeded sample state. `DatabaseSeeder` now runs only the domain seeders, while the explicit production-guarded `SampleDataSeeder` owns demonstration records; focused tests assert both inventories.
- 2026-07-15: Laravel's development server command only forwards environment values present in `$_ENV`; on this host, shell exports were available through `getenv()`/`$_SERVER` but not `$_ENV`. The first isolated Dusk attempt therefore kept its temporary database but wrote generated files to the configured application storage. The maintained runner now starts PHP's local server with `variables_order=EGPCS`, so the web process and Dusk process resolve the same marked disposable storage. The browser test asserts that generated/deleted report paths are under that root, and the accepted before/after storage manifest is unchanged.
- 2026-07-15: A valid ODS generated by the locked PhpSpreadsheet writer is reported as `application/zip` by the CI host's `fileinfo`, so a simple MIME allow-list incorrectly rejected an approved evidence type. The evidence action now opens ZIP-reported ODS/DOCX/XLSX files read-only, bounds the inspected entries, verifies the expected ODF/OOXML structure, and stores the canonical MIME. Tests exercise JPEG, PNG, WebP, PDF, TXT, LOG, CSV, DOCX, XLSX, and ODS content plus a generic ZIP renamed to `.ods`, which remains rejected.
- 2026-07-15: The required action inventory was compared class-by-class rather than inferred from names. `CompleteAssessment`, `ReopenAssessment`, and `ArchiveAssessment` are consolidated in `TransitionAssessment`; reorder and patch persistence are in `SaveAssessmentWorkspace`/`SaveFindingDetails`; priority override recalculation is in `RecalculateFindingPriority`; recommended/implemented references are in `SetFindingSolutionReference`; finding state changes are in `TransitionFinding`; and evidence creation is split into `StoreEvidenceFile`/`StoreEvidenceUrl`. These implementations are exercised and are not placeholder or dead logic, so introducing duplicate facades during Milestone 4 would add regression surface without fixing behavior. The exact approved class inventory and invokable convention nevertheless remain unmet architecture work, not silently reinterpreted. At that audit point, the absence of `app/Policies` and dashboard persistence for backup/integrity failures was also confirmed; both were resolved by later Milestone 5 slices.
- 2026-07-17: The assessment lifecycle consolidation was resolved without facades: `TransitionAssessment` was removed and its three operations became the required invokable `CompleteAssessment`, `ReopenAssessment`, and `ArchiveAssessment` actions. Checking the caller-provided model before acquiring the aggregate lock allowed a stale draft instance to overwrite a lifecycle change already persisted by another request. Each operation now loads and validates the current row while holding the assessment save lock and database transaction; the regression test proves a stale transition is rejected without changing status or `lock_version`.
- 2026-07-17: The private-storage manifest value supplied at handoff did not include its serialization command and is not reproduced by the explicit relative-path/content algorithm used in this slice. The command `(cd storage/app/private && find . -type f -printf '%P\0' | LC_ALL=C sort -z | xargs -0 sha256sum | sha256sum)` produced `f232151aa30eaecfef301508e45c6fefa7fa903205b9c857897dc62090aea9a8` both before and after the gates. The configured database independently retained the supplied SHA-256 `8b8d7a0ddb7ae0d5a6b79150ae45dc7e29e128018a59b76925602ad47dffe328`.
- 2026-07-17: Operational-status persistence cannot depend on a newly introduced database table because the approved production sequence creates and verifies its pre-migration backup before running migrations; using the new release's backup command against the previous schema would otherwise make a valid backup fail. Backup and integrity outcomes are therefore stored as strictly validated, atomically replaced JSON under the configured private-storage root. The files are covered by application backup/restore, remain available when an integrity failure affects database access, and do not require a schema fallback or a change to the approved backup-before-migrate order. Technical failure text is retained for operations but is not rendered on the dashboard.
- 2026-07-17: Evidence-format acceptance previously generated eight of its ten format families only inside a temporary test directory, leaving the committed fixture inventory incomplete. The repository now contains a valid reusable file for every approved extension, including both JPEG extensions, and the acceptance dataset opens those committed files directly. The existing invalid renamed-ZIP case remains generated separately so it cannot be confused with a reusable valid fixture.
- 2026-07-17: The literal root-container invocation `composer quality` was rejected before script execution by Composer's superuser plugin safety check. Repeating the same locked script as `COMPOSER_ALLOW_SUPERUSER=1 composer quality`, which explicitly enables the two allow-listed Composer plugins in this root-only development container, passed Pint, PHPStan, 149 application tests/1,381 assertions, Canary 34/34 with zero skips, and the locked advisory audit. No production or project configuration was changed for this environment-specific invocation.
- 2026-07-17: Laravel 13 resolves a policy callback only when the policy implements the requested ability method, so a `before()`-only singleton policy would not cover Filament's authorization calls. The implemented policy defines each ability used by the resources, permits ordinary record operations only for the persisted `singleton_key = 1` administrator, and deliberately returns false for bulk delete and force-delete abilities. Evidence and generated-report downloads now perform the same policy check after authentication instead of relying on route middleware alone.
- 2026-07-17: The supplied GitHub log showed translation keys in every failing assertion while a clean checkout had no `.env`; repository inspection established that both Laravel locale defaults were English and the isolated verifier did not override locale or timezone. This made the failures cascade across domain validation, commands, Filament, PDF, and XLSX rather than identifying independent regressions in those features. The maintained environment now exports `it`, `it`, and `Europe/Rome`, the application defaults match that contract, and the exact non-Dusk verification command plus the 48-test affected subset pass even when the parent environment is forced to `en`/`UTC`. This is local evidence only; no hosted GitHub job result is claimed.

- 2026-07-13: The initial repository contains the specification package only; no Laravel application, `composer.json`, or `composer.lock` exists yet.
- 2026-07-13: The available development host was Windows with PHP 8.3.28, Composer 2.9.2, and Git 2.54.0. Distribution-specific package commands were not run. The standalone SQLite CLI was unavailable and was later removed from the runtime contract because the application uses PHP's `pdo_sqlite` extension directly.
- 2026-07-13: Required PHP extensions `fileinfo`, `gd`, `intl`, `pdo_sqlite`, and `zip` were present but disabled on the development host and were enabled; the bundled `sqlite3` extension was enabled as well. The system Composer 2.9.2 cannot self-update without administrator rights, so the bootstrap used a portable Composer 2.10.2 PHAR verified against the official SHA-256 `5ee7125f8a30a34d246cefdc0bc85b8a783b28f2aec968994118512350d28027`.
- 2026-07-13: The independent base bootstrap resolved `laravel/laravel` 13.8.0 with `laravel/framework` 13.19.0. `composer validate --strict` passed, the initial lock audit reported no advisories, SQLite migrations ran, and the two scaffold tests passed after creating the ignored local environment.
- 2026-07-13: The original D-008 package `aureuserp/advance-table-repeater` was unavailable on Packagist, and its upstream repository exposed no release or tag. The user approved removing the dependency and marked the original D-008 SUPERSEDED. The new APPROVED D-008 uses the native Filament 5 table Repeater as the primary implementation; the formal D-008 blocker is resolved. No VCS repository, `dev-master` pin, commit pin, fork, compatibility code, or alternate grid was added.
- 2026-07-13: Spatie Laravel PDF 2.12.0 and DOMPDF 3.1.5 are compatible, but Spatie's stock DOMPDF driver does not expose a post-render hook. The application-owned Spatie DOMPDF driver binding adds the required Canvas `page_script` without changing renderer. The generated proof was parsed successfully as a four-page PDF with accented Italian text, repeated chrome, and `Pagina X di Y`; this integration is proven.
- 2026-07-13: Blueprint audit found pre-implementation defects scheduled for correction in their owning Milestones: the CI stub loses its temporary SQLite environment between steps and omits several required gates; the report stub and 50-finding fixture do not cover the full report contract; `fixtures/evidence/sample.log` is ignored; several required fixture formats/counts are absent; and the Nginx stub has HTTPS and location-order issues. All supplied JSON is syntactically valid UTF-8, and the PHP/Blade stubs pass syntax checks.
- 2026-07-13: Milestone 0 locked the following principal versions: Laravel Framework 13.19.0, Filament 5.6.8, Livewire 4.3.3, Spatie Laravel PDF 2.12.0, DOMPDF 3.1.5, PhpSpreadsheet 5.9.0, Opis JSON Schema 2.6.0, Laravel Boost 2.4.12, Filament Canary 1.2.0, Laravel Dusk 8.6.0, Pest 4.7.5, Pest Laravel plugin 4.1.0, Larastan 3.10.0, Pint 1.29.3, and PDF Parser 2.12.5. The authoritative complete version set remains `composer.lock`.
- 2026-07-13: Package metadata and licenses were reviewed. The principal packages are MIT except DOMPDF (LGPL-2.1-only), Opis JSON Schema (Apache-2.0), and PDF Parser (LGPL-3.0-only). There is no community runtime plugin in Milestone 0. Filament Canary 1.2.0 is a development-only MIT package compatible with Filament 5 and Laravel 13; source inspection found commands, sweep/testing services, and config publication only, with no frontend assets or views. No vendor file was edited.
- 2026-07-13: The Laravel starter Node/Vite files were removed after Filament assets and application-owned plain CSS/JavaScript were published successfully. No Node, npm, pnpm, Vite, browser renderer, or external PDF service is required by the application or report path. The PDF configuration contains only the DOMPDF driver.
- 2026-07-13: Native Repeater clone initially copied hidden persisted identifiers. A subsequent save correctly failed instead of corrupting data. The workspace now derives new-item identity from Filament's stable item UUID and maintains the returned UUID-to-Eloquent-ID map across Livewire requests. Browser proof now adds, clones, deletes, and reorders without duplicate identifiers.
- 2026-07-13: The native table requires a 512 MB PHP memory limit to render the 50-finding proof on this Windows host; the approved PHP configuration already specifies 512 MB, the local PHP configuration was aligned, and `assestme:diagnose` enforces it. The final 50-finding benchmark passed: first render 2.3328 s, response 3,291,784 bytes, explicit save 0.1543 s, reorder 0.1503 s, PDF 0.2898 s, and XLSX 0.1384 s.
- 2026-07-13: Automated evidence is green: 26 Pest tests with 180 assertions; Pint clean; Larastan level 6 with no baseline or ignored errors; Canary strict 5/5 pages with zero failures/skips; Composer validation strict; locked audit with zero advisories; and Dusk Chrome 150.0.7871.101 at 1920×1080 with 1 test/16 assertions covering real login, multiline blur-save, explicit-save presence, add, native clone, confirmed delete, native accessible reorder, offline/unsaved status, and a console free of severe errors.
- 2026-07-13: Manual/browser checklist for Milestone 0: Windows 11, Chrome 150.0.7871.101, 1920×1080, dark mode, login/workspace/PDF/XLSX actions and horizontal table layout visually inspected; pass. A separate real login used the final local administrator and reached assessment workspace 1; pass. ChromeDriver's synthetic drag-and-drop did not trigger Filament's SortableJS reorder on this Windows host, so physical drag-and-drop is not claimed as manually verified. Reordering itself is verified through Filament's native accessible move-down action and the drag handle remains enabled. Keyboard shortcuts, Edge, Firefox, iOS Safari, and Android Chrome are not claimed in Milestone 0 and remain part of the later full blocking QA matrix.
- 2026-07-13: Milestone 1 client/site slice added normalized client fiscal identifiers, non-blocking duplicate warnings, HTTP/HTTPS-only website validation, archived-client protection for new sites, native searchable Filament resources, private PNG/JPEG logo upload constraints, and soft-delete archive actions. Evidence is green: 32 Pest tests with 259 assertions, Canary strict 11/11 pages, Pint, Larastan level 6, and a Chrome browser proof with 1 test/7 assertions and no severe console errors. Dusk's PHP downloader could not use the local PHP CA trust store (`cURL error 60`), so ChromeDriver 150.0.7871.115 was installed from the exact official Chrome-for-Testing milestone manifest URL through the Windows trust store; the binary remains an ignored local test dependency.
- 2026-07-13: The Windows browser proof currently targets the configured local file SQLite database. Dusk's `DatabaseMigrations` reset test data but left that database with migrations rolled back afterward; local schema, domain seed, and administrator were explicitly restored before rerunning Canary. A Canary result obtained while the schema was absent showed 11 skips despite a zero process exit and was rejected; the accepted rerun is 11 passed and zero skipped.
- 2026-07-13: Milestone 1 asset slice added the exact ordered 13-type seed, reserved collision-safe slugs, client/site/type foreign-key constraints, client-site ownership validation, at-least-one-identifier validation, IPv4/IPv6 validation, uppercase colon-separated MAC normalization, and disabled-type protection that preserves existing references. Evidence is green: 39 Pest tests with 328 assertions, Pint, Larastan level 6, diagnostics, Canary strict 17/17 pages, and the expanded Chrome proof with 1 test/11 assertions and no severe console errors.
- 2026-07-13: Milestone 1 classification slice added the exact ordered 18-category seed, archived-record-safe slug reservation with deterministic suffixes, strict uppercase `#RRGGBB` normalization, archive/restore resources for categories and tags, and no premature finding/template associations. Evidence is green: 44 Pest tests with 396 assertions, Pint, Larastan level 6, Canary strict 23/23 pages, and the expanded Chrome proof with 1 test/13 assertions and no severe console errors.
- 2026-07-13: Filament 5.6.8's native `AppAuthentication` provider met D-028 without an extra package. The user model now implements its TOTP/recovery contracts while synchronizing `mfa_enabled_at`; profile enrollment uses exactly eight hashed single-use recovery codes stored through encrypted casts, login requires the TOTP challenge when enabled, profile password changes enforce the approved 14–128 mixed-case/number/symbol policy, and `assestme:reset-mfa` requires confirmation unless explicitly forced. Evidence is green: 49 Pest tests with 432 assertions, including a real primary-credential-plus-TOTP login flow and single-use recovery, Pint, Larastan level 6, Canary strict 23/23 pages, and the Chrome profile proof with 1 test/15 assertions and no severe console errors.
- 2026-07-13: The risk/effort section specifies table names, default labels, matrix values, and high-level constraints but does not define the required columns, keys, color fields, or ownership relations for the five risk tables and only partially defines `effort_levels`. Implementation was not guessed and was deferred while independent Milestone 1 slices continued.
- 2026-07-13: The approved settings dependencies resolved to `filament/spatie-laravel-settings-plugin` 5.6.8 and `spatie/laravel-settings` 3.9.0, both MIT licensed, with zero locked advisories and no broad package updates. An initial Windows-shell command incorrectly reduced `^5.0` to exact `5.0`; Composer rejected the vulnerable initial release and reverted its files. The approved caret constraint was then applied to `composer.json`, and Composer generated the successful lock update.
- 2026-07-13: General/report settings cannot yet be completed without inventing values: `GeneralSettings.active_risk_profile_id` requires the undefined risk schema/seed, while the report specification says “consultant/business contact fields” without naming or typing those properties. The dependency infrastructure is locked, but no incomplete settings class or placeholder UI was added.
- 2026-07-13: The application-aware backup slice now creates a checkpointed SQLite `VACUUM INTO` snapshot, copies private storage including generated reports, records application/settings metadata, writes and verifies a per-file SHA-256 manifest, restricts archives to mode 0600, and applies 7-daily/4-weekly/6-monthly retention to managed archives. Restore rejects non-maintenance execution, verifies the archive before replacement, creates an unpruned safety backup, replaces database and private storage with filesystem compensation, reconnects SQLite, and runs `assestme:diagnose`. The Laravel Scheduler registers the backup for 02:30 Europe/Rome and the existing single system cron entry remains authoritative. The production deployment script was aligned with the implemented restore and diagnostics signatures. CI-visible tests restore both populated and empty private storage against isolated temporary database/storage paths and prove a tampered archive exits non-zero. Evidence is green: 54 Pest tests with 491 assertions, Pint, Larastan level 6, diagnostics, Canary strict 23/23 with zero skips, Composer validation strict, and locked audit with zero advisories. No browser run was claimed because this slice has no browser surface.
- 2026-07-13: The Milestone 1 dashboard now shows real draft/completed assessment counts, open and high/critical included finding counts, the latest five assessments, unresolved cleanup failures, and the timestamp of the newest managed successful backup. Recoverable deletion operations persist UUID, entity, state, trash path, and a size/SHA-256 file manifest. Archive mode retains files; permanent mode moves private files into a same-tree operation directory before the database transaction, restores them when a restricted database delete fails, and records completed or failed post-commit cleanup without false success. Path traversal, duplicate paths, symlinks, and missing referenced files are rejected before deletion. Evidence is green: 62 Pest tests with 545 assertions, Pint, Larastan level 6, diagnostics, Canary strict 23/23 with zero skips, Composer validation/audit, and Dusk Chrome with 1 test/17 assertions covering the dashboard plus existing Milestone 1 pages with no severe console errors.
- 2026-07-13: `assestme:storage:cleanup` now retries only committed or previously failed deletion operations, validates that each persisted trash path exactly matches the configured private-trash root and operation UUID before removal, treats an already absent committed directory as cleaned, and exits non-zero while preserving a visible `cleanup_failed` record when removal fails. Focused success, invalid-path, and mocked filesystem-failure tests are included. Evidence is green: 65 Pest tests with 569 assertions, Pint, and Larastan level 6.
- 2026-07-13: The CI workflow and its maintained stub keep the SQLite environment across steps, run every current quality/diagnostic/benchmark gate, run Dusk against a readiness-checked local server, retain failure artifacts, and include a separate clean-checkout bootstrap job that verifies exactly one administrator and all 18 seeded categories. `bootstrap-local.sh` runs domain seeds and diagnostics; `verify.sh` runs Dusk by default unless explicitly disabled by CI before its dedicated browser step. All shell scripts are committed executable. Both YAML files parse successfully with Symfony YAML and the embedded Tinker assertions were executed locally. Specification 2.3 later clarified that the hosted runner is CI infrastructure rather than a production operating-system requirement.
- 2026-07-13: D-037 resolved to `leek/filament-right-click` 1.3.5 and `occtherapist/advanced-table-export-for-filament` 1.0.1, both MIT licensed and compatible with the locked Laravel 13/Filament 5 stack. Package metadata, service providers, Composer hooks, registered assets, views, translations, actions, and relevant source paths were inspected before activation; neither package adds a Composer plugin or install script, and the exporter reuses locked OpenSpout 4.32.0. Right Click adds one lazily loaded JavaScript asset and stylesheet, published through the native `filament:assets` command without Node or a frontend build. Advanced Table Export has no frontend build requirement. No vendor file was edited.
- 2026-07-13: The application-owned standard-table enhancement applies Right Click only to the seven currently existing standard resource lists and keeps every context action duplicated by a visible Filament record action; it is absent from the assessment workspace. Generic export is intentionally limited to CSV and XLSX, with Italian controls and a 2,000-row cap; plugin PDF/JSON/XML/clipboard paths are disabled, so assessment PDF/workbook generation remains dedicated. A focused test reopens a real XLSX download, and Dusk dispatches a real `contextmenu` event, invokes the contextual edit modal, proves the visible edit path, and reports no severe console errors. Accepted evidence is green: 75 Pest tests with 608 assertions; Pint; Larastan level 6; Composer validation/audit; Canary strict 23/23 with zero skips; and Dusk Chrome 150.0.7871.101 at 1920×1080 with 1 test/23 assertions. The database, domain seed, and local administrator were restored after Dusk.
- 2026-07-13: The user approved the missing Milestone 1 decisions and specification 2.2 records them as D-038 through D-040. Risk profiles own their consequence, likelihood, priority, and matrix rows; effort levels are global. VAT treatment, rates, and calculations are absent and non-configurable, while VAT numbers remain anagraphic identifiers only. PDF and XLSX each show the fixed VAT-excluded estimate note once. Optional consultant identity/contact fields now include logo, PEC, tax code, and a text-only name/role signature block.
- 2026-07-13: The native Filament 5 table Repeater layout was remediated without replacing the approved component or editing vendor files. `compact()`, wrapped headers, top-aligned cells, bounded three-row multiline controls, and Filament's native container-responsive vertical rendering replace the previous forced fixed table layout. The trailing clone/delete cell previously collapsed to 34 px and allowed both 32 px actions to overflow; the workspace-scoped published stylesheet now reserves a 96 px native action column. Browser geometry confirms both actions remain inside that cell, the document has no horizontal overflow, and the first long-content row remains 93 px high. Evidence is green: `composer quality` with 78 application tests/655 assertions, PHPStan with zero errors, Canary strict 23/23 with zero skips, and locked audit with zero advisories; focused Dusk adds 1 test/34 assertions for desktop containment, mobile rendering, persistence actions, offline state, and zero severe console errors. Dusk now uses file-SQLite `DatabaseTruncation` across both browser classes so the aggregate run no longer leaves stale WAL/schema state between classes.
- 2026-07-13: Milestone 1 risk, effort, and settings are complete. Risk-profile ownership and same-profile matrix constraints are enforced by foreign keys and one aggregate transaction; referenced levels are disabled instead of deleted, the active/default profile remains enabled, and all 16 consequence/likelihood combinations are required. General and report settings use typed Spatie settings classes and private consultant-logo storage; VAT identifiers are normalized anagraphic values and no fiscal setting or calculation exists. Accepted evidence is 87 application tests/774 assertions, Pint clean, Larastan level 6 with zero errors, Canary strict 31/31 with zero skips, locked audit with zero advisories, and Dusk Chromium 150 with 1 test/28 assertions and no severe console errors.
- 2026-07-13: The Linux browser-test host provided Chromium as a confined package. ChromeDriver session creation stalled while discovering an ephemeral debugging port and running as root was rejected by Chrome's sandbox. The Dusk harness now adds `--no-sandbox` only when the effective user is root and uses ChromeDriver's supported `--remote-debugging-pipe` transport; the resulting browser run passes. This affects development/CI browser transport only and adds no production runtime dependency.
- 2026-07-13: Milestone 2 template management is complete. Aggregate saves enforce same-profile risk references, solution ownership, estimate/billing invariants, exactly one recommended solution, and soft deletion of omitted nested solutions. Import preview reports create/replace/unchanged state and field-level differences before the confirmed atomic apply; invalid or duplicate payloads leave the database unchanged, and JSON export re-imports as data-equivalent. The domain seeder installs eight Italian templates with eight solutions. Accepted evidence is 99 Pest tests/851 assertions, Pint, Larastan level 6, Canary strict 34/34 with zero skips, Composer validation/audit, and focused Dusk with 1 test/5 assertions and no severe console errors.
- 2026-07-13: Milestone 3 replaced the temporary proof projections with the definitive related finding-solution, risk, effort, scope, and evidence records. Native Filament file upload state is adopted from a private pending directory only after application-level extension, MIME, size, image-dimension, duplicate-hash, and aggregate-limit validation; downloads remain authenticated with defensive response headers. Closing a slide-over and mutating lifecycle state in the same Livewire page exposed an Alpine action-state reference after the component tree changed; successful complete/reopen transitions now perform a full workspace redirect, preserving the approved component and avoiding vendor changes. Completed-to-archived transitions preserve `completed_at`, while reopening clears it. Accepted evidence is 111 Pest tests/1,001 assertions, Pint, Larastan level 6, Canary strict 34/34 with zero skips, Composer validation/audit, focused Dusk with 1 test/8 assertions and no severe console errors, diagnostics fully green, and every 50-finding performance budget passing.
- 2026-07-15: The first Milestone 4 reporting slice replaced the user-facing proof PDF with the definitive Spatie Laravel PDF/DOMPDF pipeline. Immutable normalized payload and complete settings snapshots, PDF version rows, physical UUID filenames, SHA-256 verification, completion validation, verified embedded evidence, PNG/JPEG logo revalidation including the 5 MB limit, archived master-data labels, the fixed VAT-excluded note exactly once, authenticated downloads, history, freeze-safe reload, and generated-report storage audit are implemented. The direct proof PDF/XLSX routes were removed so incomplete Milestone 0 artifacts cannot be downloaded as reports. Automated PDF coverage parses Italian output, first/final content page labels excluding the cover, long text, page breaks, 50 findings, alternatives, real image/caption embedding, non-image type references, clickable URL annotations, consultant/client logos, missing/corrupt evidence, invalid/oversized logos, immutable versions, tamper rejection, and freeze only after successful persistence. Accepted current evidence is `composer quality` with 120 application tests/1,101 assertions, Pint clean, PHPStan over 212 files with zero errors, Canary strict 34/34 with zero skips, locked audit with zero advisories, aggregate Dusk Chromium with 5 tests/91 assertions and no severe console errors, and a complete successful `scripts/verify.sh` run. The definitive 50-finding PDF gate passes the 30-second and 50 MB limits. The maintained general benchmark is green at 1.1782 s first render, 0.0521 s explicit save, and 0.0568 s reorder, but its PDF/XLSX metrics still time the internal Milestone 0 proof generators until the XLSX slice can migrate both report metrics together.
- 2026-07-15: Laravel `DatabaseTruncation` preserves the framework migrations table but normally truncates Spatie's `settings` values, so an aggregate Dusk run left later classes unable to reconstruct typed settings whose settings migration was already marked as run. The shared browser harness now preserves only the `settings` table; browser tests do not mutate it, while all domain/test records continue to be truncated. The accepted aggregate rerun is 5 passed with 91 assertions.
- 2026-07-15: Laravel `Filesystem::allFiles()` excludes hidden files unless its second argument is true. The application backup therefore omitted `.gitignore` and could also omit staged recovery data below `.trash`, contradicting the complete-private-storage backup requirement. Copy, manifest, and archive enumeration now explicitly include hidden files. The restore integration test proves hidden placeholder and staged-trash round trips, and a real pre-Dusk archive was inspected to contain `storage/private/.gitignore` before a successful restore.
- 2026-07-15: Standard Filament `DeleteAction`, `DeleteBulkAction`, and `ForceDeleteBulkAction` operated directly on Eloquent and therefore bypassed the configured archive/permanent policy and staged file recovery. The resource layer now contains no standard destructive delete action: a shared policy-aware action provides the visible/header/right-click paths, delegates business behavior outside Filament, reports restricted deletion without false success, and warns when committed trash cleanup remains retryable. Destructive bulk deletion was removed because the approved recovery service is intentionally record-scoped; bulk restore remains available.

---

## 24. Final outcome

Complete this section only after all acceptance criteria pass.

Not yet implemented.
